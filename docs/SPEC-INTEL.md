# SPEC-INTEL — commerce-intel 고도화 스펙

**v2 · 2026-08-03**

이 문서가 commerce-intel의 **유일한 스펙이자 판단 기준**이다 (2026-07-31 사용자 결정 D11).
구 `docs/SPEC.md`(v16)는 삭제됐다 — 실측 근거는 `docs/EVIDENCE.md`로 추출했고 전문은
git 이력에 있다. `docs/TEST-CASES.md`는 v11(2026-07-31, 사용자 지시 개정)로 intel 기준이 됐다.
여기 없는 세부 절차는 `skills/` 각 SKILL.md가 정본이다.

## 이 문서를 고치는 규칙 (2026-08-03 사용자 지시로 개정)

**이 스펙은 동결 문서가 아니다. 구현이 바뀌면 스펙을 그에 맞춰 갱신한다.**
구 규칙("스펙을 고치지 말고 어긋난다는 사실을 보고하고 지시를 기다린다")은 폐기됐다 —
문서가 구현보다 낡으면 다음 세션이 낡은 쪽을 사실로 읽는 것이 더 큰 해악이다.

지킬 것은 셋뿐이다.

1. **구현과 함께 고친다** — 코드를 바꾼 커밋에서 스펙도 같이 바꾼다. 나중에 몰아 쓰지 않는다.
2. **왜 바뀌었는지 남긴다** — 결정 기록(§1)에 한 줄, 날짜와 근거(사용자 지시/실측)를 적는다.
   기각된 대안도 적는다. 그게 다음 사람이 되돌리지 않게 하는 장치다.
3. **실측과 판단을 섞지 않는다** — 잰 값은 잰 값으로, 결정은 결정으로 적는다.
   미검증 추정을 사실처럼 적는 것은 이 프로젝트의 제1원칙 위반이고 이 규칙에도 예외가 없다.

## 버전 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v1 | 2026-07-31 | 최초 작성. 인터뷰 3라운드 결정 반영 |
| v2 | 2026-08-03 | 스펙 동결 해제(구현에 맞춰 갱신). 리포트 모듈화 D25 · 공통 UI 규약 D26 · §4 개정 · §6에 사이즈별 재고 분석 반영 |
| v35 | 2026-08-07 | **상품명 뱃지 보존 + 겹침 분석 규칙 D77** — name_badge 속성(접두 대괄호만, 전 위치 초안은 실측 기각·원복), SKILL.md에 match_key JS + 검산 의무, Turso 재백필 912상품 |
| v34 | 2026-08-07 | **누적판매 구간 하한 적재 D76** — purchase_count 한정 D48 예외(사용자 결정), 적재 폴백 + Turso 소급 144행, 원문 display 보존 |
| v33 | 2026-08-07 | **prune 자동화 + Turso 직접 솎기 D75** — open_db()/default_db_target() 통일, 임시 테이블 제거(청크 DELETE·obs_attr 명시 삭제), VACUUM 로컬 한정, crontab 주간 등록(--cron) |
| v32 | 2026-08-07 | **분석이 EDA 산출물을 입력으로 받는다 D74** — analyze/insight --signals(요청 문맥 검증·불일치 시 자동 재수행), intel-query 분석 섹션에 --ai-notes 방법론 조정·소요 실측(소규모 29분) 문서화 |
| v31 | 2026-08-07 | **토큰 읽기/쓰기 분리 + 고아 참조 정리 + 브랜드 병합 D73** — pick_token(인자 > WRITE > 읽기), 쓰기 토큰 SKILL.md 하드코딩 제거(env 경유), clean-orphans 명령(runs 48h 보존 규칙), 2000아카이브스 3분열 병합 + 수동 별명 등록 |
| v30 | 2026-08-07 | **시트 미러 편집·삭제 감지 + Aside 분석 확장 D72** — mirror_dirty 트리거 카운터(UPDATE·DELETE, 상쇄 케이스 포함) + 행 수 대조 보조로 증분 탭 자동 재구축, 분석 스크립트 4종(--db 기본값)을 default_db_target()으로 통일해 INTEL_DB_URL만으로 Turso 직접 연결, intel-query 스킬에 분석·리포트 섹션 추가 |
| v29 | 2026-08-06 | **미완 항목 일괄 처리 D71** — 재료 변경 무효화 attr_base 확장(무효화 이력), prune 전 지표+obs_attr 감시(빈도순 CASE)+FK pragma, upload --force를 교체 의미로 재정의+뷰·트리거 후순서. display 컬럼 제거안은 D48·실데이터 5,722건 근거로 기각 |
| v28 | 2026-08-06 | **proxy.db 본 DB 통합 D69** — D65-8에서 분리했던 proxy_defs·proxy_cache·proxy_history를 본 DB(intel.db)의 SCHEMA_V3에 통합. lazy 판정 전환 이후 캐시가 천천히 쌓여 분리의 이점이 약해졌고, FK/트리거/자동동기화를 못 하는 대가가 더 커졌다. proxy_connect/proxy_db_path/proxy_db_exists/PROXY_SCHEMA 삭제, 모든 호출부 conn 직접 사용으로 통일. _SCHEMA_MARKER를 proxy_history로 갱신해 기존 v3 DB가 프록시 표를 자동 수령. migrate_proxy_merge.py 신규(로컬+Turso). upload_to_turso --proxy 폐기, 프록시 표를 INTEL_ORDER에 포함 |
| v27 | 2026-08-05 | **상품 정적 속성 이력 + 프록시 재판정 체인 D68** — product_history·attr_history(intel.db)·proxy_history(proxy.db), 조회 뷰 product_changes·attr_changes, 재료(이름·이미지) 변경 시 프록시 캐시 무효화→재판정 대기 이력→다음 판정이 완성. connect()가 기존 v3에 빠진 표를 마커(최신 추가 표) 확인으로 1회 보충(라이브·Turso 무이관 업그레이드 — 매 connect DDL 왕복은 PR #14 리뷰로 기각) |
| v25 | 2026-08-05 | **vision 프록시 lazy·정의 샘플 접지 D66** — 정의는 실물 썸네일 1~2장 열람 후 즉시, 판정은 분석 요구 시 협의 후 배치. 절차 명문화(proxy-extraction·SKILL·db-contract) |
| v26 | 2026-08-05 | **Turso 클라우드 이전 D67** — open_db() 연결 추상화(INTEL_DB_URL), libsql 드라이버 호환 래퍼(row 셈·예외 번역), 프록시 별도 DB, upload_to_turso 이관·검산, check-run 중복 방지. 회귀 61+222+10 통과 |
| v24 | 2026-08-05 | **DB 스키마 v3 D65** (사용자 확정 설계 10항) — 컨텍스트 CHECK · 상품/옵션 경량화 · 카테고리 계층화(N:M) · 브랜드 별명/입점 정규화 · obs_attr · runs 정수 PK FK · proxy.db 분리 + lazy 판정 · migrate_v3. 정본 120MB → intel 19MB + proxy 110MB, 회귀 61+222 통과 |
| v23 | 2026-08-05 | **시트 증분 5,000행 청크 D63**(대량 증분 500 해소) · **정본 v2 교체 D64** — 이관이 sync_state 진행점을 새 rowid 번호로 번역(proxy_cache 구멍 1.5만 행 무소음 누락 차단). 151→120MB, 회귀 61+210 통과 |
| v22 | 2026-08-04 | **배포가 조용히 실패하던 것 D57**(스킬 7종이 팀원 환경에 안 깔려 있었다 — 프론트매터 YAML) · **정본 부분 배포 D58**(2000아카이브스 + 여성 데님팬츠 브랜드랭킹 상위30만 seed DB로) · **merge가 새 DB에서 죽어 있던 것 D59**(배포본으로 팀원 절차를 밟아서야 보였다) |
| v21 | 2026-08-04 | **프록시 값 공간 오염 3겹 방어 D53** · **생존 편향 — 이탈 브랜드 D54**(동적 속성이 축이 되게) |
| v20 | 2026-08-04 | **EDA 정리 D52** — 공식 `data-*`는 대체가 아니라 보완(탐색·공유 전 QA) · 규칙 정본을 analysis-context 하나로 · `sanity_check.py`를 파이프라인에 연결 |
| v19 | 2026-08-04 | **Y 선정을 그룹 비교에도 D51** — 구조 결측 분리(자사몰이 하트를 안 준다) · 브랜드 표기 정규화 · 프록시 자동 배선 D51-a · 접두사 가드 D51-b |
| v18 | 2026-08-04 | **화면 수집기 D50** — 조회수·누적판매 **결측이 무작위가 아니다**(잘 팔린 것만 노출). 경쟁셋 = 브랜드 랭킹 상위 30 D50-a |
| v17 | 2026-08-04 | **레이아웃 축 전환 D49** — 「차이 없음」을 판단 보류 위로. 「말할 수 있는 것」으로 합침 · 광고는 나눠서 본다 D49-a |
| v16 | 2026-08-04 | **robots 호스트별 확인 D48** — `goods-detail`은 `Disallow: /`. 구간 표기를 순서형 축으로(하한은 정확하다) · 무신사 허용 경로 수집기 D48-a |
| v15 | 2026-08-04 | **모델링 규칙 D47** — lever/response 역할로 상관 방향 세움 · 퍼널 전환율 · 액션 제안 · 「차이가 없었다」 |
| v14 | 2026-08-04 | **시트 미러 무결성 D46** — 올린 뒤 세어 보고 진행점을 옮긴다. 총계 대조·`--repair`·빈 열 정리 |
| v13 | 2026-08-04 | **DB 스키마 v2 D45** — 반복 텍스트를 정수로. 31MB→15.9MB · 관측 1행 266→81바이트. 읽는 코드 무수정(뷰+트리거) · 솎기 D45-a |
| v12.1 | 2026-08-04 | **계층 필터 우산/리프로 좁힘 D42-a** (PR #8 리뷰) · `refresh-cycle` 7개 플랫폼 형식 통일 |
| v12 | 2026-08-04 | **카테고리 계층 판정 D42 · 프록시 기본 생성 D43 · 인사이트 표현 D44** — 대등하지 않은 쌍 제외, 묻지 않아도 프록시를 만든다, 청중 배지·표시 10개 |
| v11 | 2026-08-04 | **랭킹 축적 4사이트 허용 D41** — W컨셉·EQL·SSF·무신사 글로벌(D30 개정). 갱신 주기·기간별 랭킹 실측 |
| v10 | 2026-08-03 | **프록시 자동정의 연결(D39 갭 해소) · 상세 PDF 차트 D40** — 분석 요청이 프록시를 만든다. 차트는 reportlab Drawing 직접 렌더(외부 라이브러리 0) |
| v9 | 2026-08-03 | **AI 프록시가 검정 축이 되도록 D39** — HTML→PDF 전환(D27) 때 EDA·분석이 프록시를 안 읽던 누락 복구. 사용자 지적("프록시 가설검정이 왜 없나 — 제일 중요") |
| v8 | 2026-08-03 | **무신사 글로벌 플랫폼 스킬 D38** — `platform-musinsa-global`(draft). 국내와 별개 도메인·통화·수집 경로. 정찰 실측 근거 |
| v7 | 2026-08-03 | **동적 속성 표 D35 · 신규 플랫폼 스킬 2종 D36** — 축을 스키마 변경 없이 늘린다. EQL·SSF 어댑터(실측 근거). 후기 수집은 별도 스킬로 분리 결정(미착수) |
| v6 | 2026-08-03 | **스타일:변형 2계층 D34** — 플랫폼마다 한 상품을 세는 단위가 달라 생기던 편향을 없앤다. 시즌 표기 보존도 함께 |
| v5 | 2026-08-03 | **분석 단 신설·4단 분리 D33** — EDA가 만든 신호를 아무도 안 읽던 구멍을 메운다. 방법론 결정(규칙표+AI 예외)·기법 4종 신설·임계 완화 |
| v4 | 2026-08-03 | 팀 커버리지 조회 D32 — D31(전원 풀 배포)의 대가인 중복 수집을 막는다. §2-2에 팀원 수집분 규칙, §3에 읽기 예외 1건 명시 |
| v3 | 2026-08-03 | **전면 재설계.** MD 인터뷰 + 소유자 피드백 9건 반영(`0803_인터뷰 정리.md` · `0803_feedback.md`). HTML 산출 폐기·PDF 전환 D27 · 리포트 2층 D28 · 파이프라인 5단 분리 D29 · 신규 플랫폼 수집 신분 D30 · 배포 형태 D31. **D25·D26은 인터랙티브 UI 전제라 폐기**(D27이 대체) |

## §0 기본 예제와의 차이 (요구 원문)

1. raw data가 하드코딩이 아니라 **구글 시트 형태로 저장**되어야 한다(업데이트 가능).
   이미 저장된 레코드는 **재조사/재저장하지 않는** 재사용 가능한 DB 설계가 필요하다.
2. 리포트는 **interactive 산점도를 포함**해 가격 탄력성에 대한 가설을 사용자가 직접
   도출할 수 있어야 한다. **변인통제**가 가능해야 한다(단순 data summary 금지).
3. 입점처를 하드코딩하지 않는다 — **입점처 자체를 리서치하는 서브 에이전트**와,
   처음 접하는 플랫폼의 스킬을 만드는 **skill-maker 스킬**이 필요하다.
   단 중요 플랫폼(무신사·29CM·자사몰)은 제네릭과 **따로** 전용 스킬로 만든다.
4. 유저 스토리는 3개 고정이 아니다 — 실무자 관점에서 파생되는 요구를 모두 다룬다.
   궁극 목적은 **시장 조사와 가격 탄력성 분석**이다.

## §1 결정 기록 (2026-07-31 인터뷰)

| # | 분기 | 결정 | 기각된 대안 |
|---|---|---|---|
| D1 | 정본 위치 | **로컬 SQLite 정본 + 구글 시트 미러** | 시트가 곧 DB(쿼터·복구 취약) / 시트 정본+로컬 캐시(동기화 충돌) |
| D2 | 시트 쓰기 수단 | **Sheets API + 서비스 계정(gspread)** | Drive 커넥터(셀 단위 갱신 불가) / Apps Script 웹훅 |
| D3 | 탄력성 분석 | **수요 프록시는 많을수록 좋다. 축 2개 고정 아님, 산점도 외 그래프도 허용 — 가능성을 열어둔다** | 단일 프록시 고정 |
| D4 | 문서 처리 | **SPEC-INTEL.md 분리, 기존 SPEC·TC 동결** | v17 정식 개정 / 나중에 반영 |
| D5 | 스킬 분할 | **오케스트레이터 + 플랫폼별 독립 스킬 + generic + skill-maker** | 단일 스킬 유지 / 부분 분리 |
| D6 | 자동화 범위 | **개발 단계에서는 스킬 초안까지 자동 생성.** 최종 다듬기·배포는 개발자(사용자) | 승인 후 생성 / 발견만 |
| D7 | 정적 속성 재사용 | **TTL 90일** — 만료 시 다음 수집 때 재확인 | 영구 재사용 / 30일 / 180일 |
| D8 | 시변 값 스킵 창 | **사이트 갱신 주기 기준**(무신사 실시간 30분·29CM 1시간 등, 어댑터 명시값). 주기 미상이면 24시간 | 일률 24h / 항상 재관측 |
| D9 | 자사몰 스킬 범위 | **엔진 무관 `platform-ownmall`** — 자사몰 공통 절차를 스킬이 갖고 엔진별 수집 스킴만 분기한다. SPEC v16의 "Cafe24가 아니면 비범위" 제약을 **폐기한다** | `platform-cafe24`(엔진 한정) |
| D10 | 원본 스킬 폴더 | **`commerce-research/` 제거** — 어댑터·스크립트를 `skills/`로 이관한 뒤 삭제한다 | 원본 보존(upstream 머지 통로) |
| D11 | 구 SPEC 처리 | **SPEC은 하나만 둔다** — `SPEC.md`(v16)에서 실측 근거만 `docs/EVIDENCE.md`로 추출하고 SPEC.md는 삭제(git 이력 보존). intel 판단 기준은 이 문서다. `TEST-CASES.md`는 v11로 intel 기준 개정(사용자 지시) | SPEC.md를 아카이브로 병존(혼동 유발) |
| D12 | 외부 플러그인 차용 | anthropics/knowledge-work-plugins(Apache-2.0)에서 **선별 차용, 통째 설치는 안 한다**(CDN 의존·트리거 경합). 차용: 통계 가드레일(심슨·생존편향·다중비교·누적 지표) → analysis-report §4, 경쟁 브리프 구조 → story-catalog §D, 근거-주장 분리 보고서 → channel-scout, 스킬 템플릿 placeholder 설계 → skill-maker, 파이프라인 테스트 항목(멱등성 등) → 테스트 확충 시 | 플러그인 4종 통째 설치 |
| D12-c | skill-maker 방법론 | anthropics/claude-code **plugin-dev skill-development**의 6단계 생성 프로세스·트리거 작성 규칙(3인칭+구체 문구)·검증 체크리스트·간결성 기준(본문 2,000단어, 상세는 references)을 platform-skill-maker에 채택. 원문 복사 대신 우리 원칙(실측 일자·「미검증」 표기)과 결합해 재작성 | 원문 파일 동봉 |
| D12-d | eval·시각화 차용 | ① awesome-copilot **agentic-eval**의 evaluator 패턴(기준표 PASS/FAIL·반복 상한·수렴 확인)을 **에이전트 산출물 검수 절차**로 채택(재요청 1회, 미달 시 그대로 보고) ② data 플러그인 **data-visualization**의 차트 선택표·시각 규칙(파이/3D 금지·막대 0 시작·색약 안전)과 **build-dashboard**의 대시보드 구조(KPI 카드·단일 filteredData 파이프라인·사전 집계 기준)를 analysis-report에 채택. **Chart.js CDN은 외부 리소스 0 원칙과 충돌해 기각** — 구조만 차용 | 대시보드 스킬 통째 도입 |
| D13 | 애그리게이터 고정 | 판매처 역추적용 쇼핑 애그리게이터는 **네이버쇼핑·카카오 쇼핑하우·구글 쇼핑(보조)으로 하드코딩**한다(2026-07-31 사용자 확정 — "애그리게이터를 찾는 에이전트를 또 돌리는 건 비효율"). 다나와·에누리는 전자 중심이라 제외. robots 정책만 런타임 확인 | 애그리게이터 자체를 리서치 |
| D14 | **상태 변경 금지 원칙 개정** | 재고 파악을 위해 **계층적 프로브를 허용**한다(§6). L1 무료(옵션별 품절 상태) → L2 읽기 전용(수량 검증 응답) → L3 장바구니(담고 즉시 비움, **사용자 승인 필수**). **결제 단계 진입은 절대 금지**, 찜·리뷰 작성 등 다른 상태 변경도 금지 그대로. 구 원칙("구매·장바구니·찜 등 상태를 바꾸는 조작" 일괄 금지)은 이 범위에서만 완화된다 | 읽기 전용만 / L3 무승인 허용 |
| D15 | 재고 정밀도 | **정확한 수량(이진 탐색)** — 상한 N까지 탐색하고 초과는 `N+`로 기록(무한정 아님) | 구간 판정 / 유무만 |
| D16 | 옵션 수집 범위 | **2단계** — 라인시트 전체는 무료분(옵션 구성·품절 상태)까지, 재고 프로브는 **지정 상품 또는 상위 N개**에만. 예상 소요를 먼저 보고 | 전 상품 전수 / 지정 상품만 |
| D17 | 옵션별 판매수량 | **재고 시계열 감소분**으로 얻는다 — 관측 두 개의 차이라 추정이 아니다(가격 변화 감지와 같은 성격). 한계 2개를 반드시 명시: **재입고가 섞이면 과소 집계** · 축적 전에는 값이 없다. 관측 주기는 **기존 랭킹 주기에 편승**(무신사 30분·29CM 1시간) | 리뷰 옵션 분포 / 수집 안 함 / 리뷰로 역산(추정 금지 위반) |
| D18 | **상태 변경 원칙 재개정** (D14 대체) | 정보 획득에 필요한 상태 변경은 **실제 결제 실행만 빼고 전부 허용**한다(2026-07-31 사용자 확정). 결제 **페이지 진입은 허용**, 결제 완료는 절대 금지. D14의 L3 사용자 승인 요건은 **폐지** — 대신 상태 변경을 썼으면 무엇을 했고 되돌렸는지 **보고 의무**. 가역 조치(장바구니 비우기 등) 의무는 유지. **비로그인 원칙은 별도 결정 전까지 유지** — 로그인이 필요해지면 그때 묻는다 | 계층 승인제 유지(D14) |
| D19 | 파생 프록시 계층 | ① **수집은 풍부하게** — 화면 노출 재료(배지·라벨 등)를 `raw_extras`로 원문 보존 ② **추출은 리포트 단계** — DB 차원을 늘리지 않고 리포트가 프록시를 계산·주입 ③ **프록시는 제네릭** — 하드코딩 없이 요청마다 AI가 정의(proxy_defs)하고 ④ **비싼 판정(비전 등)은 proxy_cache로 재사용** — 재료 지문(fingerprint)이 같으면 재판정하지 않는다 | DB 컬럼으로 저장(차원 폭발) / 매번 재판정(토큰 낭비) |
| D20 | 분석 리포트 생성 방식 | **AI 자유 생성이 요청 특화 리포트의 기본**(2026-07-31 사용자 확정 — 4단계 흐름: 수집 요청→DB 갱신→분석 요청→맞춤 리포트). 품질은 구조로 보강: ① 데이터는 `--emit-json`으로 코드가 추출(손 전사 금지) ② 필수 요소는 `lint_analysis_html.py`가 검사(통과 전 완성 보고 금지) ③ 렌더 확인. 범용 대시보드는 빠른 탐색용으로 유지 | 섹션 조립형 / 프리셋만 |
| D21 | 전수조사 표본 모드 | **사용자가 명시할 때만** 표본 수집을 허용한다(임의 샘플링 금지는 유지 — 묻지 않은 표본이 금지인 것). 설계: **로그-랭크 층화 + 층내 균등 간격**(피보나치 직관의 정식화 — 표본 크기 조절·결정적 재현·층 가중 복원). **목록부터 표본**(사용자 확정 — 총계 대조 대신 "표본 계획 완주"가 완전성 기준, `meta.sampling` 필수). **확장은 중첩 정밀화**: 간격 절반 오프셋 추가 → 기존 표본이 부분집합으로 유지되어 재조사·중복 0 + DB product_id·reuse-attrs가 겹으로 방어. **층 가중 추정 허용 + "표본 추정" 라벨·층별 n 병기 강제** — 추정 금지 원칙은 "미노출 값 지어내기" 금지이고, 공개된 설계의 표본 추정은 다른 층위임을 명시 | 피보나치 그대로(표본 ~log N개) / 비싼 단계만 표본 / 가중 추정 금지 |
| D22 | **규모 상한 전면 폐지** | "이 프로젝트에 존재하는 모든 상한을 없앤다"(2026-07-31 사용자 확정). 수집 규모 상한·협의 임계(라인시트 3,000/전수조사 5,000) **폐지** — 규모와 무관하게 진행하되 **예상 소요 사전 보고는 유지**(투명성), 0건 중단·무한 루프 방지(3연속 무증가)도 유지. ※ 랭킹 top 100은 상한이 아니라 **diff 연속성 규격**, 재고 이진 탐색 N=100은 무한 루프 방지 안전장치라 존치 — 이것도 없애려면 별도 지시 | 상한 유지/협의 유지 |
| D23 | 프록시 정의 주체 | **AI가 자동으로 제안·정의·등록한다**(2026-07-31 사용자 확정 — "새 프록시를 사람이 직접 정의하는 게 아니라 AI가 자동으로"). 분석 요청을 받으면 유용한 프록시를 스스로 판단해 정의 카드(이름·질문·재료·값 공간·method)를 만들고 `proxy-load`로 즉시 등록·판정한다. 사용자에게는 **정의 카드를 보고**한다 — 선택지로 묻지 않는다. `proxy_defs` 기존 정의 재사용 우선은 유지. **예외 없음** — 사람 외형 값 공간도 AI가 설계·등록한다(같은 날 후속 지시로 사전 확인 예외 삭제). §7-2 정직성 규칙은 그대로 적용: 인상 분류임을 값 이름·각주에 명시, 확신 없으면 unknown | 사용자 확정 후 등록(구 §7-1 운용) / 사람 외형만 사전 확인 |
| D24 | 시계열 뷰 상시화 | 범용 대시보드에 **시계열 추이 패널을 상시 추가**(2026-07-31 사용자 지시 "랭킹을 소스로 시계열 뷰 상시 추가"). 축적 관측(시점 2개 이상) 있으면 자동 표시·없으면 숨김. 순위 축 반전·순위권 밖 선 끊김·이동평균·48시점 다운샘플·120계열 상한. 예측/외삽 없음 | 랭킹 전용 diff만 유지 |
| D25 | **리포트는 종류가 아니라 모듈의 조합** | 전수조사·랭킹·라인시트를 각각 별개 리포트로 뽑지 않는다(2026-08-03 사용자 지시). 기능을 모듈로 갖고 있다가 **사용자가 고른 것만** 한 리포트로 조립한다 — `--modules`(CLI)와 리포트 안 토글 **둘 다** 제공한다. 모듈 9종: `kpi` `scatter` `dist` `group` `variants` `timeseries` `events` `linesheet` `table`. **요청했지만 데이터가 없어 못 만든 모듈은 화면에 적는다** — 조용히 빠지면 *안 한 것*과 *못 한 것*을 구분할 수 없다. 조건 패널은 모듈이 늘어도 하나이고 전 모듈이 같은 `filteredData`를 본다. 스토리 리포트(`build_report.py`)는 **원본 JSON 입력 경로로 존치**한다 — DB 적재 전에도 리포트가 나와야 하고 회귀 111건이 그 출력을 고정하고 있다 | 리포트 종류별 별도 파일 유지 / CLI만 / 리포트 안 토글만 |
| D26 | 조작 규약은 하나 (`report_ui.py`) | 스토리 리포트와 분석 리포트가 팔레트·CSS·조작 JS를 각자 갖고 있어 생김새와 동작이 갈라지던 것을 **공통 모듈 하나로** 모은다(2026-08-03 사용자 지시 "리포트 종류별로 다른 방식 통일"). 공유: 색 토큰·컴포넌트 CSS·`data-tip` 툴팁·표 정렬·칩 필터(축 안 OR/축 간 AND·건수 병기)·계층 캐스케이드·묶어 보기·**불리언 수식 컴파일러**. 공유하지 않는 것: 필터 적용 방식(스토리는 DOM 숨김, 분석은 `filteredData` 재계산) — **술어는 공유하되 적용은 각자**다. 도움말은 전부 `data-tip`이고 `title` 속성은 쓰지 않는다(브라우저가 1초 넘게 기다렸다 띄워 "도움말이 없다"로 읽힌다) | 각자 유지 / 전면 통합(적용 방식까지) |
| D27 | **HTML 산출 폐기 → PDF** (D25·D26 대체) | 리포트 산출물에서 **HTML을 없앤다**(2026-08-03 사용자 지시 — "html이 아예 없었으면 좋겠음. 분석은 AI가 하고 디테일한 리포트를 pdf 형식으로"). 읽는 자리가 모바일이고 보고·공유에 쓰이기 때문이다. 조건을 바꿔가며 확인하는 일은 **사람 손에서 AI 쪽으로 넘어온다** — 심슨 검사·세그먼트 분해를 AI가 미리 돌리고 결과만 싣는다. 이로써 **D25(모듈 토글)·D26(조작 규약)은 전제가 사라져 폐기**되고, `report_ui.py`·템플릿 2종·두 생성기의 렌더 층(약 6,300줄)이 함께 폐기된다. 계산 층(매칭·합집합·급등락 판정·diff·옵션 집계)은 이식해 존치. **엔진은 reportlab**(`pdf_doc.py`) — 한글 CID 폰트 내장이라 폰트 번들 불요, 결정적, 크로스 플랫폼. 대가로 `pip install reportlab` 하나가 생겨 README의 "설치할 패키지 없음"이 깨진다. ※ 폰트에 없는 글자는 **빈칸이 아니라 엉뚱한 한글 음절로 그려진다**(실측: `·` `⚠` `✅` `❌` `₩` `€` `¥`) — `esc()`가 일괄 치환한다 | **Chrome headless `--print-to-pdf`**(1순위였으나 실측 기각 — 이 환경 Chrome 150에서 `--dump-dom`·`--screenshot`은 정상인데 인쇄 경로만 무한 대기. 절대경로·플래그 최소화·샌드박스 해제 전부 실패. CDP 웹소켓 직접 구현은 일정 위험) / HTML 유지 후 사용자가 인쇄 / Markdown→pandoc(바이너리 설치 부담·표현력 제약) |
| D28 | 리포트 2층 구조 | **인사이트 PDF(짧다·매일 본다) → 상세 PDF(근거 전부)** 로 나누고, 인사이트의 각 항목이 상세의 해당 위치를 가리킨다(2026-08-03 사용자 지시). 인사이트는 **강한 주장 5 + 약한 단서 20**(사용자 확정 수치). 강한 주장은 ①효과 크기 임계 ②n 임계 ③다중비교 보정 생존 ④세그먼트 분해에서 역전 없음 ⑤홀드아웃 재현을 **전부** 통과한 것만이고, **5개가 안 나오면 억지로 채우지 않는다**(빈자리를 약한 것으로 승격시키면 구분이 무의미해진다). 약한 단서는 어느 조건을 미통과했는지와 재확인 방법을 병기. 각 항목에 `audience`(판매전략/디자인/마케팅) 필수 — MD 요구 "한 번 더 가공해서 판매전략/디자인/마케팅 측에서 활용할 수 있게". **참고용·정성적**이므로 강한 주장도 실행 지시가 아니라 관측 진술로 쓴다 | 단일 PDF(모바일에서 길다) / 인사이트만(근거 확인 불가) / PDF 간 앵커 점프(뷰어별 지원 제각각 → 쪽 번호 병기로 대체) |
| D29 | 파이프라인 5단 분리 | `collect → store → explore(EDA) → analyze → insight` 로 쪼개고 **각 단은 앞 단의 산출물 형식만 알고 뒷 단을 모른다**(2026-08-03 피드백 2 — 팀원마다 원하는 게 다르다: 수집만 / 분석 리포트 / 인사이트만). 지금 platform-* 스킬에만 적용된 규율(계약 JSON만 안다)을 전체로 확장하는 것이다. `commerce-intel`은 **오케스트레이터로 존치** — 5단을 엮고 재사용을 판정하고 요청을 라우팅한다. **피드백의 4단이 아니라 5단인 이유**: EDA는 SLA(10분)와 산출 성격(사람용 리포트가 아니라 인사이트 엔진의 입력)이 분석 단과 달라, 붙여두면 10분을 지킬 수 없다. 기능별 쿼리 SLA — 신규 수집 밤새 OK / 단순 조회 초 단위 / EDA 10분 / 인사이트 30~60분 | 4단(피드백 원안) / 단일 스킬 유지 / 역할별 별도 스킬 세트(중복 폭발) |
| D30 | **신규 플랫폼 수집 신분** | W컨셉·EQL·SSF **셋 다 robots.txt가 `ClaudeBot`·`Claude-User`를 명시 허용**하고 있었다(2026-08-03 실측). 기존 "일반 UA 전면 차단" 판정은 **`User-agent: *` 블록만 읽은 오독**이다 — 화이트리스트 섹션을 못 봤다. 수집 방식은 **ⓐ 사용자 지시 건별 수집만**(사용자 확정) — 크론 무인 축적은 `*: Disallow: /` 적용 대상으로 보고 하지 않는다. 랭킹 축적은 무신사·29CM로 충분하다. **W컨셉만 예외** — robots는 허용하는데 서버가 403을 준다(정책과 집행 불일치). 브라우저 UA 헤더 3종으로 200이 되는 것을 실측했고 사용자가 진행을 결정했다. **선은 UA·Accept 헤더까지** — 캡차 우회·IP 로테이션·TLS 지문 위조는 범위 밖(차단 무력화는 성격이 다르다). 실측 결과: EQL·SSF는 필수 10필드 전부 획득 가능, W컨셉은 `sold_out`만 구조적 불가(PLP가 품절을 목록에 싣지 않음 — `status` 파라미터 4종 실측 총계 동일) → **PLP 단독 수집 시 `sold_out: null` + notes 명기**. `false`로 채우면 품절이 존재하지 않는 데이터가 들어간다(결측이 아니라 오염) | UA 위장 전면 금지(기존 — 오독에 근거) / 크론 축적까지 허용 / 세 곳 모두 포기 |
| D31 | 배포 형태 | **전원 풀 배포 + 공유 DB 없음**(2026-08-03 사용자 확정). 각자 로컬 SQLite를 갖는다. 알려진 대가: 중복 수집·축적 분산·사이트 부하 증폭. 완화 3종을 넣는다 — ① 요청 간격·일일 상한을 스킬에 고정 ② `intel_db.py merge`로 나중에 각자 DB를 합칠 수 있게(공유로 갈 때의 마이그레이션 경로) ③ **랭킹 축적은 한 명만 돌린다**를 README에 명시(시계열이 갈리면 시계열 분석 자체가 무의미해진다) | 수집은 1인·팀원은 읽기 전용(오늘 배포에 가장 안전했으나 기각) / 공유 DB 서버(SQLite 동시 쓰기 취약, 일정 밖) |
| D32 | **팀 커버리지 조회 — 수집 전에 시트 `runs`를 본다** | D31의 대가 중 **중복 수집**을 실제로 막는 장치(2026-08-03 사용자 지시 — "이미 수집된 걸 수집하려 하면 경고"·"랭킹뿐 아니라 다른 수집도 맨 처음 시트를 참조"). 새 대장을 만들지 않고 **이미 미러되고 있는 `runs` 탭을 읽는다** — `run_id·site·story·target·collected_at`이 곧 커버리지 대장이고, 1,206행이라 조회가 가볍다(관측 5만 행을 읽는 게 아니다). `runs.story`→`observations.context` 매핑은 `context_of`와 같은 규칙을 `run_context`로 공유한다(실측: 실제 시트 1,206행이 로컬 context 24/24 완전 일치). 판정은 **기존 재사용 규칙을 그대로 적용**한다(§2-2) — 무조건 경고가 아니다. 랭킹은 30분마다 바뀌므로 재수집이 정당하고, 갱신 주기 밖이면 이력만 알리고 통과시킨다. **내가 올린 `run_id`는 제외**한다(로컬 판정이 이미 본 것이라, 남기면 "팀원이 했다"가 거짓이 된다). 부수 효과로 D31 완화책 ③(랭킹 축적 1인)이 README 문구가 아니라 실제로 강제된다. **한계 두 가지를 명시한다**: ⓐ 시트는 *완료된* 수집만 안다 — `runs` 행은 적재 후 생기고 그 다음 미러 때 올라가므로 "지금 남이 수집 중"은 못 잡는다(중복 창 = 미러 주기) ⓑ 조회 실패는 수집을 막지 않는다 — `consulted=false`로 떨어뜨리고 로컬 판정으로 진행한다 | **시작 시점 클레임 탭**(진행중까지 잡아 중복 창이 몇 분으로 줄지만, 팀원이 시트에 쓰게 되어 §3 단방향 미러 원칙이 깨지고 비정상 종료 시 유령 행 처리가 필요 — 실제 충돌 빈도를 보고 재검토) / 커버리지 전용 탭 신설(`runs`가 이미 그것이다) / 조회 실패 시 수집 차단(시트 미설정만으로 전원이 멈춘다) / 중복이면 차단(랭킹 축적이 불가능해진다) |
| D33 | **분석 단 신설 — 방법론을 정하는 자리** (D29 개정) | 사용자 지적으로 드러난 구멍이다(2026-08-03): **EDA가 `signals[]`를 만드는데 인사이트 엔진이 한 번도 읽지 않았다**(grep 확인 — 참조 0줄). 가설 생성 함수 셋이 조건 없이 전부 돌았고 "이 데이터엔 이 기법이 맞고 저건 안 맞는다"는 판단이 코드에도 산출물에도 없었다 — 피드백 3번("EDA 결과를 네가 판단 후 가설 검정으로 들어가")의 **판단 후**가 통째로 빠져 있었다. ※ D29에서 "analyze 단이 비었다(HTML 폐기로 사라졌다)"고 적었던 것은 **틀렸다** — 비어 있던 게 아니라 안 만들어져 있었다. **구조**: ①규칙표 자동 판정(기법 6종, 채택·기각을 사유·n과 함께) ②AI 예외층(`--ai-notes`로 기각·주의·추가 — 규칙은 통과했으나 이 데이터엔 부적절한 것을 걸러낸다. 2단계 실행: `--plan-only`로 계획을 보고 판단) ③**분석 계획서**를 상세 PDF에 싣는다(기각된 것도 — *안 한 것*과 *못 한 것*은 다르다). **기법 4종 신설**(MD 질문 직결): 소진 속도(Kaplan-Meier, 미품절은 중도절단 — 빼면 "빨리 팔린 것"만 남아 편향된다) · 용량-반응(할인 폭 10%p 구간별 — 상관계수로는 "얼마나 주면 되나"에 답할 수 없다) · 쌍체 비교(같은 상품 짝지어 부호 뒤집기 순열 — 독립 두 집단 비교는 상품 구성 차이가 섞인다. 실측 335쌍) · 이중차분(구 event_study는 인하군·인상군의 증분 *수준* 비교라 전후 비교일 뿐이었다). **스킬은 4단으로 분리**하고 오케스트레이터가 라우팅한다 — **단계 스킬은 사용자 발화 예시를 갖지 않는다**(여러 스킬이 같은 말에 반응하면 재사용 판정·팀 커버리지를 하는 오케스트레이터가 조용히 우회된다). **임계 완화**(사용자 지시 "표본이 항상 많지 않다"): 숫자를 낮추기 전에 구조적 병목 둘을 고쳤다 — ⓐ 홀드아웃 "확인 불가"를 "실패"로 처리하던 것("재현 안 됨"과 "확인 못 함"은 다른 말이다. 자격은 주되 선별에서 뒤로 민다) ⓑ **순열 p 하한이 다중비교 임계보다 높았다**(2,000회면 하한 0.0005인데 가설 107개에 α=0.05면 임계 0.00047 — 구조적으로 아무도 통과 못 한다. 반복 4,000으로 해결). 숫자는 최소만: N_MIN 30→20(순열은 대표본 근사를 안 쓴다)·EFFECT_MIN 0.33→0.30·CORR_MIN 0.30→0.25 | **α 완화**(0.05→0.10 실험했으나 결과가 거의 안 바뀌어 기각 — 79→79·51→53·120→122. 진짜 병목이 아니었다) / 효과 크기를 더 낮추기('쓸모'의 척도라 낮추면 리포트가 무의미해진다) / analyze를 insight에 그대로 두기(계획서를 남길 자리가 없다) / 단계 스킬에도 발화 예시 주기(오케스트레이터 우회) |
| D34 | **스타일 : 변형 2계층 — 분석 단위를 명시한다** | 사용자 지적 #7에서 출발했으나 실측으로 **문제가 둘로 갈렸다**(2026-08-03). ⓐ **시즌 오병합(버그)** — 괄호 제거 규칙이 시즌 표기까지 지워 `(FW23) T-Logo LT Hoodie`와 `(FW24) …`가 같은 상품이 됐다. `match_key`가 시즌 표기(FW/SS/SP/AW + 숫자)를 키에 남기도록 고쳤다. **FW/SS 문자를 필수로 요구한다** — 숫자만 보면 상품코드 `[0157]`을 시즌으로 오인한다(실측에서 실제로 걸렸다). 효과는 공짜다: 플랫폼 간 매칭 688→688(손실 0) · 사이트 내 잘못된 병합 40건 감소. ⓑ **집계 단위 편향(설계)** — 플랫폼마다 한 상품을 세는 기준이 다르다. 무신사는 `REYA LACE TOP (5 COLORS)` 한 줄, 자사몰은 색상마다 한 줄. 실측 접힘률이 **자사몰 13.6% 대 29CM 0.0%**(격차 13.6%p), 데님팬츠에서 **무신사 15.1% 대 bymono 0.3%**(14.9%p)다. 그대로 세면 ①카탈로그 폭 비교가 왜곡되고(자사몰 734 대 29CM 608로 보이지만 스타일로는 634 대 608) ②같은 스타일의 색상 3개를 독립 관측 3건으로 세어 **n이 부풀고 p값이 실제보다 작게 나온다**(유사 반복). **해결**: `style_rows()`로 (site, style_key) 단위로 접되 수치는 **중앙값**을 쓴다 — 첫 건은 어느 색이 먼저 수집됐느냐에 따라 달라져 재현되지 않고, 평균은 한 색만 세일 중일 때 끌려간다. `variant_n`·`variant_price_spread`를 남겨 접으면서 버린 정보량을 밝힌다(실측: 묶인 그룹 2,193개 중 **843개는 변형끼리 가격이 다르다**). **단위 배정**: 그룹 비교·상관·쌍체 비교는 **스타일**, 품절·재고·소진 속도는 **상품(변형)** — 색상마다 다른 값이라 접으면 안 된다. 스타일의 `sold_out`은 **전 변형이 품절일 때만** 품절이다. 리포트 정직성 배너에 어느 단위로 셌는지와 접은 건수를 싣는다 | 변형 단위 유지(플랫폼 비교가 서로 다른 기준으로 세어진다) / 대표를 첫 건으로(재현 안 됨) / 대표를 평균으로(세일 중인 색에 끌린다) / 모든 분석을 스타일로(품절·재고는 색상마다 다르다) / 시즌도 계속 제거(다른 상품이 합쳐진다) |
| D35 | **동적 속성 표 — 축을 행으로 뺀다** | `products.attributes`(JSON)가 핏 하나만 담고 있어 컬러·소재·넥라인·시즌을 더할 때마다 컬럼이 필요했다(2026-08-03). 축을 행으로 뺀 `product_attributes(site, product_id, attr_name, value, basis, decided_at, ttl_days)`를 만든다 — **proxy_cache와 같은 모양**이라 AI 프록시 판정(D19) 결과도 이 표로 그대로 들어온다. **속성별 TTL**: 컬러는 안 변하니 크게, 시즌 태그는 짧게(핏은 D7과 같은 90일 기본). **판정 실패(`null`)는 저장하지 않는다** — `{"핏": null}`은 재판정 대상이지 저장 대상이 아니고, 저장하면 TTL 안에 재시도가 막힌다. 기존 JSON은 지우지 않는다(폐기 예정 리포트가 아직 읽어 하위호환). connect()에서 1회 멱등 이관(실측: 핏 12,814건 이관, null 12,031건 제외). `collect()`는 표를 먼저 보고 JSON을 폴백으로 병합한다(표가 더 최신이라 덮는다). CLI: `set-attrs`(모든 속성이 이 한 통로로)·`attr-stats`. **컬러 축은 인프라만 넣고 채우는 것은 8번(보류)** — 기존 핏이 이미 소비자라 죽은 코드가 아니다. ※ **보존 정책·lifecycle 태깅은 함께 안 했다** — 관측이 6일치(5만 행)뿐이라 롤업이 죽은 코드고, 자사 상품이 DB에 6건뿐이라 태깅 대상이 없다. 자사몰 수집·축적이 선행돼야 의미가 생긴다 | JSON에 컬럼 계속 추가(축마다 스키마 변경) / observations처럼 관측 표로(속성은 시변이 아니다) / null도 저장(재판정 차단) |
| D36 | **EQL·SSF 플랫폼 스킬 (draft)** | D30 실측(2026-08-03)을 근거로 `platform-eql`·`platform-ssf`를 작성. robots가 Claude 봇을 명시 허용하지만 **수집은 건별만**(D30)이라는 제약을 스킬 맨 앞에 박았다. `platforms` 테이블에 `skill_status='draft'`로 적재 — **`ready` 승격은 사용자 몫**이고, 실사용 전 브랜드 모드·검색·옵션 재고 등 미검증분(어댑터에 명시)을 채워야 한다. EQL은 CSR 껍데기+HTML 조각 응답(WMONID 쿠키 필요·totalRow가 정본 총계), SSF는 이 프로젝트가 다룬 것 중 가장 수월(쿠키·referer 불필요, 필수 10필드 결측 0%, 품절이 정렬 꼬리로 밀리는 함정) | ready로 바로 승격(미검증분이 남았다) / 스킬 없이 generic으로(전용 어댑터가 더 정확) |
| D37 | **후기 수집은 별도 스킬로 분리 (미착수)** | 무신사·29CM 후기 API 정찰은 실측 완료(2026-08-03 — 둘 다 비로그인 공개 JSON, robots 허용, 상품별 총계 노출, "부정·베스트·사진" 1차 압축은 사이트 정렬/필터로 대체 가능). 그러나 **후기는 데이터 계약(상품 단위)과 성격이 다르고 개인정보 필드**(닉네임·키·몸무게·프로필)**가 섞여 있어** 상품 파이프라인에 섞지 않고 별도 스킬로 뺀다(2026-08-03 사용자 지시). 계약 개정·구현은 그 스킬에서. 실측은 아카이브로 보존 — 무신사 `goods.musinsa.com/api2/review/v1/view/list`(sort=goods_est_asc 평점낮은순·hasPhoto), 29CM `review-api.29cm.co.kr/api/v4/reviews`(sort=BEST·/photo 전용). ※ 29CM 어댑터 §5 "정렬 파라미터 없음"은 이번 실측(sort=BEST 동작)과 어긋나 개정 대상 | 상품 계약에 reviews[] 추가(개인정보·성격 혼입) / 지금 구현(스킬 경계 먼저 정해야) |
| D38 | **무신사 글로벌 플랫폼 스킬 (draft)** | 사용자 지시로 무신사 글로벌을 **기본 플랫폼으로 스킬화**(2026-08-03 정찰 실측). 국내 `platform-musinsa`와 **별개 스킬** `platform-musinsa-global` — 오케스트레이터가 "무신사 글로벌/musinsa global" 요청을 별도 진입점으로 라우팅해야 하고, 국내와 도메인·robots·통화·가격 체계·평점 스케일이 다르기 때문이다. **실측 요지**: ⓐ 도메인은 `global.musinsa.com`, URL은 지역 접두 `/{region}/...`(region 13종, KR 없음) — `us.musinsa.com`은 DNS 없음, `www.musinsa.com/global`은 404. ⓑ robots는 Claude 봇을 명시 허용하되 **`/api/`만 제외**(국내는 `/api/` 차단이 없다) → **XHR 대신 서버 렌더 HTML 임베드 JSON(Next.js RSC)만** 쓴다. `*: Disallow: /` 구조가 W컨셉·EQL·SSF와 같아 **D30 적용 — 건별 수집만, 크론 축적은 사용자 명시 승인 전까지 보류**("기본 플랫폼"이 축적 허가는 아니다 — EQL·SSF도 건별-only다). ⓒ 카테고리 코드·`goodsNo`(product_id)는 **국내와 같은 체계** → 국내 수집분과 ID 직접 교차 매칭 가능(글로벌은 `globalYn:Y` 부분집합). 목록은 `/{region}/category/{code}?page=N`(hmac 체인 아닌 단순 page), `totalCount` 임베드. ⓓ **필수 10필드 전부 획득 가능**(sold_out은 `status`로 신호는 되나 품절값·포함 파라미터 「미검증」). ⓔ **통화가 외화·region별**(US=USD·JP=JPY, 정수지만 KRW 아님) → db-contract(정수 KRW 전제)에 **문제 제기**(계약 개정은 SPEC 소관 — adapter.md §4-1에 기록, `currencyCode` 보존·환율 없인 국내 교차비교 불가). ⓕ 국내 대비 차이: 평점이 이미 0~5(÷20 금지)·`like_count`가 PLP 직접 노출·**쿠폰가(finalPrice) 없음**(국내 함정5 해당 없음). `skill_status='draft'` — ready 승격·미검증분(품절·브랜드·검색·랭킹·옵션·크론 승인) 채우기는 사용자/후속 실측 | 국내 어댑터에 §글로벌 절만 추가(오케스트레이터 라우팅이 안 되고 통화·품절 차이가 묻힌다) / ready 즉시 승격(미검증분 남음) / 크론 축적 바로 허용(robots `*: Disallow: /`) / generic으로(전용 어댑터가 정확) |
| D39 | **AI 프록시가 검정 축이 되도록 (D19·D23 실효화)** | 사용자 지적으로 드러난 누락이다(2026-08-03): AI가 자동 정의·판정한 파생 프록시(name_lang·thumb_cut·logo_presence, 3,178·220·120건 판정돼 있음)가 **인사이트 리포트에서 검정되지 않았다.** 원인 — HTML→PDF 전환(D27) 때 `collect()`의 프록시 주입은 `intel_data.py`로 가져왔는데(각 상품에 `px_{name}` + `meta.proxies`), 새 EDA·분석 단(`eda.py`·`analyze.py`)이 **고정 `AXES`/`CAT_AXES`만 순회**해서 프록시가 통째로 빠졌다(grep: 프록시 참조 0줄). D19·D23이 프록시를 정의·판정·주입까지 했는데 마지막 소비 지점이 끊겨 있던 것. **수정**: `intel_data`에 `num_axes(data)`(고정+numeric 프록시)·`cat_axes(data, base)`(base+범주 프록시) 헬퍼 신설. EDA의 결측·분포·카디널리티·상관·세그먼트와 분석의 그룹 비교·소진 속도가 이 동적 축을 쓴다. 프록시 대부분이 범주형이라 **그룹 비교 축**이 되어 "영문 상품명이 한글보다 하트가 높은가(879 대 421)" 같은 검정이 성립한다(실측: 모자 랭킹에서 name_lang 가설이 리포트에 실림). 프록시 청중은 기본 디자인(상품 외형·특성 판정). 프록시 값 이름이 한글이라 조사 결합(`_josa`, 은/는·이/가)을 붙여 문장을 자연스럽게. 프록시 없는 문맥은 축에 안 붙어 하위호환. ※ 프록시가 강한 주장에 오르는지는 효과 크기가 정한다 — 억지 가중치를 주지 않는다(모자에선 카테고리 비교가 더 커서 프록시는 약한 단서에. 정직한 순위다) **✅ 갭 해소(2026-08-03 후속)**: 이건 *등록된* 프록시를 검정에 쓰는 것까지였고, **분석 요청 때 새 프록시를 AI가 자동 정의·판정(D23)하는 워크플로우**는 그때 새 4단 스킬에 연결되지 않은 채였다. proxy-extraction.md에 절차는 완비돼 있었고 **참조만 끊겨 있었다** — intel-insight SKILL.md에 §0을 신설해 연결했다(방법론 결정보다 먼저 온다: 축이 있어야 검정 대상이 된다). 흐름은 기존 proxy_defs 재사용 → 정의 카드 → 캐시 우선 판정(미캐시분만 proxy-extractor 배치) → proxy-load → **정의 카드 보고**(선택지로 묻지 않는다). 오케스트레이터 6단계에도 진입점을 뒀다. **건너뛸 조건도 명시**했다 — 기존 축으로 답해지면 만들지 않는다(축이 늘수록 다중비교 위험이 커진다). | 프록시를 리오더 축으로 가중(억지 승격) / 프록시 전용 리포트 섹션(다른 가설과 같은 관문을 통과해야 공정) / 고정 축 유지(D19·D23이 무의미해진다) |
| D40 | **상세 PDF 차트 — reportlab Drawing 직접 렌더** | 상세 리포트가 표와 수치로만 나가던 것에 그림을 더한다(2026-08-03 사용자 요청). **표는 그대로 둔다** — 표가 정확한 값이고 그림은 형태다. **외부 차트 라이브러리를 쓰지 않는다**: D12-d가 Chart.js CDN을 "외부 리소스 0 원칙과 충돌"로 기각한 것의 연장선이고, 배포 의존은 reportlab 하나로 족하다(D31 전원 배포). reportlab에 차트 모듈(`reportlab.graphics.charts`)이 있지만 우리 시각 규칙(0 시작·색약 안전·순위 축 반전·직접 라벨)을 맞추려면 결국 저수준 `Drawing`으로 내려와야 해서, 처음부터 필요한 형태만 그리는 쪽이 코드가 짧다. **4종**: `bar_h`(범주 비교, 0 시작, 음수는 반대 방향+연한 톤) · `dist_compare`(그룹 분포 **사분위 박스** — 평균 막대 두 개로는 겹침이 안 보인다. 중앙값이 갈려도 분포가 크게 겹치면 차이가 약하다는 사실이 그림에 나와야 정직하다) · `bins_bar`(구간별·용량반응, **구간마다 n 병기**) · `missing_bar`(EDA 결측 — **0 건수와 한 막대에 쌓지 않는다**, 미노출과 0은 다른 값이다). **색이 아니라 명도로 구분한다**(적록색약 8%·흑백 인쇄). 시계열(순위 추이)은 축적이 더 쌓이면 붙인다 — 그때 순위 축 반전·순위권 밖 구간 끊기를 지킨다. **렌더 실측으로 결함 3건**을 잡았다(축 라벨 겹침·음수 값 라벨 잘림·결측 차트 미표시 오판) — 코드 통과로는 안 잡히는 종류라 실제로 그려 눈으로 확인했고, 그 교훈으로 단위 테스트를 붙였다 | 외부 차트 라이브러리(D12-d 연장선 위반·배포 의존 증가) / reportlab 차트 모듈(시각 규칙을 못 맞춰 결국 저수준으로 내려온다) / 표만 유지(형태가 안 보인다) / 그림만(정확한 값이 사라진다) |
| D41 | **W컨셉·EQL·SSF·무신사 글로벌 랭킹 축적 허용** (D30 개정) | 사용자 명시 승인(2026-08-04). D30이 이 4개를 "사용자 지시 건별 수집만, 크론 무인 축적은 사용자 명시 승인 전까지 보류"로 뒀는데 **그 승인이 나왔다.** robots는 그대로다(`*: Disallow: /` + Claude 봇 허용) — 바뀐 것은 우리 판단이다. **갱신 주기 실측**(사이트가 직접 밝힌 값 우선, 시차 관측은 보조): **SSF는 매 1시간** — `/ranking` 페이지 「랭킹 노출 기준」에 "순위는 매 시간마다 갱신됩니다"라고 **사이트가 명시**해 확정. **무신사 글로벌은 brands 랭킹에만 `updatedAt` 실값**이 있고(items는 전 케이스 null) HK·JP가 같은 값이라 전 region 동시 배치로 보이나 1회 관측이라 「미검증」. **EQL·W컨셉은 사이트가 밝힌 값이 없어** 날짜를 넘긴 재관측이 필요하다 — 크론은 잠정 1시간(촘촘한 쪽이 안전하다: 관측 시각이 키라 중복 저장될 뿐 데이터가 상하지 않는다). **기간별 랭킹(1일·1주·1달)을 축적 없이 얻을 수 있는가** — 사용자 질의로 전 사이트 조사: **EQL은 된다** (`sort=DAILY_SALE_SEQ|WEEKLY_SALE_SEQ|MONTHLY_SALE_SEQ`, 셋이 서로 다른 1위·md5로 대조 확인. 3·6개월은 없음). 무신사(`period=`)·29CM(`periodFacetInput.type`, ~MONTHS_6)는 기존 확보. **SSF·W컨셉·무신사 글로벌은 없다** — 후보 파라미터를 넣어도 결과가 안 바뀐다(무신사 글로벌은 `period: null`이 번들에 **선언돼 있는데도** 서버가 무시). **⚠️ EQL은 미지원 정렬값도 200 + 정상 본문을 준다** — 가짜 값 `ZZZ_BOGUS_SEQ`도 120개가 오므로 "200이니 동작"으로 판정하면 틀린다. 새 값은 반드시 결과 대조로 검증한다 | 4사이트 건별 유지(사용자 승인으로 해소) / 주기 미상인 채 크론 등록 보류(잠정값+「미검증」 표기가 낫다 — 축적을 못 하면 시계열 자체가 없다) / 기간별 랭킹을 전 사이트에서 지원한다고 가정(SSF·W컨셉·무신사 글로벌은 실측으로 없음이 확인됐다) |
| D42 | **카테고리 계층 판정 — 대등하지 않은 쌍은 검정하지 않는다** | 사용자가 인사이트 PDF에서 "「미니」와 「하의」는 분류 레벨이 안 맞잖아"를 지적해 드러났다(2026-08-04). 29CM는 **깊이가 다른 카테고리를 한 필드에 섞어** 준다 — 롱(979)·미디(472)·미니(305)는 스커트 하위, 하의(87)는 `여성의류 > 단독` 아래, 여성의류(1)는 최상위, `여성의류 > 스커트 > 미디`(5)는 전체 경로 표기다. 그대로 두면 "미니가 하의보다 할인율이 높다"가 **강한 주장 5개 중 3개**를 차지했다. 무의미하다 — 부분과 전체를 겨루게 하는 문장이다. **근거는 실측 카탈로그**(`data/.tools/ranking_targets.json`, 1,546개): 사이트가 직접 밝힌 트리라 추정이 없다. DB의 경로 형태 값 3,468건도 함께 먹인다. 거르는 조건 둘 — ① **조상-자손**(스커트 ⊃ 미니) ② **부모가 갈린다**(미니[부모=스커트] 대 하의[부모=단독·홈웨어…]). **모르면 거르지 않는다** — 트리에 없는 값은 판단 근거가 없고, 없는 근거로 검정을 지우면 "안 본 것"이 "없는 것"이 된다. 대신 서두 경고가 그 몫을 진다. 실측: 스커트 가설 107→66개(쌍 24개 제외), 카테고리 계층 착시가 강한 주장에서 전부 사라졌다. **경고를 인사이트 PDF에도 올렸다** — 전에는 상세 PDF 각주에만 있어 카드만 보는 사람은 못 읽었다 | **집합 포함으로 판정**(실측 기각: 상품마다 카테고리가 하나뿐이라 하의와 미니의 상품 집합이 안 겹친다 — 데이터상 무관한 형제로 보인다) / **경로 값만으로 학습**(실측 기각: 하의와 미니가 한 경로에 같이 등장한 적이 없어 107→90에 그쳤고 정작 지적된 쌍이 살아남았다) / 카테고리 트리 API 재수집(정확하지만 오프라인 분석에서 못 쓴다) / 경고만 띄우고 그대로 검정(강한 주장 자리를 계속 먹는다) |
| D43 | **프록시 기본 생성 — 묻지 않아도 뽑을 수 있는 축은 다 만든다** | 사용자 결정(2026-08-04): "사용자가 묻지 않아도 가능한 인사이트를 모두 뽑는 게 목적." D19·D23이 프록시를 정의하고 D39가 검정 축에 연결했는데도 **실제로는 거의 안 만들어졌다** — 스커트 리포트 판정 0건. 원인은 정책이 아니라 마찰 둘이었다: ① 옛 §0이 "기존 축으로 답해지면 만들지 않는다"라 **기본값이 안 만드는 쪽** ② vision 배치를 손으로 조립해야 해서 매번 거르게 됐다. **기본값을 뒤집는다** — 리포트를 만들면 프록시가 여러 개 자동으로 생긴다. 실행기 `proxy_auto.py` 신설: 한 번 돌리면 **rule은 즉석 판정(비용 0)하고 vision은 배치 파일을 뽑는다**. 카드는 선언적 정규식이라 `eval`을 쓰지 않는다. 실측(스커트 1,911건): rule 6축이 전부 100% 커버리지로 몇 초에 나왔다. **vision 배치는 재료가 같은 카드를 한 배치에 묶는다** — 카드마다 만들면 같은 사진을 축 수만큼 다시 봐서 배치 432개가 나왔고, 묶으니 48개가 됐다(이미지 하나에 질문 9개). 캐시된 것과 중복 이미지는 이미 빠져 있다. **커버리지가 낮거나 값이 1종뿐인 카드는 자동으로 버리고 버렸다는 사실을 찍는다**(조용히 빠지면 "신호가 없었다"로 읽힌다). 카드 묶음(`references/proxy-cards-default.json`)은 **정본이 아니라 출발점**이다 — D19의 "하드코딩하지 않는다"는 그대로고, 어떤 축을 만들지는 AI가 요청·상품군을 보고 정한다. 다중비교 위험은 **축을 적게 만들어서가 아니라 BH 보정과 5관문으로** 막는다 | rule만 자동화(**이미지에서만 나오는 축이 통째로 빠진다** — 사용자 지적으로 기각) / 카드 목록을 코드에 박기(D19 위반) / 옛 "필요할 때만 만든다" 유지(실측으로 0건이 나왔다) / 카드마다 vision 배치 분리(같은 사진을 축 수만큼 다시 본다) |
| D44 | **인사이트 표현 — 청중 배지·마크다운·표시 개수** | 사용자 요청 3건을 한 묶음으로(2026-08-04). ① **청중 배지** — `[판매전략]`이 본문과 같은 색·크기라 눈에 안 걸렸다. 청중은 "누가 이 문장을 읽어야 하는가"를 정하는 값이라 첫눈에 갈려야 해서 색을 확실히 다르게 준다(판매전략 파랑·마케팅 주황·디자인 초록). **색약 규칙은 지킨다** — 배지 안에 글자가 그대로라 정보가 색에만 실리지 않는다. 다만 **흑백 인쇄 제약은 배지에서 뺐다**: 사용자 질문("왜 굳이 흑백이어야돼?")에 출처를 확인해 보니 색약 규칙(D12-d 차용)에 **내가 얹은 것**이었고 사용자 결정도 실측도 없었다 — 이 PDF를 인쇄한다는 근거가 없다. 차트는 값 크기를 색으로 나르므로 명도 규칙을 그대로 둔다 ② **마크다운 인라인** — `md()`가 `**굵게**`만 알아서 `*기울임*`과 인라인 코드가 별표·백틱째 인쇄됐다. 셋 다 처리한다(굵게를 먼저 치환해야 바깥 별표 한 쌍이 기울임으로 오인되지 않는다) ③ **강한 주장 5→10** — `STRONG_MAX`는 **판정 기준이 아니라 표시 개수**다. 스커트 실측에서 자격 통과 14개 중 5개만 실려 9개가 기각이 아니라 잘려 있었다. 관문은 그대로라 느슨해지지 않는다. `AUDIENCE_CAP`도 2→4로 함께 올렸다 — 1차 통과 최대치가 청중 3종 × CAP이라 2로 두면 7~10번째 자리를 **청중 균형을 안 보는 2차 채움**이 가져간다. 빈자리는 약한 단서로 메우지 않는다 | 배지를 옅은 톤으로(흑백 제약을 유지할 근거가 없었다) / 색만 다르게 하고 라벨 삭제(그때 진짜 색약 위반이 된다) / 표시 개수를 늘리며 관문 완화(전혀 다른 일이다) / AUDIENCE_CAP 유지(한 청중이 절반을 먹는다) |
| D42-a | **계층 필터를 우산/리프로 좁힘** (D42 개정) | PR #8 리뷰 지적(2026-08-04): D42의 ②가 "부모가 갈리면 제외"라 **순수 형제 리프끼리도 걸렀다** — `미니`(부모 스커트) 대 `후드`(부모 아우터)가 막혀 "스커트 계열이 아우터 계열보다 할인율이 높다" 같은 정상 비교까지 사라진다. 실측으로 재현했다. **가르는 신호는 「우산」 여부**다: 트리 어딘가에서 자식을 거느린 값(`하의`는 `남성의류 > 하의 > 데님 팬츠`의 부모, `스커트`는 미니·미디의 부모)과 어디서도 부모가 아닌 리프 전용 값(`미니`·`후드`·`데님`)을 나눈다. **한쪽만 우산이면 굵기가 다르니 제외**(미니 대 하의 — 원래 잡으려던 케이스), 둘 다 우산이거나 둘 다 리프면 대등하니 통과. 사유 이름도 `branch` → `granularity`. 스커트 실측은 그대로 4쌍 제외(전부 우산 `하의` 관련). **남는 한계**: 우산끼리 깊이가 달라도 통과한다(`여성의류` 대 `하의`) — 한 값이 트리마다 다른 깊이에 있어 깊이로는 못 가른다. TEST-CASES E-CH-13에 한계로 적었고 서두 경고가 그 몫을 진다 | 부모 갈림 기준 유지(정상 비교를 지운다) / 깊이로 판정(한 값이 트리마다 깊이가 달라 성립하지 않는다) / 아예 안 거름(원래 문제로 돌아간다) |
| D45 | **DB 스키마 v2 — 반복 텍스트를 정수로 접는다** | 사용자 요청(2026-08-04 "볼륨 최적화"). **지금 31MB는 문제가 아니다 — 행당 비용과 증가 속도가 문제다.** 실측: `observations` 1행 266바이트(테이블 5.36MB + 자동인덱스 4.36MB + `idx_obs_context` 3.27MB), 하루 2,400~3,400행. D41로 크론이 4사이트 늘어 시간당 6사이트면 연 500만 행 = **1.3GB**. 그 266바이트의 대부분이 같은 문자열의 반복이었다 — `context` 고유 **16개**를 51,034행에(1.11MB) · `site` 고유 12개(0.34MB) · `run_id` 221개(0.58MB) · `observed_at` TEXT 19바이트(0.92MB), 게다가 PK가 텍스트 4개라 자동인덱스만 4.36MB. `products`도 `image_url` 2.90MB 중 91%가 CDN 호스트 2곳의 반복이다. **바꾼 것**: ① 사전 테이블(site·context·brand·category·URL 호스트) ② 상품을 정수 대리키 `pk`로 참조(20바이트 → 2~3바이트) ③ 시각은 unix epoch INTEGER ④ 인덱스도 정수 위에. **실측 결과**: 31.01MB → 15.92MB, **관측 1행 266 → 81바이트(69.6% 감소)**, 연 1,273MB → 387MB. **읽는 코드는 한 줄도 안 고쳤다**(사용자 결정) — 옛 이름·옛 컬럼 그대로의 뷰를 얹었고, 회귀 129건이 그대로 통과하는 것이 마이그레이션 검증이 된다. 쓰기는 뷰에 `INSTEAD OF` 트리거를 걸어 해결했다. **업서트만 못 넘긴다**(SQLite: "cannot UPSERT a view") — `ON CONFLICT` 절을 호출부에서 빼고 덮어쓰기 규칙을 트리거 안으로 옮겼다. 중복 관측은 여전히 `IntegrityError`를 낸다(적재 코드가 "중복 N건"을 그걸로 센다). 증분 키는 뷰가 `_rowid`(맨 뒤 컬럼)로 내준다 — 뷰에는 rowid가 없다. **이관은 새 파일에 짓고 검산 통과 후 사람이 바꿔 끼운다**(`migrate_v2.py`) — 제자리 변환은 중간에 죽으면 정본이 반쯤 망가진다. 검산은 행 수만이 아니라 **뷰로 되읽어 전 컬럼 값을 대조**한다. **새 DB는 v2로 만들고 기존 구 스키마 DB는 자동으로 갈아엎지 않는다** | 제자리 ALTER(중단 시 정본 손상) / 소비자 전부 재작성(범위가 크고 놓친 곳이 조용히 깨진다) / `WITHOUT ROWID`(sync_sheets 증분이 rowid를 쓴다) / 뷰 없이 새 이름 노출(회귀 129건을 다시 맞춰야 한다) |
| D45-a | **오래된 관측 솎기 — 변화 순간은 보존** | 사용자 결정(2026-08-04): 간격을 드물게 하되 **가격·순위가 바뀐 관측은 무조건 남긴다.** 최근 N일(기본 30)은 손대지 않고, 그 이전은 버킷(기본 1시간)당 1개만 남기되 ① 가격·할인율·순위·품절이 앞 관측과 다르면 ② 그 상품×문맥의 첫·마지막이면 무조건 보존. **남길 이유를 세고 나머지를 지운다** — 반대로 하면 규칙을 하나 빠뜨렸을 때 데이터가 사라진다. `--dry-run`이 기본이다. **실측으로 밝혀진 한계를 그대로 보고한다**: 이 정책으로 지워지는 것은 **2.0%뿐**이다. 랭킹 스냅샷은 순위가 매 회 바뀌어 전 행이 "변화"로 잡히는데, 랭킹이 현재 40%이고 크론이 붙은 쪽이라 앞으로 대부분을 차지한다. 순위를 예외에서 빼면 5.4%다. **용량을 줄인 것은 솎기가 아니라 스키마(69.6%)이고, 솎기는 거기에 몇 %를 더하는 수준이다** — 사용자에게 이 사실을 보고했고 "지금 그대로 둔다"로 확정했다(잃는 것이 없으므로) | 순위를 예외에서 제외(오르내림 순간을 놓친다) / N일 후 무조건 하루 1개(가격 변경 순간이 사라져 이중차분이 약해진다) / 솎기 없음(나중에 필요해지면 그때 만들어야 한다) |
| D46 | **시트 미러 무결성 — 올린 뒤 세어 보고 진행점을 옮긴다** | 2026-08-04 업로드 중 발견. **시트가 DB의 4분의 1만 갖고 있는데 `sync_state`는 완료를 주장**하고 있었다(DB 51,034행 / 시트 11,727행 · 39,307행 누락). 증분 미러가 `append_rows` 직후 무조건 진행점을 옮기고 있어서, 업로드가 실패하거나 부분만 반영돼도 다음 실행은 "이미 다 했네" 하고 넘어간다 — **에러가 안 나므로 아무도 모른다.** 함께 발견된 것 둘: ① `observations` 탭이 **156열**(실제 20열)로 부풀어 빈 열이 680만 셀을 먹고 워크북이 셀 한도(1천만)의 97.9%에 도달, `append`가 400으로 거절당했다 ② 그 탭에 **헤더 행이 없었다**(1행이 데이터) — "A1이 비었을 때만 헤더를 쓴다"는 조건이 한 번 어긋나자 영영 안 쓰게 됐다. **수정**: ⓐ 올린 뒤 **시트를 다시 세어 늘어난 만큼이 맞을 때만** 진행점을 옮긴다(아니면 그대로 둬 다음 실행이 재시도) ⓑ 매 실행 끝에 **누적 총계를 대조**하고 어긋나면 `exit 1` + 어느 탭이 몇 행 모자란지 보고 — 조용히 성공으로 끝나지 않는다 ⓒ `--repair`로 어긋난 탭을 통째로 재구축(부분 보정은 불가능하다 — 어긋난 시트는 어디가 비었는지 알 수 없다) ⓓ 올리기 전 빈 열을 실제 컬럼 수로 줄인다. **행 수는 격자 크기(`row_count`)가 아니라 A열을 읽어 센다** — 격자 49,907행인데 실제 데이터가 11,727행이었던 것이 이 구분을 못 하면 안 잡힌다. 훼손 실험으로 검증했다(500행 삭제 → 검사가 exit 1로 잡음 → `--repair`로 복구) | 진행점을 무조건 전진(원래 문제) / 총계 대조 없이 증분만 확인(과거에 어긋난 것을 못 잡는다) / 어긋나면 부분 보정(중간에 빈 행이 흩어져 있으면 이어 붙일 지점이 성립하지 않는다) / 경고만 하고 exit 0(미러가 "됐다"고 하면 팀원은 시트를 믿는다) |
| D47 | **모델링 규칙 — 무엇이 Y가 될 수 있나 · 퍼널 비율 · 액션 · 무영향** | 2026-08-04 피드백 반영. ① **공급자가 정한 값은 Y가 될 수 없다** — "할인율·판매가 등 사람이 설정하는 값은 공급자 입장에서 Y가 될 수 없다"(피드백 §5). 축에 역할을 붙였다: `lever`(가격·할인·재고·옵션 — **원인 쪽에만**) / `response`(조회·하트·후기·구매·순위 — 결과 쪽). 상관은 이 역할로 **방향을 세운다**. "하트가 높을수록 할인율이 높다"는 할인을 하트가 정했다고 읽히는데 실제로는 우리가 정했다. **둘 다 반응이면 방향을 못 정하므로** 문장을 "A와 B는 함께 움직인다 (선후는 알 수 없다)"로 바꾼다 — 실측에서 스커트 상관 3건이 전부 이 경우였다 ② **절대값이 아니라 비율**(피드백 §5) — 퍼널 `조회 → 하트 → 구매`의 전환율 4종을 파생 축으로 만든다. **분모가 없거나 0이면 만들지 않는다**(0이나 100으로 채우면 없는 성과를 지어내는 것) ③ **액션 제안**(피드백 §2) — 강한 주장 카드마다 "이 숫자로 무엇을 정할 수 있나"를 한 줄. **지시가 아니다** — 우리는 상관만 봤고 MD가 "참고용·정성적"이라고 못박았다. 약한 단서는 "아직 정하지 마라"로 시작하고, 반응끼리의 상관은 선후를 단정하지 않는다 ④ **'영향이 없었다'도 인사이트**(피드백 §2) — 효과가 작아 기각된 것을 「차이가 없었다」 섹션으로 실어 "여기엔 힘을 쓰지 않아도 된다"의 근거로 준다. **표본 부족 기각과 반드시 가른다** — 전자는 "없다"이고 후자는 "모른다"인데 섞으면 안 본 것이 없는 것이 된다(n<40도 제외). **실측으로 드러난 공백**: 퍼널 최상단 `view_count`가 **0% 수집**이다(`stat.pageViewTotal`로 받을 수 있는데 한 번도 안 모았다). 하트 89% · 누적판매 17% · 보는중 19%. **비율 축은 재료가 모이기 전까지 전부 None으로 빠진다** — 인프라만 넣은 상태이고, 수집이 선행돼야 한다 | 할인율을 Y로 계속 두기(인과가 거꾸로 읽힌다) / 절대값만 쓰기(노출량에 끌려간다 — 많이 노출된 상품이 하트도 많은 건 상품의 힘이 아니다) / 액션을 지시문으로 쓰기(근거보다 세 보인다) / 무영향과 표본부족을 함께 싣기("모른다"가 "없다"로 둔갑한다) |
| D48 | **robots는 호스트마다 다르다 — 퍼널은 구간 표기를 순서형 축으로** | 사용자 지시(2026-08-04 "퍼널 인프라 고도화")로 무신사 정찰 중 발견. **`goods-detail.musinsa.com`의 robots가 `User-agent: * / Disallow: /`다**(Claude 예외 없음, 2회 재확인). 어댑터 §4-1이 "확인 2026-07-30"으로 적어 둔 `stat`(조회수·누적판매 원시 정수)·`page-view`·상세 JSON·`options`가 전부 그 호스트다 — **`www`의 robots만 보고 하위 호스트의 자체 robots를 아무도 안 봤다.** 그래서 쓰지 않는다. 전송 수단을 브라우저로 바꿔도 같다(그 호스트를 향한 자동 요청이라는 사실이 안 변한다). **사용자 결정**: "경쟁사든 자사든 구간 표기라도 얻어오는 게 낫다". 얻는 법 — 허용된 `www.musinsa.com/products/{no}`를 브라우저로 열어 화면 문구를 읽는다(서버 HTML엔 없다: 198KB에 문구 0건, 렌더 후 표시). **정수로 담지 않는 원칙은 그대로다**(함정 1). 대신 **구간의 하한은 정확하므로 순서는 오차 없이 매겨진다** — "1.2만 이상"이 12,000보다 크다는 것은 틀림이 없다. 분석 층이 `view_band`·`purchase_band`·`like_band`(하한 정수)를 파생해 **순위 기반 통계에만** 쓴다. 이 프로젝트의 통계가 **Cliff δ와 Spearman — 둘 다 순위 기반**이라 그대로 성립한다. **평균·합계·비율의 분모로는 쓰지 않는다**(하한으로 나누면 전환율이 부풀려진다 — 실제 9만인 상품을 12,000으로 나눈다). 그 결과 **D47의 퍼널 전환율은 무신사에서 성립하지 않는다** — 원시 정수가 없다. 호스트별 robots 표를 어댑터 접근 정책에 박았다 | 브라우저로 차단 호스트 API 응답 수집(전송 수단만 바꾼 우회) / 구간을 정수로 담기(없는 정밀도를 만든다 — 함정 1) / 조회수를 포기(사용자가 구간이라도 쓰겠다고 결정) |
| D48-a | **무신사 허용 경로 수집기 · 정찰 성과** | 정찰로 찾은 것(2026-08-04): **① 연령대 × 기간 랭킹**(`client.musinsa.com` — 6조합 md5 전부 다름, 세그먼트별 수요가 실제로 갈린다) **② 리뷰 만족도 설문**(`goods.musinsa.com` — `reviewSurveySatisfaction`에 사이즈·색감·퀄리티·신축성 응답. **분포 API가 없어**(404) 우리가 센다. 5448897 실측 353건 전수: 사이즈 `조금 큼 138 + 많이 큼 77 = 61%` — 리오더 사이즈 커브의 근거) **③ 상품 나이**(리뷰의 `goodsCreateDate` 2025-09-12 · 상세의 `sellStartDate` 2026-06-04 — **둘이 9개월 다르다**. 누적 지표 정규화에 어느 쪽을 쓸지 정해야 한다) **④ 연결 콘텐츠**(`content.musinsa.com` — 룩북·매거진의 `displayedFrom`·`commentCount`·`viewCount`. 콘텐츠 효과 이벤트 스터디의 재료) **⑤ 유사 상품**(`goods.musinsa.com/similar-list` — 실측은 경쟁사가 아니라 **컬러 변형**이었다. D34 변형 묶기에 쓴다) **⑥ 월간 랭킹 이력**(상세 JSON `rankingRecord` — 축적 없이 과거를 준다. 단 차단 호스트라 `www` 화면에서 읽는다). 수집기 `musinsa_collect.py` 신설(plp·likes·reviews). **PLP 페이지 상한 결함을 실측으로 잡았다** — 고정 200페이지가 246페이지짜리를 잘라 여성 데님 팬츠를 **81.5%만 받고도 "완료"로 보였다.** `totalPages` 기반 상한 + **총계 대비 커버리지 보고**로 고쳤다. `page≥2`를 직접 조립하면 403이라는 어댑터 함정도 오늘 다시 밟아(403) `nextPageUrl` 추적으로 고쳤다 | URL 추측으로 스냅 엔드포인트 찾기(5개 전부 404/530 — 브라우저 네트워크 캡처가 답이었다) / 고정 페이지 상한 유지(큰 카테고리를 조용히 자른다) |
| D49 | **인사이트 레이아웃 — 축을 「효과 크기」에서 「말할 수 있냐」로** | 사용자 지적(2026-08-04): "차이가 없는 것도 인사이트가 될 수 있다고 했는데, 지금처럼 강한/약한 가설로 나누는 레이아웃은 재검토가 필요하다". 맞다 — **「차이가 없었다」가 약한 단서보다 아래에 있었다.** 그런데 n=500에서 효과가 0.03이면 그건 약한 게 아니라 **"여기 신경 쓰지 마라"를 자신 있게 말할 수 있는 결론**이다. 축이 잘못 잡혀 있었다. **① 차이가 있었다 · ② 차이가 없었다 · ③ 판단 보류 · ④ 검정했으나 결론에 못 넣은 것**으로 바꾼다 — ①②는 **둘 다 결론**이라 나란히 두고 ③이 아래로 내려간다. 머리 통계도 「강한 주장 / 약한 단서」에서 **「말할 수 있는 것(①+②) / 판단 보류(③)」**로 합쳤다 — 나눠 놓으면 "차이 없음"이 부산물처럼 보인다. 실측(스커트): 말할 수 있는 것 14(있다 8 + 없다 6) · 판단 보류 19 | 강함→약함 사다리 유지(무영향이 끝에 묻힌다) / ②를 ①에 합치기(있다와 없다를 한 통에 넣으면 읽는 사람이 방향을 놓친다) |
| D49-a | **광고(`isAd`)는 빼는 게 아니라 나눠서 본다** | 사용자 지적: "광고로 트래픽을 샀을 때 퍼널이 얼마나 변화하는지도 의미 있는 것 아닌가". 맞다 — 기존 규칙 "오가닉 분석에서 분리한다"가 "제외"로 읽혔다. **`isAd`를 처치 변수(X)로 놓고 Y를 비교**한다. 단 **셀렉션을 경고한다**: 광고 태울 상품은 무작위로 안 고른다(잘 팔릴 것 같아서 태우거나, 안 팔려서 태운다) — 단순 비교는 광고 효과가 아니라 선택의 결과다. 같은 상품의 광고 전후를 보는 쪽이 낫다. **⚠️ 현재 재료가 없다**: PLP에서 여성 데님 11,989건 · 2000아카이브스 297건 **전부 `isAd=false`**, 정렬 4종(인기·추천·기본·검색) 재확인도 60건 중 0건. 필드는 있는데 값이 안 온다 「미확인」 | "광고는 교란이니 제외"(마케팅이 알아야 할 값을 버린다) / 값이 없으니 규칙도 빼기(재료가 생기면 바로 쓰도록 남긴다) |
| D50 | **화면 수집기(브라우저)와 결측이 무작위가 아니라는 발견** | 조회수·누적판매 원시 정수는 robots 차단 호스트에만 있어(D48) **허용된 `www` 상품 페이지를 브라우저로 읽는다.** `musinsa_page_scrape.js` 신설 — 시스템 Chrome(`channel: chrome`, 번들 브라우저 미설치), 이미지·폰트 차단, 동시 5. 2000아카이브스 297건 **실패 0**. **함정 하나를 실측으로 잡았다**: 대기 조건을 `"조회수" \|\| "후기"`로 걸었더니 후기가 먼저 떠서 정보 탭 렌더 전에 통과했고 **조회수·문의·품번이 통째로 null**이었다 — `"조회수"`만 기다리고 스크롤로 lazy-load를 깨우도록 고쳤다(0% → 79%). **⚠️ 핵심 발견 — 결측이 무작위가 아니다(MNAR)**: 무신사는 일정 수준 이상 팔린 상품에만 이 값을 노출한다. 실측(297개): 조회수 표시 있음 235개(하트 중앙 **508**) 대 없음 62개(하트 **117**) · 누적판매 있음 58개(하트 **1,594**) 대 없음 239개(하트 **315**). **값이 있는 상품만 분석하면 잘 팔리는 것만 보게 된다** — 누적판매 기준으로는 상위 20%만 남는다. 결측을 0으로 채우지 않는 것은 기존 규칙이지만 **버리는 것도 편향**이라는 사실을 analysis-context에 박았다: 이 축으로 비교할 때 "값이 노출된 상품 안에서"를 명시하고, **노출률 자체를 함께 보고**한다(노출률 차이가 곧 성과 신호다) | 결측 상품을 조용히 제외(생존 편향) / 0으로 채우기(없는 성과를 만든다) / 원시 정수 API 사용(robots 위반) |
| D50-a | **경쟁셋 정의 — 브랜드 랭킹 상위 N** | 사용자 지정(2026-08-04): 여성×데님팬츠 **브랜드 랭킹(월간) 상위 30**의 해당 카테고리 상품. 전수 11,989건은 브라우저 경로로 13시간이 걸려 현실적이지 않았고, 상위 30이면 **1,612건 · 약 40분**이다. `client.musinsa.com/.../ranking/sections/1054` + `subPan=brand`에서 `type=RANKING_BRAND` 항목의 `title.rank`와 `onClick.url`의 slug로 뽑는다(실측 200항목 중 고유 50). **선정 근거를 데이터에 남긴다** — 각 상품 `raw_extras.brand_rank`·`brand_slug`. 이게 없으면 나중에 "생존자만 봤나"를 판정할 수 없다. 실측: 무신사 스탠다드 우먼 240 · 에잇세컨즈 239 · 어반드레스 127 · **2000아카이브스 18** | 카테고리 전수(브라우저 13시간) / 상품 랭킹 상위 N(브랜드 다양성이 안 잡힌다) / 선정 근거 미기록(재현·편향 판정 불가) |
| D51 | **Y 선정을 그룹 비교에도 적용 · 구조 결측 분리 · 프록시 자동 배선** | 사용자가 자사 브랜드 리포트를 보고 지적(2026-08-04): "사람이 조정할 수 있는 값은 Y로 쓰지 말라는 컨텍스트를 먹였는데 인사이트가 대부분 그런 값이다". 원인이 셋이었다. **① D47을 `run_corr`에만 걸었다** — `run_group`은 역할을 안 봐서 "브랜드에서 A는 B보다 할인율이 낮다"가 강한 주장 8개 전부를 차지했다. 이제 **반응 지표가 하나라도 있으면 그쪽만 쓰고**, 하나도 없으면 lever를 쓰되 액션에 "우리가 정한 값이다"를 적는다(리포트를 비워 보내지 않는다). **② 같은 브랜드가 표기로 갈렸다** — `2000아카이브스`·`2000Archives`·`2000 Archives`(같은 자사몰 안에서도 갈렸다)가 서로 비교돼 "정가 차이가 없다"가 인사이트 자리를 먹었다. `brand_key()`로 공백·대소문자만 걷어내 묶고(한글·영문은 안 묶는다 — 음차 매칭은 근거가 없다), **`brand:` 문맥에서는 브랜드 축 자체를 뺀다**(전부 같은 브랜드다). **③ 진짜 원인은 반응 지표가 축에 없던 것** — 하트·후기·평점 결측률이 44.5%라 EDA가 버렸는데, 그 결측은 **품질이 아니라 구조**였다(자사몰 Cafe24는 하트를 아예 노출하지 않는다). **노출하는 플랫폼 안에서** 결측률을 다시 재도록 고쳤다(44.5% → 0%). 어느 플랫폼이 빠졌는지(`blind_sites`)를 함께 남긴다 — 비교가 그 플랫폼을 안 본다는 사실을 리포트가 말해야 한다 | 결측률만으로 축 판정(구조 결측에 축이 통째로 죽는다) / 한글·영문 브랜드 음차 매칭(근거 없는 추측) / lever 지표를 아예 금지(반응 지표가 없는 문맥에서 리포트가 빈다) |
| D51-a | **액션 플랜을 여러 줄·구체적으로 · 프록시 자동 생성 배선** | 사용자 요청: "액션플랜 좀 더 쉽고 구체적인 걸로 여러 가지가". 한 줄이었고 문장이 추상적이었다("참고선으로 쓴다"). **동사로 시작 · 카드의 숫자를 문장에 넣기 · 지시하지 않기** 세 규칙으로 2~4줄을 만든다. 글머리표는 `·`가 이 폰트에서 안 그려져(BAD_GLYPHS) `・`로 쓴다 — 접두사가 `esc()` 밖이라 치환을 못 받았다. **그리고 프록시가 하나도 안 생기던 것을 고쳤다**: D43이 "기본으로 만든다"로 정책을 뒤집었는데 **스크립트 연결을 안 해서** `insight.py`만 돌리면 0개였다(참조 0곳). `ensure_rule_proxies()`로 rule 프록시를 자동 생성·적재한다 — 실측: 자사 브랜드 문맥에서 6축 1,127건이 생기고 가설이 33 → 176개가 됐다. **vision 프록시는 여기서 안 만든다**(배치가 비싸다 — §0 절차 그대로 AI가 판단) | 문서만 고치고 배선 안 하기(D43이 그래서 0개였다) / insight에서 vision까지 자동(비용이 크고 사용자 의도를 못 본다) |
| D51-b | **문맥 접두사 중복 가드** | 내 실수로 `brand:brand:2000아카이브스`·`market:market:데님팬츠(여성)`가 DB에 들어갔다(13,898행). `context_of()`가 `story`로 접두사를 붙이는데 raw 파일 `target`에 접두사를 이미 넣어서다. **`target`에는 이름만 들어간다.** 수집기가 실수로 붙여 와도 `context_of`가 걷어내도록 고쳤다 — 조용히 두 문맥으로 갈리는 것보다 낫다. 다른 접두사가 붙어 오면 의도일 수 있으니 건드리지 않는다 | 수집기만 고치기(다음 수집기가 또 밟는다) / 모든 접두사 제거(의도한 이름을 지운다) |
| D51-c | **자동 프록시가 정본 DB가 아니라 유령 DB에 쌓이던 것** | D51-a가 배선한 `ensure_rule_proxies()`가 `proxy_auto.py`에는 `--db`를 넘기고 **적재하는 `intel_db.py`에는 안 넘겼다.** `intel_db`의 `DEFAULT_DB`는 `data/intel.db`, 즉 **CWD 상대 경로**다 — `skills/commerce-intel/scripts/`에서 돌리면 거기에 빈 DB를 새로 파고 판정을 통째로 넣는다. 화면에는 `채택 name_length 11,989건 (100%)`이 찍히는데 정본에는 **한 건도 안 남는다**. 실측(2026-08-04): 그렇게 만들어진 유령 DB `scripts/data/intel.db` 36MB를 발견했고, 그 안에 프록시 15종 13만 건이 들어 있었다. 정본 DB의 프록시는 그동안 0건이라 **프록시가 검정 축이 된 적이 없었다**(D39가 무효였다). `--db db_path`를 넘기도록 고쳤다. 같은 실수를 사람이 손으로 돌릴 때도 하므로(내가 그랬다) **`intel_db.py`는 항상 `--db`를 명시한다**를 규칙으로 둔다 | `DEFAULT_DB`를 절대 경로로 바꾸기(레포를 옮기면 깨지고, 다중 DB 리허설 `INTEL_DEMO_DB`가 막힌다) / 적재 성공 건수만 믿기(성공을 찍고 딴 데 넣는 것이 이 버그다) |
| D52 | **EDA 정리 — 공식 스킬은 대체가 아니라 보완, 도메인 스킬을 파이프라인에 연결** | 사용자 요청으로 검토(2026-08-04). 다른 세션이 ① Anthropic 공식 `data-*` 스킬 5종 설치 ② `skills/intel-fashion-md/`(패션 MD 도메인 + `sanity_check.py`) 신설을 해 뒀다. **검토 결론: `eda.py`를 공식 스킬로 갈아끼우지 않는다.** 층이 다르다 — `eda.py`는 **결정적 스크립트**로 `signals[]`를 만들어 다음 단이 먹고, 공식 스킬은 **AI 지침서**로 사람이 읽는 문서를 만든다. 갈아끼우면 실측으로 하나씩 잡은 것들이 날아간다(미노출과 0의 구분 · 구간 표기는 축이 못 된다 · 정의상 연동 쌍 제외 · 심슨의 역설 재확인 · 생존편향 · 구조 결측 분리 D51). 대신 **우리에게 없는 두 곳에 쓴다**: `data-explore-data`(처음 보는 데이터·새 플랫폼 — `eda.py`는 우리 스키마를 전제한다) · `data-validate-data`(**공유 전 방법론·편향 QA** — 5관문은 자동 판정이라 "이 분석이 타당한가"를 못 본다. D51에서 Y가 전부 할인율이던 것을 사람이 발견한 게 증거다). **중복 정리**: Y 선정·퍼널·비율 규칙이 `intel-fashion-md`·`analysis-context.md`·`intel-insight` 세 곳에 있었다 — **`analysis-context.md`를 정본으로 두고**(실측 근거가 거기 붙어 있다) 도메인 스킬에서는 걷어내 가리키게 했다. **배선**: `intel-fashion-md`를 아무도 안 부르고 있었다(description은 "함께 트리거된다"고 하는데 참조가 0곳) — 오케스트레이터가 부르게 하고, **`sanity_check.py`를 intel-insight §0-c로 넣었다**. 5관문은 "이 발견이 우연인가"를 보지 **수치가 맞는지는 안 본다** — 틀린 값으로 일관되게 계산하면 5관문을 통과한다. exit 0/1/2 규약(FAIL이면 리포트 금지)을 실측 확인했다 | `eda.py`를 공식 스킬로 대체(실측으로 잡은 도메인 규칙이 전부 사라진다) / 공식 스킬 무시(공유 전 QA와 신규 데이터 탐색이 계속 빈다) / 규칙을 세 곳에 두기(어긋나는 날 어느 쪽이 사실인지 모른다) |
| D53 | **프록시 값 공간이 바뀌면 옛 판정을 버린다 — 세 겹 방어** | 사용자 지적으로 발견(2026-08-04): "`name_lang` 캐시가 오염됐다 — 같은 개념이 `영문`(1,120)과 `영문만`(945)으로 갈렸다". 실측하니 **두 축이 오염**돼 있었다(`thumb_cut`도 `스튜디오 모델컷` 65 · `일반 착용컷` 17 등 정의 밖 99건). 원인: `proxy-load`가 정의를 `ON CONFLICT DO UPDATE`로 덮어쓰면서 **옛 캐시를 그대로 뒀다.** 값 공간 검사는 **적재 시점의 새 정의**로만 도니 이전 정의로 들어간 값은 검사를 안 거친다. 같은 이름으로 다시 정의하는 순간 축이 갈리고, 분석은 "영문과 영문만은 다르다"를 검정하게 된다. **세 겹으로 막는다**: ① **적재 가드** — 값 공간이 바뀌면 그 프록시의 캐시를 버리고 **버렸다는 사실과 건수를 찍는다** ② **`proxy-audit` 명령** — 이미 들어간 값 공간 밖 판정을 찾는다(`--fix`로 제거, exit 2) ③ **읽기 안전망** — `collect()`가 값 공간 밖 값을 발견하면 **그 축을 아예 안 쓰고** `off_space`에 남긴다. 세 번째가 핵심이다 — 앞의 둘만 있으면 "누가 청소를 안 했을 때" 조용히 틀린다. **기각한 대안: "값 공간이 바뀌면 새 이름을 쓴다"** — 사람 규약이라 안 지켜지고, `name_lang2`·`name_lang3`으로 늘면 뭐가 정본인지 알 수 없게 된다 | 새 이름 규약(사람이 지켜야 한다) / 적재 가드만(청소 안 된 DB에서 분석이 조용히 틀린다) / 옛 값을 새 공간에 매핑(추측이다 — `영문만`이 `영문`인지 우리가 정할 일이 아니다) |
| D54 | **생존 편향 — 이탈 브랜드를 데이터로 만든다 · 동적 속성이 축이 되게** | `docs/survival-bias-removal.md`(사용자 작성 원문, 구 `생존 편향 제거.md`) 반영. **랭킹만 보면 생존자만 본다** — "상위 30 중 와이드 20개"에서 "와이드가 대세"를 읽으면 틀린다(전체 800개면 진입률 2.5%, 공급 적은 스트레이트가 7/200=3.5%로 더 높을 수 있다). 전수조사가 제일 좋지만 **없어도 시간이 비생존자를 만든다.** **사용자 정의**: 생존자 = 여성 데님 브랜드 랭킹 **1주** 상위 30, 이탈자 = **1개월** 상위 30에 있었으나 1주에는 없는 브랜드. `musinsa_brand_survival.py` 신설 — 실측(2026-08-04): **이탈 6**(론론·플리즈노팔로우·달렌·캘빈클라인 진·이투둘·이지노이지) · 신규 진입 6 · **하락 중인 생존자 5**(에잇세컨즈 4→10위 등 — 문서가 말한 "살아는 있지만 죽어가는" 단계). **론론은 카테고리 상품 58개로 3위 아캄(48개)보다 많은데 이탈했다** — 공급량 ≠ 성과의 실증이다. **이탈 상품은 전수에서 찾는다** — 경쟁셋은 생존 브랜드로 뽑은 목록이라 이탈이 통째로 빠진다(그러면 생존 편향이 그대로다). 상태는 `product_attributes`(D35)로 편다(TTL 7일 — 랭킹은 계속 바뀐다). **그 과정에서 D35의 갭이 드러났다**: "스키마 변경 없이 축을 늘린다"고 했는데 **축 목록(CAT_AXES)이 고정**이라 새 속성을 넣어도 분석이 안 봤다. `attr_axes()`로 동적 속성이 자동으로 축이 되게 했다(값이 2종 이상일 때만 — 전부 같으면 비교 상대가 없다). 실측: `attr_lifecycle`이 있는데 여태 축이 아니었다 | 랭킹만으로 분석(생존자만 본다) / 이탈을 경쟁셋에서 찾기(거기 없다) / 랭킹에 없는 브랜드를 이탈로 보기(**모른다 ≠ 이탈**) / CAT_AXES에 손으로 추가(D35가 하려던 것과 반대다) |
| D55 | **축 위생 — 표기 변형이 그룹을 가르던 것 · 청중 상한이 사실상 없던 것** | 2026-08-04 여성 데님 리포트 검수에서 강한 주장 10개 중 둘이 발견이 아니라 아티팩트였다. **① 카테고리 표기 변형** — 주장 10번이 `데님팬츠`(2,953)와 `데님 팬츠`(39)의 비교였다(δ=-0.58 · n=2,992). D51이 브랜드에만 `brand_key()`를 걸고 카테고리는 앞뒤 공백만 걷어내 **안쪽 공백이 남았다.** 카테고리도 같은 키로 묶는다. 계층 표기(`의류 > 바지 > 청/데님 팬츠`)는 여전히 따로 남아 D42가 굵기 차이로 거른다. 묶으면서 **계층 조회에는 표시값을 넘긴다** — `hier`는 카탈로그 원문으로 지어져 있어 정규화 키로 조회하면 계층을 통째로 놓친다. **② 청중 편중 상한이 2차 채움에서 무효였다** — 강한 주장 10개가 전부 `마케팅`이었다(CAP=4인데). 1차에서 상한에 막힌 청중이 2차에서 남은 자리를 원래 순서대로 쓸어 담았다. 이제 2차는 **그때까지 가장 적게 실린 청중부터** 본다 — 실측 4/4/2로 갈렸다. 빈칸은 여전히 안 만든다(후보가 없으면 다음 청중으로 넘어간다). **③ 값 공간 밖 판정 청소** — D53이 적재·읽기를 막았지만 이미 갈린 캐시는 그대로였다. `proxy-audit --fix`로 `name_lang` 1,618건·`thumb_cut` 99건을 지웠다 — 안 지우면 D53 안전망이 그 축을 통째로 빼서, 막히긴 하지만 **쓸 수 있는 축이 사라진다** | 카테고리를 원문 그대로 두기(같은 값이 두 그룹으로 갈려 서로 검정된다) / 계층 조회에 정규화 키 넘기기(D42가 통째로 무력화된다) / 2차 채움을 없애 상한을 엄격히 걸기(청중별 후보가 편중된 문맥에서 리포트가 빈다) |
| D56 | **읽는 사람 기준으로 다시 쓴 인사이트 — 누가 무엇을 할 수 있나** | 2026-08-04 사용자 피드백(여성 데님 리포트 실물을 보고). 일곱 가지를 한 번에 고쳤다. **① 제3자끼리의 브랜드 비교를 뺀다** — "에잇세컨즈는 잠뱅이보다 평점이 높다"는 우리 팀이 옮겨 쓸 규칙이 없다. 브랜드 **정체성** 축(`cat_field="brand"`)은 자사가 한쪽에 있을 때만 낸다(`--own-brand`, `brand:` 문맥은 자동). **브랜드의 속성 축은 안 막는다** — "론론이 상품 58개로 3위 아캄(48개)보다 많은데 이탈했다: 공급량이 성과가 아니다"처럼 자사에 옮겨 쓸 교훈이 나오기 때문(사용자가 명시적으로 살리라고 했다). 자사 표기 3종은 실제 데이터에서 걷어 온다 — `brand_key()`가 한글·영문을 일부러 안 묶어서(D51) 한 표기만 알려주면 나머지가 제3자로 걸린다. **② 주장 한 줄에 숫자를 넣지 않는다** — `(중앙값 4.8 대 0)`은 카드 본문과 상세로. 한 줄은 무슨 일이 있었나만 말한다. **③ 축 이름을 사람 말로** — `denim_rise(AI)` → `밑위(라이즈)(AI 판정)`. 카드에 `label`을 두고 `proxy_defs.label`로 보존한다. **"AI 판정" 표시는 남긴다** — 사용자가 뺀 것은 내부 이름이지 노출값과 판정값을 구분하는 정직성 규칙이 아니다. **④ 액션 플랜에 기전 추측을 허용한다** — "액션플랜은 추측이 들어가도 돼. 가능한 선택지를 많이 제시하는 데 의의가 있어". 축마다 다른 `MECHANISMS` 목록에서 "…일 수 있다 + 확인 방법"을 뽑는다(사용자 예시: "'데님 팬츠'가 검색에 더 잘 걸리기 때문일 수 있으므로 수정 검토"). 어조는 여전히 조건부다 — 추측을 허용한 것이지 단정을 허용한 것이 아니다. **⑤ 누적판매량이 1순위 Y** — `purchase_count`·`purchase_band` 가중치 6.0(할인율 3.0의 두 배). **⑥ 개수 제한 폐기** — 강한 10·약한 20을 200으로 올렸다(폭주 방지용 상한만 남김). 자격은 처음부터 5관문이 정했다. **⑦ 조사 와/과** — `데님와` → `데님과`. 받침 방향이 은/는·이/가와 반대다. 덤으로 **같은 재료의 수치 프록시 쌍을 정의상 연동으로 뺀다** — `상품명 길이`와 `상품명 단어 수`의 r=+0.78은 발견이 아니라 산수인데 강한 주장 한 자리를 먹고 있었다. 프록시 이름이 아니라 **재료**로 판정한다(D19 하드코딩 금지) | 브랜드 축을 통째로 금지(자사 벤치마크와 브랜드 속성 교훈까지 사라진다) / "AI 판정" 표시까지 제거(노출값과 판정값이 섞인다) / 액션 플랜을 단정형으로(우리는 상관만 봤다) / 개수 제한을 0으로(폭주 시 수백 개가 실린다) |
| D57 | **배포는 조용히 실패한다 — 프론트매터를 파서로 검사한다** | 2026-08-04 팀원 환경 실측: 공유한 스킬 15종 중 **7종이 목록에 아예 없었다**(commerce-intel·intel-collect·intel-explore·intel-insight·intel-store·platform-skill-maker·platform-wconcept만 설치됨). 원인은 `description` 안의 `예: "무신사에서 …"` — **인용부호 없는 YAML 평문 스칼라에 `: `가 들어가면** 파서가 매핑으로 오인해 프론트매터 전체가 깨지고, 로더는 그 스킬을 **에러 없이 건너뛴다.** platform-29cm·eql·generic·musinsa·musinsa-global·ownmall·ssf 7종이 전부 같은 문장 틀(`예: "…"`)을 쓰고 있었다. platform-wconcept만 살아 있던 이유는 값을 인용부호로 감싸고 안쪽을 `\"`로 이스케이프해 뒀기 때문이다 — **우연히 맞았던 것**이고, 그 이스케이프 형식이 오히려 실수를 부른다. 조치: ① 8종 모두 `description: >-` **블록 스칼라**로 통일(인용·이스케이프가 필요 없어진다) ② `tools/validate_skills.py` 신설 — 실제 YAML 파서로 파싱하고 name 형식·디렉터리명 일치·description 상한(1,024자)까지 본다. pyyaml이 없는 환경에서는 평문 스칼라 속 `: `만 잡는 린트로 내려가되 **블록·인용 값은 오탐하지 않는다** ③ `package.sh` 1단계 게이트 — 깨졌으면 zip을 만들지 않는다 ④ 회귀 [E] 3건. **왜 문서까지 고치나**: zip에는 파일이 멀쩡히 들어 있고 배포한 쪽 화면에는 아무 표시가 없다. 즉 **팀원이 말해 주기 전까지 알 방법이 없는 종류의 실패**다 — 사람의 주의가 아니라 게이트로 막아야 한다 | 문제 파일만 고치고 끝내기(같은 문장 틀을 다음 스킬에 또 쓴다) / wconcept식 `\"` 이스케이프로 통일(읽기 어렵고 실수가 다시 난다) / 파서 없이 정규식 린트만(진짜 로더가 무엇을 거부하는지는 파서만 안다) |
| D58 | **정본 부분 배포 — 허용 목록으로 떼고, 범위 밖이면 실패시킨다** | 2026-08-04 사용자 지시: "정본 데이터 중에 **2000아카이브스 제품**이랑 **여성 데님 팬츠** 데이터는 정본도 배포할게. 나머지 정본은 배포 X". 범위는 사용자 확정으로 **여성 데님팬츠 = 이미 수집한 브랜드랭킹 상위30 전수조사**(같은 날 돌린 여성 전수조사 11,989건은 범위 밖)이고, 파생 판정(핏·lifecycle 속성 · 프록시 캐시 · 인사이트 이력)도 **함께** 배포한다. 구현: `tools/make_seed_db.py`가 정본의 DDL을 복제한 빈 DB에 범위 행만 옮긴다 — 브랜드는 `brand_key()`로 표기 변형 3종(`2000아카이브스`·`2000Archives`·`2000 Archives`)을 묶고, 런은 target `%브랜드랭킹 상위30%`로 고르고, 상품 키에 매달린 5개 표(observations·variants·variant_observations·product_attributes·proxy_cache)는 범위 상품에 걸린 행만, 계약·정의(platforms·proxy_defs)는 전량, 인사이트는 문맥(`brand:2000아카이브스`·`market:데님팬츠(여성`)으로 고른다. **`sync_state`는 안 나간다** — 우리 시트 미러 위치다. 실측 산출: 상품 2,722(브랜드 1,128 + 런 1,594) · 관측 4,670 · 프록시 캐시 21,058 · 속성 1,351 · 인사이트 1,056 · **8.2MB(zip 압축 후 1.6MB)**. 팀원은 `intel_db.py merge`로 자기 DB에 합친다(관측 append only · 빈 값으로 안 덮는 기존 규약 그대로). **허용 목록 방식이고, 만든 뒤 범위 밖 행이 한 줄이라도 있으면 실패시킨다** — 범위를 넓히는 건 한 줄이지만 빼는 걸 잊으면 남의 데이터가 나간다. 회귀 [F] 8건(범위 안/밖·표기 변형·검산이 실제로 잡는지). 배포에 나가는 DB는 이 seed 하나뿐임을 압축 직후 zip 안에서 센다 — 같은 날 `scripts/data/intel.db`(20MB 정본 사본)가 실수로 zip에 들어가 있었다(D57 조치와 함께 차단). **한계로 남은 것**: 여성 데님 인사이트 818건은 근거 데이터(전수 11,989건)가 범위 밖이라 팀원 환경에서 **재검증이 안 된다**. 주장은 읽히지만 숫자를 되짚을 수 없다는 뜻이고, 필요하면 그 런을 범위에 넣는 것으로 해소한다(한 줄) | 정본 통째 배포(사용자가 명시적으로 거절) / 제외 목록 방식(빼는 걸 잊으면 새고, 잊었다는 사실도 안 보인다) / 인사이트 제외(사용자가 포함을 선택) / 팀원에게 CSV로 주기(merge 규약을 못 쓰고 관측 중복이 그대로 쌓인다) |
| D59 | **merge가 새 DB에서 죽어 있었다 — 배포 첫 명령이 그것이다** | 2026-08-04, 배포본을 압축 풀고 팀원 절차를 그대로 밟다가 잡았다(D58의 seed를 빈 DB에 합치는 그 명령). 세 겹으로 깨져 있었다. **① 뷰에 업서트** — `cannot UPSERT a view`. 새 DB는 항상 v2로 지어지고 v2에서 옛 이름은 뷰다(D45). v2는 업서트 의미를 `INSTEAD OF INSERT` 트리거로 옮겨 뒀는데 `cmd_merge`만 여전히 `ON CONFLICT`를 붙이고 있었다 — D45 때 같이 안 고쳐진 함수다. 뷰면 평범한 INSERT를 던지고 덮어쓸지 지킬지는 트리거가 정하게 했다. **② 순서가 규칙이었다** — 관측을 상품·런보다 먼저 넣고 있었다. v2는 관측을 `pk`(상품)·`run_ref`(수집)로 잇는데 그 둘이 없으면 트리거가 참조를 NULL로 풀고, 바깥의 `INSERT OR IGNORE`가 그 실패를 **조용히 삼킨다**. 실측: 상품 2,722은 들어오고 **관측 4,670이 예외 없이 전부 사라졌다.** 런 → 정적 → 관측 순으로 고정. **③ `product_attributes`가 merge 대상에 없었다** — D35로 표를 만들 때 merge에 추가하지 않았다. 핏 판정 1,351건이 팀원에게 갈 길이 없었고, 그러면 같은 이미지 판단을 다시 해야 한다. 판정 충돌은 **들어온 값이 이긴다**(v2 트리거와 같은 규칙 — COALESCE로 지키면 `ttl_days=NULL`의 의도가 무시되고 v1·v2가 같은 명령에 다르게 반응한다). 회귀 [M] 4건. **교훈은 검증 방식이다** — 저장소에서 단위로 돌려서는 셋 다 안 보였다. **압축을 풀어 팀원 절차를 그대로 밟은 다음에야** 보였고, 그래서 배포 검증을 그 형태로 남긴다(E-DP-9~11) | 저장소에서 함수만 테스트(세 결함 모두 통과한다 — 정본이 v1이라서) / 뷰에도 업서트가 되게 스키마를 되돌리기(D45를 무르는 일) / 속성은 각자 다시 판정(같은 이미지 판단을 사람 수만큼 반복한다) |
| D60 | **그룹 비교 카드가 "상품 1개당"임을 스스로 말한다** | 2026-08-04 사용자 지적: "데님 카테고리에서 화이트는 원래 소수인데, 품목이 적어서 하트가 낮은 건지 품목 대비(상품 1개당) 낮은 건지 명시해줘. 다른 인사이트들도 마찬가지". 비교는 처음부터 상품 1개당 분포 비교(Cliff δ)였지만 **리포트가 그 사실을 안 말해서** 총합 비교로 오해될 여지가 있었다 — n을 합계 하나(n=291)로만 적으면 그룹 크기 편차가 결과를 만든 것처럼 읽힌다. 근거 줄을 `상품 1개당 비교 — 화이트/아이보리 83개(중앙값 X) 대 유채색 208개(중앙값 Y)`로 바꾸고 (강한·차이없음·보류 3곳 전부), 정직성 배너에 "품목 수가 많은 그룹이 유리한 합계 비교가 아니다"를 명시했다. 쌍체 비교는 `같은 상품끼리 쌍 비교 — N쌍`으로 구분 표기 | 합계 n 유지(그룹 크기 편차가 결과를 만든 것처럼 읽힌다) / 배너에만 쓰기(카드만 발췌해 공유하면 사라진다 — 카드가 자기 완결적이어야 한다) |
| D61 | **sanity_check가 리포트의 진짜 수치를 본다 — 스타일 단위·프록시 축 지원** | 오늘 리포트를 sanity check했더니 공식 도구가 핵심 수치를 하나도 못 봤다: 관측 단위로만 재계산해서 스타일 접힘(경쟁셋 358건)과 정의가 달랐고(자사 하트 중앙값 427 대 카드의 464), proxy_cache 필터가 없어 프록시 축(워싱 톤 등) 그룹을 만들 수 없었다. 확장: ① `filters`에 `px:<프록시명>` — 재료 지문이 현재 값과 맞는 판정만 유효(리포트와 같은 무효화 규칙) ② claim에 `style_fold: true` — match_key로 접고 수치는 변형 중앙값 ③ `*_band` 파생 필드(band_floor). **접기·필터 순서가 정의의 일부다** — 접기 전에 거르면 진청 변형과 한 스타일로 접히는 화이트 변형들이 각자 스타일 행세를 해 화이트 21개가 42개로 불었다(실측). 리포트처럼 **접은 뒤 스타일 최빈값**으로 그룹을 정한다. match_key·band_floor는 commerce-intel에서 임포트 — 접는 규칙이 두 벌이면 어긋나는 날 가짜 FAIL이 난다(정의 공유, 실행·집계는 독립). 실측: 경쟁셋 강한 주장 6개 축(진청/화이트 누적판매·핏·생존/이탈 조회수) 전부 PASS. 근거 줄 54개 전수 대조(스크립트)도 54/54 일치 | 관측 단위 검증만 유지(핵심 수치가 검증 밖에 남는다) / 접기 로직 자체 구현(정의가 두 벌이 된다) / 접기 전 필터(그룹 소속이 리포트와 달라진다 — 21→42 실측) |
| D62 | **약한 단서의 재확인 안내 — 상투구에서 카드 값 문장으로** | 2026-08-05 사용자 지적: "'아직 정하지 마라 — 다음 관측으로 재확인' 이렇게 성의없게 쓰지 말고 재확인 할 수 있는 방안을 좀 더 쉽고 읽는 사람이 알 수 있게". 관문별 고정 한 줄(recheck_hint)을 `recheck_lines()`로 재설계 — 규칙 셋: **① 그 카드의 값이 들어간다**(그룹 이름·효과 크기·n·판정 기준 — "「진청」과 「중청」의 후기 수 차이가 0.22로 작다(기준 0.30)") **② 시스템이 할 일과 사람이 할 일을 가른다**("다음 수집 뒤 리포트를 다시 뽑으면 자동 재판정된다"는 시스템 몫, "이 질문이 궁금하면 지목해 달라 — 단독 재검정하면 판정이 난다"는 사람 몫) **③ 종료 조건을 적는다**("그때도 이 수준이면 '차이 없음'으로 접는다" — 보류가 영원히 보류로 안 남게). 관문 판별은 fail_codes 코드 그대로(문구 매칭 금지 — PR #9), 관문 셋 이상은 상위 3개만(지면 보호). 상세 PDF도 줄 단위 렌더. E-MD-9 계약을 "'아직 정하지 마라'로 시작"에서 "판정을 미루되 확정 지시 없음 + 카드 값 + 종료 조건"으로 개정 | 관문별 고정 문구 유지(읽는 사람이 할 수 있는 일이 없다) / 자유 서술을 AI에 위임(약한 단서는 20~30장이라 매 리포트 비용이 크고, 관문별 재확인 경로는 정형적이라 엔진이 카드 값만 채우면 된다 — 액션 플랜(D64)과 달리 추론이 필요 없는 층이다) |
| D63 | **증분 append도 5,000행 단위로 끊는다** | 2026-08-05 실측: `proxy_cache` 증분 484,216행(어제 리포트의 배치 판정으로 3,518 → 486,017행 급증)을 `append_rows` **한 호출**로 보내자 구글이 500(Internal error)을 냈다. 전체 재구축(`rebuild_tab`)은 이미 5,000행 청크인데 증분 경로만 통짜였다. 증분도 같은 5,000행 단위로 끊는다. 청크 중간 실패 시 진행점은 안 움직이고(D46 ⓐ), 부분 반영은 다음 실행의 누적 대조(D46 ⓑ)가 잡아 `--repair`로 복구한다. 같은 날 대조가 실제로 잡은 것: DB에서 삭제된 옛 캐시 1,717행이 append-only 시트에 남아 시트가 DB보다 많았고(487,734 대 486,017), `--repair` 재구축으로 일치 확인 | 통짜 append 유지(대량 증분마다 500) / 실패 시 자동 재시도(부분 반영 위에 이어 붙이면 중복 — D46의 "어긋난 시트는 이어 붙일 지점이 성립하지 않는다"와 충돌) |
| D64 | **액션 플랜은 AI가 쓴다 — 엔진 템플릿은 폴백** *(구 D59 — merge 결정과 ID가 겹쳐 재부여, PR #12 리뷰)* | 2026-08-04 사용자가 원하는 액션 플랜의 실물 예시를 제시("다색 표기 → 옵션 통합 등록으로 전환해 하트·후기가 한 곳에 쌓이게 유도" 등 6건)하며 "내가 말한 게 딱 이런 거였어"로 확정. 이런 문장은 발견의 **기전을 읽고 추론**해야 나온다 — `{hi}/{lo}` 치환 템플릿(D56 4차 시도)이 낼 수 있는 수준이 아니다. 층을 가른다(기존 패턴 — 규칙표가 기본·AI가 예외): **엔진은 검정·판정·조판**(결정적·재현), **AI는 발견을 읽고 액션을 써서** `insight.py --ai-actions` JSON(`{주장 문장: [줄,…]}`)으로 넘긴다. 키가 주장 문장인 이유: 고정 시드라 같은 데이터에 같은 주장이 재현되므로 안정적이다. 형식: 첫 줄 = 평문 부연 · 이후 = `**[청중]**` 배지 + 구체 액션(그 발견의 값·기전 필수, 추측 허용). 절차는 **2회 실행**이 정석 — 1차로 발견을 얻고, 액션을 쓰고, 2차에 `--ai-actions`로 굽는다(intel-insight §③). 템플릿 액션은 오버라이드가 없을 때의 폴백으로만 남는다 | 템플릿 고도화 계속(4차 시도까지 갔지만 "당연한 말들" 평가를 못 벗어났다) / 액션을 insights 테이블에도 저장(PDF 전용이라 검수는 PDF 실물로 — verify-pdf-not-db) |
| D64 | **v2 이관이 sync_state 진행점을 새 번호로 번역한다 · 정본 v2 교체 완료** | 2026-08-05 정본 교체(사용자 지시) 중 발견. 증분 미러 키는 rowid인데 **삭제로 구멍 난 표(proxy_cache: 행 486,017 · 최대 rowid 500,835)는 이관에서 1..N으로 다시 매겨진다.** 옛 키(500,835)를 그대로 들고 가면 교체 후 새 판정 14,818행(새 번호 486,018~500,835 구간)이 "이미 미러됨"으로 읽혀 **조용히 건너뛰어진다** — D46이 막으려던 것과 같은 종류의 무소음 손실이고, 누적 대조가 잡긴 하지만 그때는 48만 행 재구축이 필요하다. 수정: `_copy_plain`에 `ORDER BY rowid`(순서 보존을 명시), 이관 끝에 **새 키 = 옛 키 이하의 행 수**로 번역(순서 보존 재번호라 성립. 실측 500,835 → 486,017, 완전 동기 상태에선 새 최대 rowid와 일치). **교체 절차 실행 기록**: 새 파일 검산 통과(행 수 6표 + 표본 400행 전 컬럼, 151.29→120.18MB — D45 실측보다 감소율이 낮은 건 proxy_cache 48.6만 행이 사전 압축 효과가 작은 표라서) → `intel-v1.bak.db`로 백업 → 교체 → `--verify-only` 재검산 통과 → 회귀 61+210 전부 통과 → 시트 미러 무결성 확인. 크론(랭킹 스냅샷)은 intel.db에 쓰지 않아 교체 창 충돌 없음 | 옛 키 유지(새 행 1.5만이 조용히 빠진다) / 진행점 0으로 리셋(48만 행 중복 append) / rowid를 명시 복사해 구멍 보존(WITHOUT ROWID 아닌 표에서 가능은 하나, 뷰 `_rowid`가 대리키 기반이라 v2 신규 행과 번호 체계가 갈라진다) |
| D12-b | 추가 검토 (차용 0건) | **productivity 플러그인**(이중 메모리·TASKS.md — 기존 메모리 체계와 동일 패턴이라 불요)과 **github/awesome-copilot**(Copilot 전용 형식, 접점 항목인 playwright-explore-website는 API 발견 지침이 없고 x-twitter-scraper는 공식 API 전제 — 우리 실측 어댑터보다 얕음)은 **차용 없이 종결**. 재검토 불요 | — |
| D65 | **DB 스키마 v3 — 관계 정식화 10항** | 2026-08-05 사용자 확정 설계(ERD 검토 후 프롬프트로 전달)를 그대로 구현. ① contexts.name CHECK — 접두사 4종(brand/market/ranking/adhoc) 고정, 별도 테이블 없이 제약만. 모르는 story는 적재가 `adhoc:`으로 접는다(안 접으면 CHECK가 적재를 죽인다) ② 상품 경량화 — attributes(JSON)·attributes_basis·first/last_seen_at·raw_extras 제거. 수집 JSON의 attributes는 적재 때 product_attributes로 직행한다(빼먹으면 reuse-attrs가 영영 빈다 — 구현 중 회귀로 실측) ③ 카테고리 계층화 — categories(name, parent self-FK, depth, UNIQUE(name,parent) + 최상위는 부분 인덱스 — SQLite UNIQUE의 NULL은 서로 달라서다) + product_categories N:M(source='platform'). 상품의 category_id 제거. AI 분류는 attr_base의 `ai_카테고리_*`(basis=llm). 뷰 products.category는 계층을 도로 편 파생 값(5단), 쓰기는 파이썬 assign_category()가 유일 통로(SQL 트리거는 경로 분해 불가) ④ brands.name → representative_name + brand_aliases(notation·site·source·verify_status). 미지 표기는 brand_key(D51) 일치 시 candidate 별명 자동 등록(resolve_brand), 음차 매칭 없음 ⑤ platforms.discovered_for_brand(쉼표 텍스트) → brand_platforms N:M ⑥ obs_attr — 시점별 비정형 지표(계약 필드 items[].obs_attrs), 고정 컬럼은 유지. prune과의 충돌은 ON DELETE CASCADE로 해소 ⑦ runs 정수 PK(id) + obs/variant_obs의 run_id 정식 FK(v2 run_ref는 rowid 참조라 FK 불가였다). 이관은 id=구 rowid로 매겨 run_ref 숫자가 그대로 FK 값이 된다 ⑧ proxy_defs·proxy_cache를 별도 data/proxy.db로(가장 빨리 자라는 표 — 실측 48.6만 행에 110MB) + defs↔cache FK ON DELETE CASCADE + rule 카드 본문을 defs.rules에 저장해 **lazy 판정**(collect가 캐시 miss를 즉석 판정·적재. 전량 선행은 proxy_auto --eager) ⑨ 옵션 first/last_seen_at 제거 ⑩ migrate_v3.py — 새 파일 검산(행 수 + 표본 400행 값 + run_id 연결 + integrity/fk check) 통과 시에만 `.v2-backup` 백업 후 자동 교체. 실측: 120.20MB → intel 19.11MB + proxy 110.17MB, 검산 전 표 OK, 회귀 61+222 통과. v1·v2 파일은 connect가 거부하고 이관 명령을 안내한다 | 항목별 개별 PR(스키마가 서로 얽혀 한 파일 DDL이 정본 — 커밋만 단계 분리) / proxy_cache를 정본에 유지(테이블 하나가 파일의 92%를 먹어 백업·미러·머지 전부를 무겁게 한다) / 카테고리 CHECK식 열거(계층은 제약으로 못 적는다) / 트리거에서 경로 분해(SQLite 트리거엔 루프·CTE가 없다 — 파이썬 단일 통로가 정직하다) |

| D66 | **vision 프록시도 lazy — 정의는 샘플 1~2장 접지로 즉시, 판정은 분석이 축을 요구할 때 협의 후 배치** | 2026-08-05 사용자 지시("vision도 lazy하게 하자" + "임의 1~2개 인스턴스만 보고 썸네일에서 뽑을 수 있는 프록시를 뽑아라"). 두 단계를 가른다: **정의**는 싸다 — image 카드는 분석 대상의 실물 썸네일 1~2장을 열람해 접지한 뒤 즉시 등록한다(그 몰에 애초에 없는 축을 정의하는 낭비 차단, 비용 ≈ 0). **판정**이 비싸다(인스턴스 수 비례) — 정의 시점엔 돌리지 않고, 분석이 그 축을 실제로 요구할 때 `collect()`의 `meta.proxies[].unjudged`로 미캐시가 드러나면 기존 협의 규칙(비용 보고 → 대규모면 범위 합의 → 배치 위임 → proxy-load)을 거쳐 돈다. rule lazy(D65-8)와 뜻이 같고("필요해질 때까지 안 돌린다") 실행 형태만 다르다 — 판정 엔진이 프로세스 밖(proxy-extractor 에이전트)이라 collect 안 즉석 판정이 불가능해서다. 샘플 열람은 정의의 접지이지 판정이 아니다 — 샘플에 값을 매겨 캐시에 넣지 않는다 | 정의 시점 전량 판정(비용이 정의를 막는다 — 축 하나 만들려고 수천 장 판정) / 샘플 1~2건 판정만으로 축 성립(그룹 비교는 값마다 n이 필요 — n=1로는 아무 검정도 성립하지 않는다) / 상품군 지식만으로 정의(실물에 없는 축을 만든다) |

| D67 | **정본을 Turso(libSQL) 클라우드로 — 연결 추상화 + 드라이버 호환 래퍼** | 2026-08-05 사용자 지시(팀원 여러 명이 하나의 클라우드 정본에 수집·조회, 로컬 개발은 기존 SQLite 유지). ① 모든 DB 열기가 `schema_v3.open_db()` 한 통로다 — `libsql://`이면 Turso, 아니면 로컬. 기본값은 INTEL_DB_URL > INTEL_DB > data/intel.db, **명시적 --db 인자가 항상 이긴다**(환경변수가 테스트의 임시 DB를 클라우드로 납치하면 안 된다) ② **드라이버 호환 래퍼가 이 층의 본체다** — libsql-experimental 실측(2026-08-05)에서 sqlite3과 4곳이 갈렸다: row_factory 없음(행이 튜플만 — 코드베이스 전체가 r["컬럼"] 이름 접근이라 그대로면 전면 파손) · 제약 위반이 IntegrityError가 아니라 **ValueError**(적재의 "중복 N건" 카운팅이 첫 중복에서 죽는다) · total_changes 없음 · lastrowid 불안정(_dim의 사전 id가 틀리면 조용히 엉뚱한 브랜드에 매달린다). LibsqlConnection/LibsqlCursor가 description 기반 Row 셈·예외 번역·last_insert_rowid() 대행으로 전부 흡수 — v3 뷰·INSTEAD OF 트리거가 libsql 위에서 그대로 도는 것까지 회귀로 고정(A7) ③ 프록시는 별도 Turso DB(PROXY_DB_URL·PROXY_DB_TOKEN). **Turso는 ATTACH 미지원** — sanity_check의 ATTACH를 별도 커넥션으로 교체(로컬도 같은 경로로 통일) ④ 이관은 tools/upload_to_turso.py — 스키마는 schema_v3가 정본, FK 순서 5,000행 배치, 비어 있지 않은 원격 거부, 행 수 검산이 통과 조건 ⑤ 공유 runs 기반 중복 수집 방지 `check-run`(같은 site·story·target 24시간 이내 → exit 1, --force로 무시) — 시트 우회 조회(D32)는 공유 DB에서 불필요해진다 ⑥ 시트 미러는 **쓰기 토큰**이 필요하다(sync_state 진행점을 DB에 쓴다) — 원 설계의 "읽기 전용 토큰" 지정은 성립하지 않아 기각. 읽기 전용 토큰은 intel-query(챗 조회) 전용. 드라이버는 py3.10+에서만 설치된다(시스템 3.9 빌드 실패 실측 — uv 3.12 안내, docs/TURSO-SETUP.md). 클라우드 프로비저닝(turso auth·db create·토큰)은 인터랙티브라 사람 몫 | libsql.connect 직접 교체(4개 비호환이 전부 조용한 런타임 파손) / 코드베이스를 튜플 접근으로 개서(수백 곳) / 프록시를 정본 DB에 합쳐 ATTACH 회피(D65-8 분리 결정 번복) / 시트 미러 읽기 전용 토큰(sync_state 쓰기와 모순) |
| D68 | **상품 정적 속성 이력 + 프록시 재판정 체인** | 2026-08-05 사용자 지시(상품명·이미지·카테고리가 조용히 바뀌는데 이전 값이 사라진다 — 리네이밍·재분류가 성과에 미치는 영향을 물을 수 없었다). ① 이력 3표 — `product_history`(intel.db: name·brand·category·url·image_url 전이, run_id 연결)·`attr_history`(intel.db: AI 속성 전이, 뷰 트리거가 캡처 — 모든 쓰기 경로가 뷰를 지나므로 놓치는 경로가 없다)·`proxy_history`(proxy.db: 판정 전이, **new_value NULL = 재판정 대기**). 전부 append-only, 조회는 `product_changes`·`attr_changes` 뷰((site, product_id)로 묻는다) ② product 이력은 **Python(`_upsert_product`) 캡처** — 트리거 안에서 다른 DB(proxy.db)를 못 만지므로 무효화 체인이 Python에 있어야 하고, 감지도 같은 자리에 둔다. merge 경로는 이력을 남기지 않는다(문서화된 갭 — merge는 팀원 DB 합류라 "변경"이 아니라 "병합"이다) ③ **재판정 체인**: 이름/이미지 변경 감지 → 그 재료(material)의 proxy_cache 행을 `invalidate_material_proxies()`가 대기 이력(old_value 보존, new_value NULL)으로 옮기고 캐시에서 삭제 → 다음 lazy 판정(intel_data)·proxy-load(intel_db)가 `record_proxy_transition()`으로 대기 행을 완성. 강제 즉시 재판정은 안 한다 — lazy 원칙(D65-8·D66)대로 분석이 요구할 때 판정된다 ④ 값→NULL 전이는 기록되지 않는다 — 이 upsert는 빈 값으로 기존 값을 지우지 않으므로(COALESCE 규칙) 저장소에 그런 전이가 실제로 일어나지 않는다(스펙 원문 "값→NULL도 기록"과의 편차 — 일어나지 않는 전이를 기록하면 거짓 이력이다) ⑤ 카테고리 변경은 "기존 platform 매핑이 있는 상품에 처음 보는 리프가 왔다"를 사건으로 기록(N:M append라 뷰 값이 안 바뀔 수 있다) ⑥ `connect()`가 기존 v3 DB에 빠진 표를 보충한다 — **마커(가장 최근에 SCHEMA_V3에 추가된 표, 지금은 attr_history)가 없을 때만 1회** SCHEMA_V3를 실행(라이브 정본·Turso가 이관 없이 코드 갱신만으로 이력 표를 얻되, 가벼운 조회까지 매번 DDL 27줄을 Turso로 왕복시키지 않는다 — PR #14 리뷰. proxy_connect도 proxy_history 마커로 동일). 표를 또 추가하면 마커도 그 표로 바꾼다. 시트 미러는 product_changes만 증분 append(attr_history는 규모 대비 수요 미확인 — 요구가 생기면 추가) | 트리거로 product 이력 캡처(다른 DB 접근 불가라 무효화 체인이 갈라진다) / 변경 즉시 강제 재판정(lazy 원칙 위반·비용 폭증) / 값→NULL 기록(일어나지 않는 전이) / proxy_history를 intel.db에(FK가 proxy_defs를 가리켜 분리 원칙 위반) |
| D77 | **상품명 뱃지 보존(name_badge) + 겹침 분석 정규화·검산 의무화** | 2026-08-07 사용자 지시("겹침 분석 수정 — REORDER 말고 다른 프리픽스도 robust하게" + "리오더 정보 보존 방식 고안"). 배경: Aside 즉석 겹침 분석이 상품명 원문 비교로 자사몰 `[REORDER]` 상품 9개를 무신사의 같은 상품과 갈라 셌고, 표의 한 칸(2개 플랫폼)에 다른 칸 값이 잘못 들어간 채 나갔다(분해 합 295 ≠ 전체 505 — 검산 부재). ① 매칭은 기존 `match_key()`가 정본(모든 대괄호·소괄호 제거, 시즌 표기만 키에 유지) — 파이프라인 쌍체 비교는 이미 이 규칙이라 영향 없었고, **즉석 분석이 규칙 밖**이었다. intel-query SKILL.md에 match_key JS 이식 + "분해 합 = 전체" 검산 의무를 명문화 ② 뱃지 보존: `name_badges()` — **이름 맨 앞의 대괄호 토큰만** 뱃지다([REORDER]·[단독]·[기획]). 전 위치로 뽑는 초안은 실측 기각 — 데님 전수조사에서 색상('[블랙]')·품번('[LC262PT04BL]')·옵션('[기장선택]') 2,399건이 뱃지로 잘못 잡혀 원복했다(접두사 한정 재백필 912상품, REORDER 10). 시즌 표기는 제외 ③ 적재 시 attr_base에 attr_name='name_badge', basis='name'으로 직행 — 이름이 바뀌면 D71 무효화가 지우고 다음 적재가 재추출한다(별도 무효화 코드 불요). 여럿이면 정렬 쉼표 결합 ④ 원문(products.name)은 불변 — 뱃지 걷기는 분석 시점 정규화일 뿐 저장 데이터는 그대로다. 회귀 E-DB-44a~f | 전 위치 대괄호 추출(색상·품번 오염 — 실측 기각) / name_key 컬럼 추가(스키마 변경 대비 이득 없음 — REPL은 JS로 즉석 정규화 가능) / 뱃지 제거 후 별도 원문 백업(원문은 이미 name에 있다 — 이중 저장) |
| D76 | **누적판매 구간 표기를 purchase_count 하한 정수로 적재** | 2026-08-07 사용자 지시("이 텍스트 표시를 그냥 purchase_count로 쓰기로 했어"). 배경: 세 플랫폼 모두 수치형 판매량이 없어 purchase_count가 100% 결측 — 있는 것은 무신사 구간 표기("1.8천 개 이상")뿐. D48("구간 하한을 정수 칸에 넣지 않는다 — 오차 무한·평균/비율 오염")을 **purchase_count에 한해** 뒤집는다: 적재 시 수치가 없고 display가 있으면 `band_floor()`로 하한을 purchase_count에 넣는다. 수치가 있으면 수치 우선, 못 읽는 표기는 NULL 유지(추정 금지 원칙은 그대로 — 하한은 사이트가 밝힌 값이지 역산이 아니다). **원문은 purchase_count_display에 보존** — 하한임을 언제든 되짚는다. view/like는 예외 밖(구간 축 유지). 함정(하한≠실값·평균 오염)은 사용자가 인지하고 수용. 기존 Turso 정본 소급: 표기 8종·144행 전부 하한 적재(2026-08-07 실측, 못 읽은 표기 0). 회귀 E-DB-43a~f | D48 전면 유지(수치 축이 영영 빈다 — 사용자가 기각) / 전 지표 확장(view/like — 사용자 지시 범위 밖) / display 컬럼 제거(원문 소실 — 하한임을 못 되짚는다) |
| D75 | **prune 자동화 + Turso 정본 직접 솎기** | 2026-08-07 사용자 지시("프루닝 자동으로 되게 해줘. turso db에서도"). ① prune.py가 `default_db_target()`(INTEL_DB_URL > INTEL_DB > data/intel.db)을 기본으로 받고 `schema_v3.open_db()` 단일 통로로 연다 — D72가 분석 스크립트에 한 통일을 prune에도 적용(전에는 sqlite3.connect 직결이라 Turso 정본을 솎을 수 없었다) ② 임시 테이블(_prune_keep) 제거 — 판정 전체를 SELECT 한 문장으로 내리고 id·사유를 클라이언트에서 접는다. 원격 커넥션의 세션 상태(temp table 수명)에 기대지 않아 로컬·Turso가 같은 경로를 탄다. plan()은 (counts, total, drop_ids)를 반환, apply_prune(conn, drop_ids)는 400개 청크 DELETE — obs_attr를 먼저 명시 삭제(정본)하고 FK CASCADE는 보험으로 둔다(원격 pragma 동작 차이 대비) ③ VACUUM·파일 크기 출력은 로컬 한정(원격은 서버가 공간 관리) ④ 자동화는 crontab 주간(일요일 04:10, `--cron`이 등록 줄 출력 — snap_ranking_any 패턴). env 소싱으로 Turso 정본 대상, 로그는 data/prune-cron.log. 스냅샷 잡(:07·:37)과 분이 겹치지 않는다 ⑤ obs_base 삭제는 mirror_dirty(D72)에 잡혀 다음 동기화 때 시트 탭이 재구축된다 — 솎기 결과가 시트에 따라간다. 원격 예행 실측(2026-08-07): 관측 15,799행 전부 최근 30일 이내 → 지움 0 (규칙 ①대로) | 적재 때마다 인라인 솎기(적재 지연 + 팀원 수집 경로마다 중복 실행) / launchd(기존 잡이 crontab — 채널 분산) / 임시 테이블 유지(원격 세션 상태 의존 검증 불가) / Turso는 로컬 사본 솎기 후 --force 재업로드(정본 전체 교체는 D71 --force 의미상 파괴 반경이 훨씬 크다) |
| D74 | **분석 단이 EDA 산출물을 입력으로 받는다 (--signals)** | 2026-08-07 사용자 지시("EDA 수행한 걸 받아오는 게 낫지 않아?"). 전에는 insight→analyze가 EDA를 무조건 내부 재수행해서 ① 사용자에게 보여준 EDA와 분석이 쓴 EDA가 다른 실행이었고(재현성) ② Turso 직접 연결에서 전체 데이터 fetch가 한 번 더 나갔다. eda.run() 출력에 requested_contexts를 박고, analyze.reuse_or_run_eda()가 **요청 문맥이 정확히 같을 때만** 재사용 — 다르면 경고 후 자동 재수행(다른 요청의 EDA로 분석하면 근거가 갈라진다 — 그걸 막자고 만든 기능이 그걸 만들면 안 된다). insight --signals → build → analyze로 관통. auto-proxy가 그 사이 새 판정을 만들 수 있으나 EDA 검사는 데이터 품질 수준이라 허용. 회귀 E-SG-1~4. 함께: intel-query SKILL.md에 --ai-notes 방법론 조정(6기법 id 명시)·분석 소요 실측(관측 1,070건 29분 — Turso 왕복 구조) 문서화, v1.3.0 | EDA 파일을 신뢰하고 무검증 재사용(다른 요청의 EDA로 분석할 위험) / 항상 재수행 유지(재현성 단절·왕복 낭비) |
| D73 | **Turso 토큰 읽기/쓰기 분리 + 고아 참조 정리 + 브랜드 한/영 병합** | 2026-08-07 사용자 지시. ① 토큰 분리: env에 INTEL_DB_TOKEN(**읽기 전용**, 팀 배포용)과 INTEL_DB_WRITE_TOKEN(쓰기, 파이프라인 머신 전용)을 나눠 두고, `schema_v3.pick_token()`이 인자 > WRITE > 읽기 순으로 고른다 — 읽기 토큰만 가진 환경은 자동 읽기 전용, 쓰기는 DB 단에서 거부. intel-query SKILL.md의 하드코딩 쓰기 토큰 제거(자체 안전 규칙 위반이었다 — PR #21 리뷰) → 편집 시 Bash로 env에서 읽어 REPL에 주입. docs/env.example 추가 — 토큰 값은 레포 밖, 온보딩 때 별도 전달 ② `intel_db clean-orphans`: 대량 삭제가 남긴 고아 참조 행(관측이 참조하지 않는 runs·contexts·brands) 정리. 2026-08-07 실측 고아: runs 1,533 · brands 1,585 · contexts 14 — runs 고아는 시트 runs 탭의 죽은 이력 + check-run 오판(재수집 차단)의 원인. **runs는 최근 48시간 보존** — 전건 중복 스킵 수집(관측 0건)의 정상 이력을 지우면 중복 방지가 뚫린다. 회귀 E-OR-1~5 ③ 브랜드 병합(brand-split-diagnosis 방안 2+3): 2000아카이브스가 brands 3행(238 한글 · 2441 '2000 Archives' 자사몰 148상품 · 2440 고아)으로 분열 — product_base.brand_id를 238로 통일, 2440·2441 삭제, brand_aliases에 영문 2종을 source='manual'·verify_status='confirmed'로 등록. resolve_brand ②(별명 경로)가 이미 구현돼 있어 **향후 수집도 자동으로 238에 붙는다**(①의 대표명 완전일치 행이 사라져야 별명 경로가 작동한다 — 그래서 병합 없이 별명만 넣는 방안 1은 재발 방지가 안 된다). 음차 자동 매칭 금지(D51)는 유지 — 수동 확정(manual/confirmed)만 허용 | 별명만 등록(방안 1 — resolve_brand ①이 선점해 재발 방지 실패) / 트리거 내 정규화(방안 5 — SQLite 한글 처리 한계) / upload_to_turso resolve 경유(방안 4 — 재이관 시에만 유효, 보류) / runs 고아 전건 삭제(check-run 중복 방지 파괴) |
| D72 | **시트 미러 편집·삭제 감지 + Aside(intel-query) 분석 확장** | 2026-08-07 사용자 지시. ① 증분 탭(INCR_TABLES)은 rowid 진행점 뒤의 새 행만 append해서 **이미 올라간 행의 수정(UPDATE)·삭제(DELETE)가 시트에 영영 안 반영됐다** — intel-query 편집 기능이 생기며 append-only 가정이 깨졌다. 감지는 2단: (1) **mirror_dirty 트리거 카운터** — 원본 4종(obs_base·variant_obs_base·proxy_cache·product_history)의 AFTER UPDATE/DELETE 트리거가 센다. 편집·삭제 모두, 삭제+신규가 상쇄돼 행 수가 안 변한 경우까지 잡는다(PR #21 리뷰 엣지). INSERT는 안 센다 — append가 정상 경로다 (2) **행 수 대조 보조** — DB < 시트면 트리거 이전 변경을 받친다. 걸리면 rebuild_tab(5,000행 청킹)으로 전체 재구축 + 진행점을 최대 rowid로 리셋(전량 삭제 시 maxr None→0 가드), 카운터는 본 만큼 차감. 한계: INSERT OR REPLACE 내부 삭제는 재귀 트리거 off라 카운터 밖 — audit+--repair가 받친다. mirror_dirty는 SCHEMA_V3 추가·마커 범프로 기존 로컬·Turso DB에 무이관 수령(D68 방식). 회귀 E-SH-1~9 ② eda/analyze/insight/sync_sheets의 `--db` 기본값을 리터럴 data/intel.db에서 `default_db_target()`(INTEL_DB_URL > INTEL_DB > data/intel.db)으로 통일 — intel_db.py는 이미 이 규칙이었고 분석 스크립트만 빠져 있어 환경변수를 설정해도 로컬 DB를 봤다. insight의 auto-proxy 스크래치 JSON은 libsql:// URL의 dirname이 열 수 없는 경로가 되므로 Turso일 때 data/에 둔다 ③ intel-query(Aside) 스킬에 분석·리포트 섹션 추가 — EDA→insight.py(분석 내장)→PDF 안내, Bash→Python 패턴 유지, 상세는 references 포인터만 | 행 수 비교 단독(편집·상쇄 미감지 — 사용자·PR #21 리뷰 지적으로 기각) / 매 동기화 전체 재구축(평시 비용 낭비) / 시트 행별 대조(API 왕복 과다) / Python 1만줄 JS 포팅(중복 정본) |
| D71 | **재료 변경 무효화를 attr_base까지 확장 + prune 전 지표 감시 + upload --force 재정의** | 2026-08-06 사용자 지시(미완 항목 일괄 처리). ① 이름/이미지 변경 시 프록시 캐시만 무효화하던 것(D68)을 **같은 재료에서 나온 AI 속성 판정(attr_base, basis=재료명)까지** 확장 — 이름에서 뽑은 핏은 이름이 바뀌면 근거가 사라진 판정이다. attr_history에 무효화 행(new_value NULL)을 남기고 지운다. D69로 프록시가 본 DB에 있어 전부 같은 커넥션·같은 트랜잭션이다 ② prune의 변화 감지를 가격·순위·품절 3종에서 **관측 전 지표 + obs_attr(비정형 묶음 서명)**로 확장. CASE 분기는 자주 바뀌는 순서(판매가·할인율 → 순위·품절 → 후기·하트·구매 → 조회·보는중·구매중 → 정가·평점 → 비정형) — 앞에서 change면 뒤는 평가하지 않는다. prune 커넥션에 FK pragma 추가(없으면 obs_attr CASCADE가 조용히 안 돌아 고아 행) ③ upload_to_turso: 뷰·트리거를 **데이터 INSERT 뒤로**(INSTEAD OF 트리거가 미리 있으면 INSERT를 가로챈다), --force는 "이어 붙이기"에서 **"원격 표 전부 DROP 후 이 로컬본으로 교체"**로 재정의(이어 붙이면 중복 위에 쌓여 검산이 영영 안 맞는다) ④ **display 컬럼 3종(view/purchase/like_count_display) 제거안은 기각** — 실DB에 display만 있는 구간 표기 관측 5,722건(view 3,489 · purchase 2,233, "1.8만 회 이상" 류). 구간 하한을 정수 칸에 넣는 것은 D48이 금지한 동작(오차 무한·평균/비율 오염)이고, 순위 기반 분석 축(view_band 등)이 이 원문에서 나온다. 사용자 확인 후 유지 결정 | display를 정수로 파싱해 컬럼 제거(D48 위반) / obs_attr로 이관 후 제거(정보는 보존되나 마이그레이션·밴드 축 개편 대비 이득 없음) / --force를 TRUNCATE 의미 없이 유지(중복 누적 버그 존치) |


## §2 저장 계층 — 로컬 DB 정본

### 2-1 스키마 (SQLite: `data/intel.db`)

DB를 읽고 쓰는 것은 **오케스트레이터 스킬 하나뿐이다**(§5 역할 경계). 그래서 도구도
`skills/commerce-intel/scripts/intel_db.py`에 둔다 — 플랫폼 스킬은 DB를 모른다.
DB 파일 경로는 **작업 폴더 기준 `data/intel.db`**이고 `INTEL_DB`로 덮을 수 있다.


정적(비싼 판단·거의 안 변함)과 시변(관측마다 변함)을 **테이블로 분리**한다.
수집 JSON(기존 데이터 계약, SPEC v16 §6)은 그대로 유지되고, DB 적재는 그 뒤에 온다 —
**수집 → raw JSON → 검증 → DB 적재 → 시트 미러** 순서다.

**스키마 v3다 (D65, 2026-08-05).** 옛 이름은 전부 뷰이고(D45) 물리 저장은 정수
사전·대리키(`schema_v3.py`)다. 상세 계약은 `references/db-contract.md` §2가 정본 —
여기는 지도만 남긴다.

| 테이블 | 무엇 | 키 | 갱신 방식 |
|---|---|---|---|
| `products` | 정적 속성: 이름·URL·이미지·브랜드(대표명)·카테고리(계층 파생) | `(site, product_id)` | upsert. `static_verified_at`으로 TTL 관리 |
| `product_attributes` | 동적 속성(핏·컬러·시즌·`ai_카테고리_*`) — 축을 행으로 (D35) | `(site, product_id, attr_name)` | upsert |
| `categories` + `product_categories` | 계층 카테고리(self-FK·depth) + 상품 N:M 매핑(source='platform') (D65-3) | category_id / (pk, category_id, source) | 적재가 자동 |
| `brands` + `brand_aliases` + `brand_platforms` | 대표명 + 플랫폼별 표기 별명 + 입점 N:M (D65-4·5) | brand_id 계열 | 적재·scout가 자동 |
| `observations` | 시변 값: 가격·할인·후기·평점·하트·판매·조회·실시간 지표·품절·순위. `context`는 접두사 4종 CHECK(D65-1), `run_id`는 runs(id) 정식 FK(D65-7) | `(site, product_id, observed_at, context)` | **append only. 갱신·삭제하지 않는다** |
| `obs_attr` | 시점별 비정형 지표(SNS 언급수 등 계약 외 필드) (D65-6) | `(obs_id, attr_name)` | upsert |
| `platforms` | 입점처 리서치 결과: 플랫폼명·URL·엔진·정찰 JSON·스킬 상태 | `platform_key` | upsert |
| `runs` | 수집 실행 이력: story·target·건수·source_total·incomplete·notes·raw 파일 경로. 정수 PK `id`, run_id TEXT UNIQUE | `id` | append |
| `sync_state` | 시트 미러 진행 상태(테이블별 마지막 동기 지점) | 테이블명 | 내부용 |
| `proxy_defs` + `proxy_cache` | **별도 `data/proxy.db`** (D65-8) — 정의(rule 본문 포함, lazy 판정)와 판정 캐시(FK CASCADE) | proxy_name 계열 | proxy-load·lazy |
| `product_history` + `attr_history` | 정적 속성·AI 속성 변경 이력 (D68) — 조회는 `product_changes`·`attr_changes` 뷰 | id | **append only** |
| `proxy_history` | 프록시 판정 전이 이력 (proxy.db, D68) — `new_value NULL`은 재판정 대기 | id | append(대기 행은 재판정이 완성) |

- `observations.context`는 관측이 나온 화면이다(`ranking:바지` · `brand:인사일런스` ·
  `market:데님팬츠(남성)`). **랭킹에서 온 `viewers_now`를 다른 문맥에 섞지 않는다** —
  SPEC v16의 "출처가 섞이면 일관성이 깨진다" 원칙을 DB에서도 유지한다.
- 미노출 값은 `NULL`이다. 0과 구분한다(기존 원칙 그대로).
- 기존 축적 스냅샷 47개+는 `intel_db.py import`로 소급 적재할 수 있다 — 과거를
  버리지 않는다.

### 2-2 재사용 판정 (요구 1-1)

수집을 시작하기 전에 DB에 물어본다. 판정은 `intel_db.py`가 하고, 기준은 이렇다.

| 대상 | 규칙 |
|---|---|
| **정적 속성** | `static_verified_at`이 **90일(D7)** 이내면 재확인하지 않는다. 특히 이미지 판단이 필요한 핏 분류는 DB 값을 그대로 쓴다. 90일이 지났으면 다음 수집에서 재확인 대상으로 보고한다 |
| **시변 값** | 같은 `(site, context)`의 최신 관측이 **사이트 원본 갱신 주기(D8)** 이내면 수집을 생략하고 기존 관측을 쓴다. 주기는 플랫폼 스킬에 명시된 값(무신사 실시간 랭킹 30분, 29CM 랭킹 1시간)이고, 명시가 없으면 24시간이다 |
| **팀원 수집분** (D32) | `--team`을 주면 로컬에 신선한 관측이 **없을 때만** 시트 `runs` 탭을 추가로 본다. 같은 `(site, context)`를 팀원이 위 시변 규칙의 주기 안에 이미 수집했으면 중복이라고 알린다. 주기 밖이면 이력만 보고하고 통과시킨다 |
| **사용자 명시 재수집** | "새로 받아", "다시 조사해"라고 하면 두 규칙 모두 무시하고 재수집한다 |

스킵했으면 **스킵했다는 사실과 기존 관측 시각을 사용자에게 보고한다** — 조용히
옛 데이터를 새것처럼 내지 않는다.

팀 조회에는 한계가 둘 있고, 둘 다 사용자에게 그대로 말한다(D32):

- 시트는 **완료된 수집만** 안다. `runs` 행은 적재 뒤에 생기고 그 다음 미러 때 올라가므로,
  "지금 팀원이 수집 중"인 건은 잡히지 않는다 — 중복 창이 미러 주기만큼 남는다.
- 조회에 실패해도(키 없음·쿼터·네트워크) **수집을 막지 않는다.** `consulted: false`로
  떨어뜨리고 로컬 판정만으로 진행하되, 판정 근거에 "팀 커버리지 미확인"을 남긴다.

## §3 구글 시트 미러

- 방향은 **로컬 → 시트 단방향**이다. 시트는 사람이 보고·공유하는 창구다.
  시트에서 손으로 고친 값은 정본에 반영되지 않는다(다음 미러에서 덮일 수 있다) —
  이 사실을 시트 첫 탭 안내문에 명시한다.
- **읽기는 딱 한 곳뿐이다** — 팀 커버리지 판정(D32)이 `runs` 탭을 조회한다.
  이건 단방향 원칙의 예외가 아니다: 읽은 값을 정본에 쓰지 않고 "이미 수집됐나"
  판정에만 쓴다. 시트로 정본을 갱신하는 경로는 여전히 없다.
- 스프레드시트 1개, 탭 = 테이블(`products` · `observations` · `platforms` · `runs`).
- `products`·`platforms`·`runs`는 전체 다시 쓰기, `observations`는 **증분 append**
  (`sync_state` 기준). 증분도 **5,000행 단위로 끊어 보낸다**(D63 — 통짜 대량 append는
  구글이 500을 낸다). 시트 셀 한도(1,000만)에 접근하면 오래된 관측 탭을 연도별로
  분리한다 — 정본은 로컬이므로 시트 분리가 데이터 손실이 아니다.
- 인증은 서비스 계정 JSON 키다. 설정 절차는 `docs/SHEETS-SETUP.md`. 키 파일 경로는
  환경변수 `INTEL_SHEETS_CREDENTIALS`(기본 `~/.config/intel/service-account.json`),
  스프레드시트 ID는 `data/sheets_config.json`에 둔다. **키 파일은 git에 넣지 않는다.**
- 시트 인증이 없거나 실패해도 **수집·DB 적재는 진행한다.** 미러만 밀린 상태가 되고,
  다음 성공 시 `sync_state`부터 따라잡는다. 실패 사실은 사용자에게 보고한다.
- **쓰는 사람은 1명이다**(D31 전원 풀 배포의 귀결). `products`·`runs` 등은 전체 다시
  쓰기이므로, 팀원이 편집자 권한으로 미러를 돌리면 자기 로컬 DB 내용으로 팀 축적을
  덮고 자기 DB에 없는 탭은 지운다. **팀원 서비스 계정은 뷰어**로 준다 — 그래야
  `check --team`(D32) 읽기는 되고 덮어쓰기는 불가능하다. 팀원 데이터는 시트가 아니라
  `.db` 파일로 받아 `merge`한다. 절차는 `docs/SHEETS-SETUP.md` §5.

## §4 분석 리포트 — 가설 도출 도구

리포트는 요약이 아니라 **가설 도출 도구**다(요구 2). 생성물은 로컬 단일 HTML
(외부 네트워크 불필요)이고, 다음을 갖춘다.

**리포트는 모듈의 조합이다**(D25). 아래 요소는 모듈로 존재하고 사용자가 고른 것만 한
리포트로 조립된다 — 필요한 것을 고르는 것이지 종류를 고르는 것이 아니다.

| 모듈 | 내용 | 나오는 조건 |
|---|---|---|
| `kpi` | n·중위 판매가·중위 할인율·품절·평균 옵션 품절률 | 항상 |
| `scatter` | 축 선택형 산점도 (X·Y·**색 기준**·로그) | 수치 축 1개 이상 |
| `dist` | 히스토그램(중위선·구간 하한 명시) | 수치 축 1개 이상 |
| `group` | 그룹별 박스 플롯 — **심슨의 역설 확인 장치** | 수치 축 1개 이상 |
| `variants` | 사이즈별 재고 (§6-6) | 옵션 관측 있음 |
| `timeseries` | 시계열 추이 (D24) | 시점 2개 이상 |
| `events` | 가격 변경 사건과 관측 창 증분 | 변경 감지분 있음 |
| `linesheet` | 플랫폼 합집합 표 (입점·가격 차이·묶어 보기) | 플랫폼 2개 이상 |
| `table` | 상품 표 (전 행 유지) | 항상 |

- 기본은 전 모듈이다. 못 만든 모듈은 이유와 함께 리포트에 적는다.
- `linesheet` 모듈의 상품 매칭은 **스토리 리포트와 같은 규칙 하나**를 쓴다
  (`build_report.match_key` — 정규화 상품명 완전일치). 두 곳에 규칙을 적으면 같은
  데이터에서 다른 합집합이 나온다.
- **색만으로 계열을 구분하지 않는다** — 색 기준 축을 고르면 색과 **마커 모양**이 함께
  바뀐다. 색은 점이 많은 값 5개에 주고 나머지는 회색 + 종수를 적는다(가나다순으로 자르면
  어느 값이 색을 받을지가 이름 순서로 정해진다).

1. **변인통제 패널** — 카테고리·핏·브랜드·가격대·품절·플랫폼을 조건으로 고정/제외.
   모든 차트가 같은 필터를 공유한다. 현재 조건과 표본 수(n)가 항상 화면에 보인다 —
   **n이 작아진 채 결론을 읽는 사고를 막는다.**
2. **축 선택형 산점도** — X·Y를 수집된 수치 필드 중에서 고른다(가격·할인율·하트·
   후기 수·평점·누적판매·조회·실시간 지표·순위 변동 등). 로그 스케일 토글, 점 호버에
   상품 정보, 클릭하면 상품 URL.
3. **프록시별 적합 그래프** — 축 형태는 열려 있다(D3). 분포 비교(히스토그램·박스),
   시계열(축적 관측이 있는 상품의 지표 추이 — 순위 반전·순위권 밖 선 끊김·이동평균),
   순위 추이 등 feature 속성에 맞는 형태를 쓴다. 산점도는 방법 중 하나다.
   **시계열 뷰는 범용 대시보드에 상시 붙는다**(시점 2개 이상이면 자동 표시, D24).
4. **정직성 규칙** — 이 도구가 보여주는 것은 **가격-반응의 상관 탐색이지 엄밀한
   가격 탄력성이 아니다.** 리포트에 명시한다. 구간 표기(`300회 이상`)에서 온 값은
   축으로 쓰지 않고, 축약 파싱 값은 각주로 밝힌다(SPEC v16 §6 오차 규칙 준수).
   미노출(`NULL`)은 0으로 그리지 않는다 — 그 점은 차트에서 뺀 뒤 제외 건수를 밝힌다.

## §5 스킬 구조 (D5)

```
skills/
├── commerce-intel/        오케스트레이터 — 요청 해석, DB·시트·재사용 판정,
│                          분석·리포트, 공통 규칙(속도·차단·추정 금지)의 정본
├── platform-musinsa/      전용 플랫폼 스킬 (기존 어댑터 실측 내용 승계)
├── platform-29cm/         전용 플랫폼 스킬
├── platform-ownmall/      전용 플랫폼 스킬 (자사몰 — 엔진 무관, §5-1)
├── platform-generic/      처음 접하는 플랫폼용 — 정찰 절차 + 공통 함정 카탈로그
└── platform-skill-maker/  정찰 결과 → platform-* 스킬 초안 생성
.claude/agents/
└── channel-scout.md       입점처 리서치 서브 에이전트
```

- **역할 경계**: 플랫폼 스킬은 "그 사이트에서 데이터 계약 JSON을 만드는 방법"만 안다.
  DB 적재·시트·분석·리포트는 전부 오케스트레이터의 것이다. 플랫폼 스킬에는
  분석 로직을 넣지 않는다.
- **channel-scout**: 브랜드의 입점처를 발견하고(검색·공식몰·입점 목록), 새 플랫폼이면
  기술 정찰(API 유무·렌더링 방식·총계 노출 여부·robots)까지 해서 보고서를 낸다.
  결과는 `platforms` 테이블에 적재된다.
- **skill-maker**: 정찰 보고서를 입력으로 `platform-<이름>/` 스킬 **초안**을 만든다(D6).
  초안에는 실측으로 검증된 것과 미검증 추정이 **명시적으로 구분**되어야 한다 —
  미검증 항목을 사실처럼 적으면 이 프로젝트의 제1원칙 위반이다. 초안 상태는
  `platforms.skill_status = draft`로 기록되고, 사용자가 다듬어 확정하면 `ready`가 된다.
- **`commerce-research/`는 제거한다**(D10). 트리거 설명이 오케스트레이터와 거의 그대로
  겹쳐 **어느 스킬이 뜰지 모호해지는 것**이 실제 해악이고, 원본 보존은 git 이력이
  이미 하고 있다. **순서를 지킨다** — 어댑터를 `platform-*`로, 스크립트를
  `commerce-intel/scripts/`로 옮긴 뒤에 삭제한다.
- `docs/SPEC.md`·`docs/TEST-CASES.md`는 **근거 아카이브**로 남긴다. 런타임에 읽히지
  않고(스킬이 아니다) 트리거에도 관여하지 않는다. 실측 근거(§7 함정 카탈로그 등)를
  다시 얻으려면 브라우저 실측을 되풀이해야 하므로 지우지 않는다.
  **intel 작업의 판단 기준은 이 문서(SPEC-INTEL)다.**

### 5-1 자사몰은 엔진에 매이지 않는다 (D9)

자사몰의 **70~80%가 Cafe24지만 예외가 있다**(Shopify 등, 2026-07-31 사용자 지적).
엔진 이름을 스킬 이름에 박으면 예외를 만났을 때 스킬 자체가 무력해진다.

```
platform-ownmall/
├── SKILL.md                  엔진 무관 절차 — 도메인 확정 → 엔진 판별 → 분기 → 수집
└── references/
    ├── engine-detect.md      판별식 카탈로그 (cafe24 · shopify · 고도몰 · 임웹 · 자체구축)
    ├── engine-cafe24.md      ← 기존 cafe24.md 승계 (실측 완료)
    ├── engine-shopify.md     신규 — 실측 전까지 전 항목 「미검증」 표기
    └── engine-unknown.md     판별 실패·미지원 → platform-generic 정찰로 인계하는 절차
```

**엔진과 무관한 것**(SKILL.md가 갖는다) — 도메인을 지어내지 않고 검색·진위 확인·
사용자 선택으로 확정한다 · 브랜드 페이지 하나로 끝나지 않으니 카테고리를 전수 순회한다 ·
반응 지표(후기·평점·좋아요)가 아예 없을 수 있고 그때는 `null`이며 우회하지 않는다 ·
화면 총계가 없을 수 있고 그때 `source_total`은 `null`이며 완전성 근거를 따로 세운다.

**엔진에 매인 것**(`references/engine-*.md`가 갖는다) — 목록 URL 형식 · 페이지 넘김
종료 신호 · 품절 판정 위치 · 카테고리 누락 같은 실측 수치.
**Cafe24에서 잰 값을 다른 엔진에 옮겨 적지 않는다**(`ALL` 23% 누락은 Cafe24 실측이다).

미지원 엔진을 만나도 **비범위로 거절하지 않는다** — `platform-generic` 정찰로 넘겨
1회성 수집을 시도하고, 반복 대상이면 `platform-skill-maker`로 `engine-*.md`를 늘린다.

## §6 옵션(컬러·사이즈) 정교화 — 라인시트 심화 (2026-07-31 신설)

라인시트를 상품 단위에서 **옵션(SKU) 단위**로 내린다. 얻으려는 것은 옵션 구성 ·
옵션별 재고 · 옵션별 판매수량이다.

### 6-1 재고 프로브 계층 (D18로 재개정 — 2026-07-31)

**아래 순서를 지킨다 — 허용의 문제가 아니라 비용의 문제다. 위 단계로 얻히면
아래로 내려가지 않는다.**

| 층 | 방법 | 상태 변경 |
|---|---|---|
| **L1 무료** | 이미 응답에 있는 옵션별 품절 상태 — 자사몰 JSON-LD `offers[].availability`, 무신사 `goodsOption`·`isSoonOutOfStock`·`isRestock`, 29CM 옵션 응답 | 없음 |
| **L2 읽기 전용 프로브** | PDP·옵션 API에 수량을 실어 **사이트가 돌려주는 재고 검증 응답·안내 문구**를 읽는다("최대 N개 구매 가능" 등) | 없음 |
| **L3 상태 변경 프로브** | 장바구니 담기·수량 변경·**결제 페이지 진입까지** 포함해 성공/실패 경계와 화면 노출 값을 읽고, **가역 조치(비우기 등)를 한다**. 승인 불요(D18) — 대신 무엇을 했고 되돌렸는지 보고한다 | 있음(가역) |

**절대 금지 (D18에서도 유지)**: **실제 결제 실행**(주문 완료·결제 수단 입력 제출),
로그인 실계정 사용(비로그인으로 안 되면 거기까지 — 필요하면 사용자에게 묻는다),
장바구니 등 변경 상태를 되돌리지 않고 남기는 것, 차단(403/429/캡차) 상태에서의 계속.

- **L3는 실패해도 되돌린다** — 담기에 성공한 뒤 예외가 나도 비우기를 반드시 시도하고,
  못 비웠으면 `meta.notes`에 남기고 사용자에게 보고한다. L3를 썼다는 사실 자체도
  보고에 포함한다(승인은 불요하지만 조용히 하지 않는다).
- 프로브는 **일반 수집보다 더 느리게** 한다(요청 간격 하한을 2배로) — 같은 상품에
  연속 요청이 몰리는 성격이라 부하가 집중된다.
- 어느 층으로 얻었는지를 **옵션마다 `stock_basis`에 남긴다**(`option_api`/`probe_read`/
  `probe_cart`/`unknown`). 층이 다르면 값의 성격도 다르다.

### 6-2 재고 정밀도 (D15)

- **이진 탐색**으로 정확한 수량을 찾는다. 상한 `N`(기본 100)까지만 보고 초과는
  `stock_qty: null` + `stock_display: "100+"`로 기록한다 — 상한을 넘긴 것과 모르는 것을
  구분한다.
- 옵션당 요청 수는 `log2(N)` 수준(N=100이면 약 7회)이다. 프로브 전에 **예상 요청 수와
  소요를 계산해 보고**한다.

### 6-3 적용 범위 (D16)

| 단계 | 대상 | 비용 |
|---|---|---|
| **기본(전체)** | 라인시트 전 상품의 **옵션 구성 + 옵션별 품절 상태**(L1) | 무료~상세 1회 |
| **심화(선택)** | 사용자가 지정한 상품 또는 하트·랭킹 상위 N개의 **재고 수량**(L2/L3) | 상품×옵션×log2(N) |

심화는 **묻지 않고 시작하지 않는다** — 대상 수·예상 요청 수·소요를 알리고 승인받는다.
전 상품 프로브를 요청받으면 규모를 계산해 보여주고 그래도 원하면 진행한다.

### 6-4 옵션별 판매수량 = 재고 시계열 감소분 (D17)

옵션별 판매수량을 노출하는 사이트는 없다. **관측된 재고의 감소분**으로 얻는다 —
관측 두 개의 차이이므로 추정이 아니다(가격 변화 감지와 같은 성격, EVIDENCE §8).

- 관측 주기는 **기존 랭킹 주기에 편승**한다(무신사 30분 · 29CM 1시간). 별도 잡을
  만들지 않고 랭킹 스냅샷 잡에 옵션 재고 관측을 얹는다.
- `stock_qty`가 줄었으면 그 차이가 **최소 판매량**이다. 늘었으면 **재입고**이고
  그 구간의 판매량은 알 수 없다.
- **밝혀야 하는 한계 2개**: ① 재입고가 섞인 구간은 **과소 집계**된다(재입고 전에 팔린
  수량이 가려진다) ② 축적이 쌓이기 전에는 값이 없다 — 첫 관측만으로는 판매량이 `null`이다.
- 리포트에는 **"최소 판매량(관측 구간 N시간, 재입고 M회 감지)"**로 표기한다.
  "판매량"이라고만 쓰면 실제보다 작은 수를 진짜 판매량으로 읽는다.

### 6-5 데이터 계약 확장

상품 레코드에 `variants[]`를 **선택 필드로** 추가한다(없으면 기존과 동일 — 하위 호환).

```json
"variants": [{
  "option_id": "4297589-BLACK-M",
  "color": "BLACK", "size": "M",
  "sold_out": false,
  "stock_qty": 7, "stock_display": null,
  "stock_basis": "probe_read",
  "observed_at": "2026-07-31 14:00:00"
}]
```

- `color`/`size`는 **사이트가 준 옵션 문자열을 파싱**해 담고, 축이 두 개가 아닌 사이트
  (단일 옵션·3축 이상)면 `option_name` 원문을 그대로 둔다. **없는 축을 지어내지 않는다.**
- DB에서는 `variants`(정적: 옵션 구성)와 `variant_observations`(시변: 재고)로 나눈다 —
  §2 products/observations 분리와 같은 이유다. 옵션별 판매수량은 저장하지 않고
  **관측 감소분으로 계산한다**(중복 저장하면 관측과 어긋날 수 있다).
- 옵션을 수집하지 않았으면 `variants`는 **없거나 `null`**이다 — 빈 배열(`[]`)은
  "옵션이 없는 상품"을 뜻하므로 구분한다.

### 6-6 사이즈별 재고 — 분석 반영 (2026-08-03 신설)

수집·적재는 되고 있었는데 **분석이 그 테이블을 읽지 않고 있었다**(2026-08-03 사용자 지적).
`variants`·`variant_observations`에서 옵션마다 마지막 관측을 가져와 상품에 붙인다.

| 필드 | 뜻 | 주의 |
|---|---|---|
| `opt_total` `opt_live` `opt_out` | 옵션 수 · 판매 중 · 품절 | 옵션 **개수**다 |
| `opt_out_rate` | 품절 옵션 비율(%) | **수량 비율이 아니다** — L1만 수집되면 수량이 없다 |
| `stock_sum` | 재고 수량 합 | 수량을 관측한 옵션만 더한다(L2/L3) |
| `sold_min` | 최소 판매량 = 재고 감소분 | §6-4. 관측 2개의 차이라 추정이 아니다 |
| `sizes[]` | `[사이즈, 품절여부, 수량]` | 사이트가 준 문자열 그대로 — 표기가 다른 사이트를 합치지 않는다 |

- 이 필드들은 산점도 축·그룹 기준·표 열로 그대로 쓰인다. 사이즈별 패널은 **자연 순서**
  (XS→S→M→L→XL→FREE→수치)로 세운다 — 가나다순 정렬 금지의 예외다.
- **`sold_min`은 없으면 만들지 않는다.** 같은 옵션을 수량까지 두 번 이상 관측해야 차이가
  생긴다. 2026-08-03 실측: 수량 관측 1,850건이 있으나 **옵션당 시점이 1개뿐**이라 아직
  산출되지 않는다. 그 사실을 화면에 적는다("한 번 더 관측해야 성립한다").
- 재입고가 섞인 구간은 **과소 집계**되므로 이름이 `sold`가 아니라 `sold_min`이다.
  화면에도 "최소 판매량 · 재입고 N회 감지 · 관측 창 from~to"로 적는다.

## §7 파생 프록시 — 수집은 풍부하게, 추출은 리포트에서 (D19)

가설 도출에는 계약 필드 밖의 신호가 필요하다 — 예: 썸네일에 모델이 있는가(스튜디오
컷인가 일반 착용 컷인가, 모델의 외형 특성), `[00 착용]`·`[00 PICK]` 같은 셀럽·인플루언서
배지가 있는가, 상품명이 영문인가 한국어인가. **이런 신호는 예시일 뿐이고, 요청마다
새로 정의될 수 있어야 한다 — 하드코딩하지 않는다.**

### 7-1 계층 분리

| 계층 | 원칙 |
|---|---|
| **수집 (재료 보존)** | 화면에 노출된 부가 표시물(배지·라벨·마크 문자열)을 `raw_extras`에 **원문 그대로** 담는다. 해석하지 않는다 — 재료가 남아 있으면 프록시는 나중에 얼마든지 뽑는다. 이미지는 URL로 이미 보존된다 |
| **정의 (제네릭)** | 프록시는 `proxy_defs`에 등록되는 **정의 카드**다: 이름 · 판정 질문 · 재료(name/image/badge/detail) · 값 공간 · 판정 방법(rule/vision/llm). 요청 맥락에서 **AI가 자동으로 제안·정의·등록**하고 정의 카드를 보고한다(D23 — 예외 없음, 사전 확인 절차 없음). 한 번 정의된 프록시는 다음 요청에서 재사용된다 |
| **추출 (리포트 단계)** | DB 관측 테이블에 차원을 늘리지 않는다. 리포트 생성기가 proxy_cache를 읽어 상품에 `px_*` 열로 주입하고, 대시보드가 **자동으로 필터·축을 만든다** |
| **판정 캐시 (재사용)** | 비싼 판정(비전·LLM)은 `proxy_cache`에 (프록시, 상품, **재료 지문**) 키로 저장한다. 지문 = 판정 재료의 식별자(이미지 프록시는 image_url, 이름 프록시는 name). **재료가 안 바뀌면 재판정하지 않는다** — 이미지가 교체되면 지문이 달라져 자동 무효화된다 |

### 7-2 정직성 규칙

- 프록시 값은 **판정이지 사실이 아니다** — 대시보드에서 프록시 축·필터에는
  "AI 판정" 표기와 미판정 건수가 붙는다. 미판정은 별도 값 `(미판정)`으로 구분한다
  (0도 unknown도 아니다).
- 판정 불확실하면 `unknown` — 찍지 않는다(핏 분류와 같은 원칙). 판정 근거(basis)를
  캐시에 함께 남긴다.
- 사람 외형에 대한 판정(모델 특성 등)은 **이미지에 보이는 인상의 분류이지 실제 국적·
  정체성 추정이 아니다** — 값 이름과 리포트 각주에 이를 명시하고, 확신 없으면 unknown.
- 룰 기반(정규식— 영문/한글 판별, 배지 매칭)은 싸므로 리포트 생성 시 즉석 계산해도
  되지만, 비전·LLM 판정은 **반드시 캐시를 먼저 조회**하고 미캐시분만 판정한다.

### 7-3 실행 주체

- 절차 정본: `skills/commerce-intel/references/proxy-extraction.md`
- 배치 판정: `proxy-extractor` 서브 에이전트 — 이미지 묶음을 받아 정의 카드의 값
  공간 안에서만 판정한다(전수조사 핏 분류의 비전 서브에이전트 분담 전례를 일반화)

## §8 유저 스토리 개방 원칙 (요구 4)

- 스토리 A/B/C(SPEC v16)는 **폐기가 아니라 부분집합**이다 — 검증된 절차로 계속 쓴다.
- 그 밖의 요구(예: "이 브랜드 어디에 입점해 있어?", "핏별 가격대가 어떻게 달라?",
  "지난달 할인 들어간 상품들 반응 어땠어?")는 고정 절차가 없어도 **공통 원칙 안에서**
  조합해 수행한다: 추정 금지 · 노출값만 · 차단 시 중단 · DB 재사용 판정 선행 ·
  결과는 DB 적재 후 리포트.
- 절차가 3회 이상 반복되면 스킬에 스토리로 승격하는 것을 사용자에게 제안한다 —
  임의로 스펙화하지 않는다.

## §9 성공 기준

1. 같은 대상을 두 번 요청하면 두 번째는 **재사용 판정이 먼저** 일어나고, 스킵 여부와
   근거(마지막 관측 시각·TTL)가 보고된다.
2. 수집 완료 후 구글 시트에서 사람이 최신 데이터를 볼 수 있다(미러 실패 시 그 사실이
   보고되고 다음 성공 때 따라잡힌다).
3. 리포트에서 사용자가 변인을 고정하고 축을 바꿔가며 **스스로 가설을 만들 수 있다** —
   요약 수치만 있는 리포트는 이 기준 미달이다.
4. 처음 보는 플랫폼 이름이 나오면: channel-scout 정찰 → generic 절차로 1회성 수집,
   또는 skill-maker로 스킬 초안 — 어느 쪽이든 **하드코딩된 입점처 목록에 없다는 이유로
   거절하지 않는다.**
5. 기존 원칙(추정 금지·미노출=null·차단 중단·완전일치 매칭)은 전부 계승된다.
6. 옵션 심화를 요청하면 **예상 요청 수·소요를 먼저 보고**하고, L3(장바구니)를 쓸 때는
   승인을 받으며, 판매수량은 **"최소 판매량 + 한계"**로 표기된다. 프로브 후 장바구니가
   비워졌음이 확인된다.
