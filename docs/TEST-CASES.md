# commerce-research 테스트 케이스

- 상태: **v8 (2026-07-30)** — **스토리1에 자사몰(Cafe24) 추가**
  (T14·T15 / N7 / E-IN-16~17 · E-SR-13~15 · E-DQ-22~24 · E-MP-14~15 신규)
- **최근 수정: 2026-07-30 16:40**
- 스펙: [SPEC.md](SPEC.md) · 가이드의 3분류(triggering / functional / performance)를 따른다

테스트는 두 층이다.

| 층 | 무엇을 보나 | 어떻게 돌리나 |
|---|---|---|
| **자동 (스크립트 회귀)** | `scripts/` 3종이 계약대로 동작하는가 | `python3 tests/run_tests.py` — 픽스처만 쓰고 사이트에 붙지 않는다. 현재 **92건 전건 통과** |
| **수동 (트리거·기능)** | 스킬이 실제 사이트에서 끝까지 도는가 | 새 대화에서 자연어 요청 1개로 실행 |

각 엣지 케이스에는 검증 층을 표시한다: `[자동]` 회귀 테스트에 있음 · `[수동]` 실사이트 실행으로 확인 · `[문서]` 어댑터/SKILL의 지시로 방어(위반 시 수동 검출).

## 1. 트리거링 테스트 (수동)

**트리거 필요조건: 플랫폼 이름(무신사/29cm/자사몰)이 요청에 명시돼야 한다.**
브랜드나 카테고리만 말하면 트리거하지 않는다 (2026-07-29 확정).
**"자사몰"·"공식몰"·자사몰 도메인도 플랫폼 지정으로 인정한다** (v8, 2026-07-30).

### 트리거되어야 함 (11)

| # | 요청 | 스토리 |
|---|---|---|
| T1 | "무신사에서 인사일런스 상품 전체 라인시트로 정리해줘" | 1 |
| T2 | "무신사 인사일런스 제품들 표로 만들어줘. 이미지랑 후기 수도 넣어서" | 1 |
| T3 | "29cm에 있는 인사일런스 상품 다 모아서 보여줘" | 1 |
| T4 | "무신사 데님팬츠 시장 전수조사 해줘" | 2 |
| T5 | "29cm 데님 바지 카테고리 상품들 평점이랑 후기 수 정리해줘" | 2 |
| T6 | "무신사 데님 상품들 핏 종류별로 분류해줘 (부츠컷 이런 거)" | 2 |
| T7 | "무신사 바지 랭킹 top 100 지난 일주일 변화 보여줘" | 3 |
| T8 | "3월 한 달간 무신사 바지 랭킹에서 할인 들어간 상품 찾아줘" | 3 |
| T9 | "무신사 바지 랭킹 스냅샷 찍어놔" | 3(C-1) |
| T10 | "musinsa denim pants 전수조사 부탁해" (혼용 표현) | 2 |
| T11 | "무신사 바지 랭킹 모니터링 시작해줘" | 3(C-1 — 스냅샷+스케줄 등록) |
| T12 | "무신사랑 29cm에서 인사일런스 라인시트로 정리해줘" | 1(멀티 — 2플랫폼 합집합) |
| T13 | "인사일런스가 무신사랑 29CM 중 어디에 뭘 더 많이 냈는지 비교해줘" | 1(멀티 — 입점 커버리지) |
| T14 | "인사일런스를 무신사랑 자사몰에서 비교해줘" | 1(멀티 — **자사몰**, v8) |
| T15 | "2000아카이브스 공식몰(2000archives.com) 상품 라인시트로 정리해줘" | 1(**자사몰 단일**, 도메인 직접 지정, v8) |

### 트리거되지 않아야 함 (7) — 판정 기준 ≥14/15, 0/7

| # | 요청 | 이유 |
|---|---|---|
| N1 | "오늘 서울 날씨 알려줘" | 무관 |
| N2 | "파이썬으로 크롤러 짜는 법 알려줘" | 일반 코딩 질문 |
| N3 | "무신사 주식 전망 어때?" | 사이트 데이터 수집 아님 |
| N4 | "쿠팡에서 노트북 최저가 찾아줘" | 비지원 사이트 |
| N5 | "무신사에서 이 바지 대신 구매해줘" | 구매 행위 = 비범위 |
| N6 | "인사일런스 상품 전부 라인시트로 정리해줘" | **플랫폼 이름 없음** — 필요조건 미충족 |
| N7 | "무신사랑 우리 쇼피파이 스토어 비교해줘" | **Cafe24가 아닌 엔진** — 자사몰이어도 비범위 (v8) |

판정: 각 요청을 새 대화에서 실행해 스킬 자동 로드 여부 기록. 기준 ≥14/15, 0/7.

## 2. 스토리별 완주(happy path) 기준

완주 시나리오는 5개다 — 인사일런스 라인시트(무신사 단일) / **인사일런스 2플랫폼 비교(v6)** /
**인사일런스 무신사+자사몰 비교(v8)** / 데님팬츠(범위 합의) 전수조사 /
무신사 바지 랭킹 top 100 diff.
**통과 기준은 [SPEC.md](SPEC.md) §4의 성공 기준을 그대로 쓰고 여기 반복하지 않는다.**
실패 양상(엣지)은 전부 §3 카탈로그에 있다.

### 2-1. 스토리1 멀티 플랫폼 실측 기준선 (2026-07-30, 인사일런스)

회귀의 기준이 되는 실측값이다. 재실행 시 사이트 데이터가 변해 숫자는 움직이지만
**관계식은 항상 성립해야 한다.**

> **v6 갱신 (2026-07-30 12:45)** — 아래 값은 **품절 포함 기준으로 다시 측정**했다.
> 구 기준선(무신사 564 / 29CM 422 / 합집합 647 / 양쪽 339 / 단독 225·83)은
> **무신사만 품절이 빠진 비대칭 비교**였다. 필터를 맞추자 29CM 단독 83건이 0건이 됐다
> (90건이 무신사 품절 상품과 매칭). 구 값으로 회귀를 판정하지 마라.

| 항목 | 실측 | 불변 관계 |
|---|---|---|
| 무신사 수집 | **2,022** (`&isSoldOut=true`, 품절 1,459 + 판매중 563) | 화면 총계와 같아야 한다 |
| 29CM 수집 | **435** (브라우저 순회, 기본이 품절 포함) | 화면 총계와 같아야 한다 |
| 합집합 | **2,019** | 매칭 + 단독 합계와 같아야 한다 |
| 양쪽 입점 | **435** | 29CM 전량이 무신사에 있다 |
| 무신사 단독 / 29CM 단독 | **1,584 / 0** | 435+1,584+0 = 2,019 ✓ |
| 수집 건수 − 합집합 행 수 | **3** | 무신사 안에 정규화 이름이 같은 상품 3쌍(전부 품절, 재등록)이 있어 접힌다. 0이 아닐 수 있다 |
| 사이트별 합집합 등장 수 | 2,019 / 435 | 무신사는 접힌 3건만큼 원본(2,022)보다 적다 |
| 품목 통합축 대응 | **34건** (3건 미만 약한 대응 **14개**) | 근거 건수를 함께 싣는다 |
| 하트·후기 배율 | 품목별로 산출 (최대 하트 21배·후기 77배) | 단독 입점 상품은 배율 비교에서 빠진다 |
| 판매가 동일 / 상이 | **305 / 130** (매칭 435건 중) | 합이 매칭 건수와 같아야 한다 |

### 2-2. 자사몰 실측 기준선 (2026-07-30, 인사일런스 `insilence.co.kr`) — v8

카테고리 순회로 얻은 값이다. 사이트 데이터가 변해 숫자는 움직이지만 **관계식은 성립해야 한다.**

| 항목 | 실측 | 불변 관계 |
|---|---|---|
| 남성복 `ALL` (`man_list?cate_no=337`) | **222** | 10페이지, 마지막 6건 |
| 여성복 `ALL` (`woman_list?cate_no=348`) | **206** | 9페이지 |
| 남·여 `ALL` 합집합 | **428** | 두 값의 합보다 작거나 같다(중복 존재 가능) |
| `SEASON OFF` (`outlet_list?cate_no=685`) | **349** | |
| `SEASON OFF` 중 `ALL`에 없는 것 | **127 (23%)** | **0이 아니다** — `ALL`만 쓰면 이만큼 놓친다 |
| 전체 합집합 | **555** | `ALL` 합집합보다 반드시 크다 |
| `source_total` | **`null`** | 화면 총계가 없다. 수집 건수를 넣으면 안 된다(E-SR-10) |
| 페이지 크기 | **24** | |
| 목록의 품절 배지 | **0건 / 192개 스캔** | 배지가 없을 뿐 품절이 포함돼 있다 |
| 후기·평점·좋아요·조회수·누적판매 | **전건 `null`** | 수집하지 않는다. 0이 아니라 `null`이다 |

## 3. 엣지 케이스 카탈로그

### 3-1. 입력 엣지 케이스 (E-IN)

| # | 상황 | 기대 동작 | 층 |
|---|---|---|---|
| E-IN-1 | 존재하지 않는 브랜드 ("아사드프qwe 라인시트") | 검색 0건을 명시적으로 보고. 빈 리포트를 만들지 않는다 | [자동 V3] + [수동] |
| E-IN-2 | 브랜드명이 여러 브랜드숍에 걸림 ("인사일런스" vs "인사일런스 우먼") | 후보를 모두 제시하고 사용자가 고른다. 임의로 하나를 고르거나 합치지 않는다 | [수동] |
| E-IN-3 | 모호한 카테고리 ("바지 전수조사") | 사이트의 세부 카테고리 후보를 개수와 함께 제시 | [수동] |
| E-IN-4 | 한/영 혼용·오타 ("musinsa denim pants", "인사일런스") | 정상 트리거·대상 해석. 애매하면 확인 질문 1회 | [수동 T10] |
| E-IN-5 | 스토리3 기간 미입력 ("랭킹 변화 알려줘") | 변화 분석을 하지 않는다. 스냅샷 1개만 찍고 기간 지정을 안내 + C-1(모니터링 시작)을 제안 | [수동] |
| E-IN-6 | 기간이 미래 ("다음 주 변화") | 미래는 비교 불가임을 설명, 스냅샷 축적 방법을 안내 | [수동] |
| E-IN-7 | 기간 역전 (--from > --to) | `diff_snapshots.py`가 종료 코드 2로 거부 | [자동 D3] |
| E-IN-8 | 상대 기간 ("지난 일주일", "3월 한 달간") | 절대 날짜로 변환해 리포트에 명시. 오늘 날짜는 `date`로 확인(추측 금지) | [수동] |
| E-IN-9 | 한 요청에 스토리 두 개 ("라인시트 하고 랭킹도") | 두 태스크로 분리 실행, 산출물 2개. story 혼합 JSON은 리포트 생성기가 거부 | [자동 B7] + [수동] |
| E-IN-10 | 성별 지정 ("남자 데님만") | 무신사 `gf=M` / 29CM 남성 트리로 매핑, `target`에 "(남성)" 표기 | [수동] |
| E-IN-11 | 구매 요청이 섞임 ("정리하고 제일 싼 거 사줘") | 수집·정리만 수행. 구매는 비범위임을 명시 | [수동 N5 변형] |
| E-IN-12 | 비지원 사이트 ("에이블리에서") | 지원 사이트(무신사·29CM)를 안내, 임의로 다른 사이트를 긁지 않는다 | [수동] |
| E-IN-13 | 사이트 미지정 상태로 실행됨 (대화 중간 "그 브랜드도 정리해줘" 등) | **무신사를 기본값으로 가정하지 않는다.** 어느 사이트인지 묻고, 답을 받은 사이트만 수집한다 | [수동] |
| E-IN-14 | 스토리1 대형 브랜드 (상품 3,000 초과) | 수집을 시작하지 않고 규모·예상 소요를 알린 뒤 좁힐 축(카테고리 등)의 실제 개수를 제시. 스토리2와 같은 협의 절차 (v4) | [수동] |
| E-IN-15 | C-1 모니터링 시작 요청 ("모니터링 시작해줘", "추적해줘") | 스냅샷 1개를 찍어 성공을 확인한 뒤 환경 스케줄러에 **사이트 원본 갱신 주기**(무신사 30분/29CM 1시간)로 등록하고, 등록 내용(사이트/카테고리/주기/저장 경로)을 보고 (v4) | [수동] |
| E-IN-16 | **자사몰인데 Cafe24가 아니다** (v8) | 엔진 신호(`cafe24`·`cfa-js.cafe24`·`app4you.cafe24`)가 없으면 **수집하지 않는다.** 지원 엔진이 Cafe24뿐임을 알리고 멈춘다. 다른 엔진을 추측으로 긁지 않는다 | [문서] + [수동 N7] |
| E-IN-17 | **자사몰만 지정됐다** ("인사일런스 자사몰만 정리해줘") (v8) | 단일 플랫폼 라인시트로 동작한다. 비교 축 4개는 만들지 않는다(비교 대상 없음). 후기·평점·좋아요 열은 전부 미노출로 찍히고 **왜 없는지 각주로 밝힌다** | [수동] |

### 3-2. 검색(탐색) 엣지 케이스 (E-SR)

| # | 상황 | 기대 동작 | 층 |
|---|---|---|---|
| E-SR-1 | 브랜드 slug/ID를 모른다 | 추측 금지. 무신사는 PLP 응답 `brandLinkUrl`에서, 29CM는 상품 JSON-LD `brand.url`에서 추출 | [문서] |
| E-SR-2 | 옛 문서의 죽은 URL (store.musinsa.com 등) | 어댑터의 현행 URL만 사용. 404/무응답 URL로 재시도하지 않는다 | [문서] |
| E-SR-3 | 구형 URL 리다이렉트 (308/302) | 따라가되 최종 URL을 `url` 필드에 기록 | [문서] |
| E-SR-4 | 브랜드는 존재하는데 상품 0개 | "브랜드 없음"과 구분해 "브랜드 존재, 상품 0건"으로 보고 | [수동] |
| E-SR-5 | 카테고리 코드를 모른다 (`003002` 외 미검증) | 사이트 네비게이션에서 실측. 코드를 지어내지 않는다 | [문서] |
| E-SR-6 | 사이트 총계와 실수집 수가 어긋난다 (광고 상품 포함 등) | `source_total` 기록 → 검증기가 ±5% 판정, 초과 시 리포트에 경고 배너 | [자동 V4b] |
| E-SR-7 | 정렬 파라미터가 조용히 무시된다 (무신사 sortCode 폴백) | 정렬 변경 후 결과가 실제로 달라졌는지 확인. 전수 수집은 정렬에 의존하지 않는다 | [문서] |
| E-SR-8 | 29CM 목록 수집 (v6 개정) | **브라우저 목록 순회가 기본이다**(`&page=` 페이지네이션). BEST API는 전량이 아니므로(435 중 422, 원인 미특정) 보완 경로다. 어느 경로를 썼는지 `meta.notes`에 기록 | [문서] |
| E-SR-9 | 랭킹 top 100인데 페이지 순회를 시도 | 무신사 랭킹은 102개가 한 번에 온다 — 불필요한 추가 요청을 하지 않는다 | [문서] + [수동] |
| E-SR-10 | **`source_total`에 수집 건수를 넣는다** (v6) | 금지. 자기 자신과 비교하는 **순환 검증**이 되어 오차가 항상 0.0%로 통과한다(실제 사고: 422/435를 "오차 0.0% 통과"로 보고). `source_total`은 **화면에 노출된 총계**다. 총계를 못 읽었으면 `null`로 두고 검증을 건너뛴 사실을 남긴다 | [문서] + [수동] |
| E-SR-11 | **랭킹 API의 합집합 포화를 완전성 근거로 쓴다** (v6) | 금지. 정렬 조합 합집합이 더 안 늘어나는 것은 정렬들이 같은 집합을 반복해서 준다는 뜻일 뿐이다. 실측: `hasNext=false` + 합집합 포화를 모두 만족한 422건이 435건 중 422건이었다. **누락 원인은 미특정** — 품절 필터도 아니고(수집분에 품절 120건) 저반응 때문도 아니다(누락분 좋아요 4~39·후기 0~3은 수집분이 커버하는 구간). 예측 불가이므로 화면 총계 대조만이 근거다 | [문서] |
| E-SR-12 | **브랜드 홈을 전 상품으로 착각** (v6, 무신사) | `/brand/{slug}`는 브랜드 홈이라 128개에서 끝난다. 전 상품은 `/brand/{slug}/products`로 563개다. 둘 다 200이고 둘 다 상품이 보이므로 **화면 총계와 대조**해야만 구분된다 | [문서] + [수동] |
| E-SR-13 | **자사몰 도메인을 지어낸다** (v8) | 금지. 브랜드명만 주어지면 **검색해서 찾고, 타이틀·로고·푸터 상호로 브랜드가 맞는지 확인**한다. 후보가 여럿이거나 애매하면 제시하고 사용자가 고른다(E-IN-2와 같은 처리). `{브랜드}.co.kr`을 추측해 넣지 않는다 | [문서] + [수동] |
| E-SR-14 | **자사몰 `ALL` 카테고리를 전량으로 착각** (v8) | `ALL`은 **그 트리 안에서만** 전부다. 인사일런스 실측: 남·여 `ALL` 합집합 428인데 `SEASON OFF` 349 중 **127건(23%)이 `ALL`에 없다.** 전체 합집합은 555. **네비게이션의 모든 상품 카테고리를 돌고 `product_id`로 중복 제거**해야 한다 | [문서] + [수동] |
| E-SR-15 | **자사몰 페이지 번호 목록을 끝으로 착각** (v8) | 페이지네이션은 **5개씩 보이는 슬라이딩 창**이다. 1페이지에서 `1 2 3 4 5`만 보여도 실제는 10페이지일 수 있다(실측 `cate_no=337`). 종료 판정은 **`.btnNext a`의 href가 `#none`**이다. 범위를 넘는 페이지는 에러가 아니라 **200에 0건**을 준다 | [문서] |

### 3-3. 브라우저/사이트 안정성 엣지 케이스 (E-ST)

| # | 상황 | 기대 동작 | 층 |
|---|---|---|---|
| E-ST-1 | 403/429/캡차 | 즉시 중단 → 수집분으로 부분 리포트, `incomplete: true` + 중단 지점 명시. 우회 금지 | [자동 V4/B5] + [수동] |
| E-ST-2 | 차단 후 UA 위장 유혹 | User-Agent를 바꿔 재시도하지 않는다 | [문서] |
| E-ST-3 | 간헐 타임아웃/5xx | 간격을 늘리며 2회 재시도, 3연속 실패면 항목 스킵 + `notes` 기록 | [문서] |
| E-ST-4 | 페이지네이션 무한 루프 (같은 페이지 반복) | 직전 페이지와 동일 항목이면 중단 + **3연속 무증가**(E-ST-12), `notes` 기록. **페이지 수·스크롤 횟수 상한은 쓰지 않는다** — 페이지 크기와 스텝당 개수가 사이트마다 달라 같은 숫자가 다른 양을 뜻한다. 양의 상한은 **상품 수 하나뿐**이다(E-ST-13) | [문서] |
| E-ST-5 | 사이트 개편으로 필드 대량 결측 | 결측 >30% → 검증기 FAIL(2), "구조 변경 의심" 배너. 리포트 생성 전에 어댑터 점검 | [자동 V2/B6] |
| E-ST-6 | 경로 B(화면)가 막힌다 — 렌더 실패·구조 변경 (v6 개정) | **경로 B가 기본이므로 폴백 방향이 뒤집혔다.** 경로 A(JSON)로 내려가고 `meta.notes`에 기록. 경로 A로 내려갔으면 **전량 보장이 깨질 수 있다는 것을 리포트에 명시**한다(품절 누락) | [문서] |
| E-ST-15 | **⚠️ 목록의 기본 필터를 확인하지 않는다** (v6, 최중요) | 무신사 브랜드 목록은 기본이 **품절 제외**(인사일런스 563)이고 `&isSoldOut=true`를 붙여야 전량(**2,022**)이다. 29CM은 기본이 **품절 포함**(필터 이름이 `품절상품 제외`). **필터가 걸린 총계도 화면 총계 대조를 정상 통과한다** — `source_total`과 함께 필터 상태를 `meta.notes`에 적어야 근거가 된다. 플랫폼 비교 시 필터를 맞추지 않으면 결론이 뒤집힌다(실측: "29CM 단독 83건" → 필터 정렬 후 **0건**, 90건이 무신사 품절 상품과 매칭). 함정: `soldOut=true`는 조용히 무시된다 | [문서] + [수동] |
| E-ST-16 | **카테고리 필터 순회에서 병렬 축·은퇴 코드를 놓친다** (v6, 무신사) | 카드·목록 API에 `category`가 없어 소분류 필터를 순회해 붙인다. ① `107`·`108`은 병렬 축이라 넣으면 686건 중복 — **대분류 합 = 전체 총계**로 판별한다 ② `001008`(기타 상의)처럼 메뉴 트리·칩 행에 없는 은퇴 코드가 있다 — **대분류 총계와 소분류 합의 차이**로만 드러난다(722 vs 718) ③ 메뉴에 같은 이름이 코드 두 개로 있다(`벨트` `101006`/`101007` 등) — 이름으로 역추적하면 한쪽을 놓치므로 **코드를 순회하고 이름은 코드에서 얻는다** | [문서] + [수동] |
| E-ST-11 | **가상화 목록을 끝까지 스크롤한 뒤 한 번 읽는다** (v6) | 마지막 66개만 잡힌다. 무신사 PLP는 페이지가 57배로 늘어도 DOM 안 상품 링크가 132~144개로 고정이다. **매 스텝 수집해 `product_id`로 누적**해야 한다 | [문서] + [수동] |
| E-ST-12 | 스크롤 종료 판정 (v6) | **3연속 무증가 = 끝.** 근거: 랭킹은 끝에 닿으면 증가가 완전히 0이 된다(8스텝 연속 0 확인). 29CM은 빈 페이지 + 명시 메시지로 결정적 종료라 추정이 필요 없다 | [문서] |
| E-ST-13 | 자연 종료가 없는 목록 (v6, 무신사 PLP) | 24,686개까지 계속 나온다. 스토리별 상한(1·2 모두 3,000)에서 마감하고 **상한에 걸려 끊은 것과 목록이 끝난 것을 리포트에서 구분**한다 | [문서] + [수동] |
| E-ST-14 | **랭킹 코드가 조용히 무시된다** (v6, 무신사) | 화면은 6자리만 받는다(바지 = `003000`). `003`을 주면 에러 없이 `sectionId=200&categoryCode=000`(NEW·전체)이 온다. **도달 URL과 1위 상품을 확인**해 다른 랭킹이면 실패로 처리한다 | [문서] + [수동] |
| E-ST-7 | 로그인 모달/벽 | 닫기 1회 시도, 안 되면 그 지점까지 부분 수집으로 처리 | [수동] |
| E-ST-8 | 수집 중 강제 중단 (세션 끊김) | `data/raw/` 체크포인트에서 이어서 재개, 항목 중복 없이 | [수동] |
| E-ST-9 | `totalPages`가 실제와 다름 (무신사 실측) | 총 페이지 수 대신 **빈 결과**로 종료 판정 | [문서] |
| E-ST-10 | 마크다운 변환이 script 데이터를 버림 (29CM 실측) | "데이터 없음" 판정 전에 원본 HTML 확인 | [문서] |

### 3-4. 데이터 품질 엣지 케이스 (E-DQ)

| # | 상황 | 기대 동작 | 층 |
|---|---|---|---|
| E-DQ-1 | 평점 스케일 혼입 (무신사 목록 0~100) | 5점으로 변환. 0~5 범위를 벗어나면 검증기 경고 | [자동] |
| E-DQ-2 | 판매가 > 정가 | 검증기 경고 | [자동] |
| E-DQ-3 | 기록된 할인율과 가격 계산이 ±1%p 초과로 어긋남 | 검증기 경고 | [자동] |
| E-DQ-4 | `product_id` 중복 | 라인시트=경고, 랭킹 스냅샷=오류(중복 불가) | [자동] |
| E-DQ-5 | 랭킹 `rank` 중복·비연속 | 중복=오류, 비연속=경고(빠진 순위 나열) | [자동] |
| E-DQ-6 | 구간 표기를 정수로 변환해 담음 ("300회 이상"→300) | `view_count`와 `view_count_display` 동시 존재 → 검증기 경고 | [자동 V4c] |
| E-DQ-7 | 스냅 게시물 지표를 상품 지표로 오독 (무신사) | `snapReviews.aggregations` 값은 담지 않는다 | [문서] + [수동 대조] |
| E-DQ-8 | 브랜드 하트(8.9만)를 상품 하트로 오독 | 여러 상품의 값이 동일하면 오독 의심. 상품마다 다른지 대조 | [문서] + [수동 대조] |
| E-DQ-9 | 0과 null의 혼동 | 후기 0건은 `0`, 미노출은 `null`. 0을 null로, null을 0으로 바꾸지 않는다. 리포트에서 0은 "0", null은 "미노출" | [자동 B2h] |
| E-DQ-10 | 실시간 지표("N명이 보는 중/구매 중") 처리 | `viewers_now`/`buyers_now`에 담는다. "1.2천명"→1200, "만"→×10000. **랭킹 목록에만 있다 — 상품 상세에서 찾지 않는다.** 반올림된 노출값이므로 순위 산정 근거로 쓰지 않는다 | [문서] + [수동] |
| E-DQ-11 | 스토리2에 `reviews[]`가 담겨 옴 | 검증기가 "리뷰 본문을 수집하지 않는다" 경고(WARN) | [자동 V5d] |
| E-DQ-12 | 쿠폰 적용가를 판매가로 담음 (29CM) | `price_sale`은 `sellPrice`만. 쿠폰가는 조건부라 비교 불성립 | [문서] |
| E-DQ-13 | 이미지/상품 URL이 상대 경로 | 절대 URL 아니면 검증기 경고 | [자동] |
| E-DQ-14 | `soldQty` 노출 플래그 꺼짐 (29CM `isDisplaySellQty: false`) | 값이 있어도 담지 않는다 — 화면에 안 보이는 값이다 | [문서] |
| E-DQ-15 | 품절 상품 | 표에서 빼지 않고 품절 배지로 표시 | [자동 렌더] + [수동] |
| E-DQ-16 | 원본 갱신 주기보다 촘촘한 축적 (같은 랭킹 중복 저장) | 축적 주기는 사이트별 원본 주기(무신사 30분 / 29CM 1시간). 무신사는 응답의 `updatedAt`을 meta에 저장하면 동일 랭킹 재수집을 판정할 수 있다 | [문서] |
| E-DQ-17 | 무신사 랭킹 응답에 광고 배너 항목 혼입 (`BANNER_COLUMN`, 요청마다 바뀜) | `PRODUCT_COLUMN`만 걸러 담는다. 안 거르면 매 요청 랭킹이 변한 것처럼 보인다 | [문서] |
| E-DQ-18 | 29CM 목록 응답에 중복 상품 (422건 중 유니크 421 — 실측) | `product_id` 기준 dedup. 안 하면 검증기가 중복 경고를 낸다 | [문서] + [자동 E-DQ-4] |
| E-DQ-19 | 29CM `saleRate`를 `discount_rate`에 그대로 담음 | `saleRate`는 **쿠폰가 기준**이다(실측: 정가 54,000 / 판매가 43,200 / 쿠폰가 37,160 / saleRate 31). `originalPrice`·`sellPrice`로 재계산 — 안 하면 쿠폰 할인율이 판매가 할인율로 위장된다 | [문서] + [자동 E-DQ-3] |
| E-DQ-20 | 29CM 브랜드 목록을 좁은 랭킹 창으로 수집 | `periodFacetInput`은 랭킹 창이다. 실측: `WEEKLY` 365 / `HOURLY` 26 vs 합집합 422. `MONTHLY` + 정렬 4종 합집합으로 모은다. **누락분은 상대 플랫폼 단독 입점으로 오집계된다** | [문서] |
| E-DQ-21 | 29CM `smallCategoryCode`가 `null` | 중분류 이름으로 폴백. 둘 다 없으면 `category` 결측(실측 422건 중 9건) | [문서] |
| E-DQ-22 | **자사몰 가격 클래스 이름이 뒤집혀 있다** (v8) | 인사일런스 렌더 DOM은 `.sale_price`에 **정가**(166,000), `.original_price`에 **판매가**(149,000)를 담는다. **클래스 이름을 믿으면 정가·판매가가 뒤집힌다.** `price_sale`은 상세 JSON-LD `offers[].price`로 확정하고, 정가는 라벨(`소비자가`)이나 취소선으로 찾는다 | [문서] |
| E-DQ-23 | **자사몰 목록 가격 박스가 비어 있다** (v8) | `.custom_price_wrap`은 서버 HTML에서 비어 있고 JS가 채운다. HTTP로 HTML만 받아 파싱하면 가격이 빈 문자열이 된다(실측). 브라우저로 렌더한 뒤 읽거나 서버 HTML에도 있는 `.real_contents`를 쓴다 | [문서] |
| E-DQ-24 | **자사몰 일부 사이즈 품절을 품절로 셈** (v8) | 상세 JSON-LD `offers`는 **사이즈별 배열**이다. **하나라도 `InStock`이면 판매중**이고, `offers`가 빈 배열이거나 전부 `OutOfStock`일 때만 품절이다. 실측 14건 중 혼재 5건 — 이걸 품절로 세면 안 된다 | [문서] |

### 3-4-1. 멀티 플랫폼 매칭 엣지 케이스 (E-MP) — v6 신규

| # | 상황 | 기대 동작 | 층 |
|---|---|---|---|
| E-MP-1 | 상품명이 조금 다른 동일 상품 (`가먼트 다잉` vs `가먼트다잉`) | 정규화(공백 제거)로 **매칭된다** | [자동 B1m] |
| E-MP-2 | 이름이 비슷하지만 다른 상품 (`헨리넥 숏슬리브` vs `와플 헨리넥 롱슬리브`) | 매칭하지 **않는다**. 각각 단독 입점 | [자동 B1s] + [문서] |
| E-MP-3 | 이름이 다르고 **정가까지 같은** 다른 상품 (`레터링 그래픽 티셔츠` vs `스탠실 그래픽 티셔츠`, 둘 다 66,000원) | 매칭하지 않는다. **가격을 매칭 보조 신호로 쓰지 않는 이유가 이 케이스다** | [문서 — 최중요] |
| E-MP-4 | 유통 표기가 붙은 이름 (`[2 PACK] 수피마 코튼...`) | 대괄호 안을 제거해 정규화. 그래도 상대 플랫폼에 없으면 단독 입점 | [자동 B1m] |
| E-MP-5 | 한 사이트 안에 정규화 키가 겹치는 레코드(동명이품) | 첫 레코드를 대표로 쓰고 **나머지는 세어 경고 배너**로 밝힌다. 조용히 버리지 않는다 | [자동 — `union.collisions`] |
| E-MP-6 | 상품명이 결측인 상품 | 매칭 대상에서 빼고 `product_id`로 고유 행을 만든다. 이름 없는 상품끼리 한 행으로 뭉치면 안 된다 | [자동 build_union] |
| E-MP-7 | 매칭+단독 합이 합집합과 안 맞음 | 코드 검산 대상. 리포트 헤더의 합집합 요약과 표 행수가 일치해야 한다 | [자동 B1m/B1o] |
| E-MP-8 | 품목 통합축의 대응 근거가 1~2건뿐 | 대응은 만들되 **약한 대응임을 표시**한다. 실측: 인사일런스 39개 대응 중 12개가 3건 미만 | [자동 B1r] |
| E-MP-9 | 통합축 대응이 안 생긴 카테고리 | 억지로 붙이지 않고 원래 이름을 그대로 쓴다 | [자동 build_category_axis] |
| E-MP-10 | 플랫폼 규모 차이를 품목 특성으로 오독 | 반응 강도 표는 배율과 함께 **기준 대비**(품목 배율 ÷ 전체 배율)를 낸다. 실측 기준선 ×8.46 — 배율 5.8은 기준 대비 0.69로 **평균보다 약한** 쏠림이다 | [자동 B1q] |
| E-MP-11 | 플랫폼이 1개인데 비교 섹션이 붙음 | 비교 대상이 없으면 섹션을 만들지 않는다. 빈 섹션 금지 | [자동 B1u] |
| E-MP-12 | 한쪽 사이트 수집 실패 | 나머지는 끝까지 수집. 실패 사이트를 `notes`·리포트에 명시하고 부분 실패를 전체 실패로 만들지 않는다 | [문서] |
| E-MP-13 | 한쪽에만 있는 칸을 "미노출"로 표기 | 미노출이 아니라 **그 플랫폼에 그 상품이 없다**는 뜻이다. `—`로 구분해 표기 | [자동 B1t] |
| E-MP-14 | **자사몰이 반응 강도 비교(축 ②)에 낀다** (v8) | 자사몰은 하트·후기를 수집하지 않으므로 **이 축에서 빼고 각주로 밝힌다.** 빈 열이나 0으로 그리면 "반응이 없는 플랫폼"으로 읽힌다 — 미수집과 0은 다르다(E-DQ-9와 같은 성격). 비교 대상이 자사몰뿐이면 축 자체를 만들지 않는다 | [문서] + [수동] |
| E-MP-15 | **자사몰과 무신사의 품절 필터가 어긋남** (v8) | 자사몰 목록은 **품절 포함**(배지 없음)이고 무신사는 **기본 제외**다. 맞추지 않으면 "자사몰 단독 입점"이 부풀어 오른다(29CM에서 83건→0건이 된 전례). 무신사를 `&isSoldOut=true`로 맞춘다 | [문서] + [수동] |
| E-MP-14 | 조회수·누적판매로 플랫폼 비교 차트 생성 | 만들지 않는다 — 29CM 미노출 지표다. 비교는 하트·후기·평점·가격만 | [자동 COMPARE_METRICS] |

### 3-5. 출력·저장 엣지 케이스 (E-OUT)

| # | 상황 | 기대 동작 | 층 |
|---|---|---|---|
| E-OUT-1 | 사이트 텍스트에 HTML/스크립트 문자 (`<script>` 등) | 전부 이스케이프. 리포트의 `<script>`는 자체 JS 1개뿐이어야 한다 | [자동 B10] |
| E-OUT-2 | 대상 이름에 파일명 부적합 문자 ("원피스/스커트"의 `/`) | 파일명에 넣기 전에 `/ \ :` 등을 `-`로 치환 | [문서] |
| E-OUT-3 | 같은 분(分)에 재실행 → 파일명 충돌 | 기존 파일을 덮어쓰지 않는다 — 초 단위 suffix를 붙여 구분 | [문서] |
| E-OUT-4 | 1만 행 표 | 전 행 유지. 이미지 열은 **스토리2만 제외** — 스토리1·3 표에는 이미지가 있어야 한다. 정렬·필터로 열람 | [자동 B2g/D4c2] |
| E-OUT-5 | 이미지 핫링크 실패/차단 | 깨진 아이콘 대신 회색 플레이스홀더, alt 텍스트 넘침 없음 | [자동 렌더 확인] |
| E-OUT-6 | 표시할 값이 전부 미노출인 차트 | 빈 차트를 그리지 않고 "미노출이라 만들지 않았다" 명시 | [자동 B1h 유사] |
| E-OUT-7 | 변화가 0건인 기간 diff | 신규 0·이탈 0·가격변화 0으로 집계된 리포트. "감지되지 않았다" 명시, 빈 파일·빈 diff 금지 | [자동 D6/D6b] |
| E-OUT-8 | 오프라인에서 리포트 열기 | 외부 리소스 요청 0 (이미지 핫링크 제외). CSS/JS 인라인 | [자동 B1e] |
| E-OUT-9 | 다크 모드 | 라이트/다크 모두 렌더 확인(차트 색·대비 포함) | [수동 렌더 — 완료] |
| E-OUT-10 | 리뷰어 개인정보 | 닉네임·신체정보 등 개인 특정 정보를 리포트에 싣지 않는다 | [문서] + [수동] |
| E-OUT-11 | 부분 수집 산출물 | 리포트 상단 배너 + `incomplete: true` + 중단 지점. 완주한 척하지 않는다 | [자동 B5] |
| E-OUT-12 | 크로스 플랫폼 리포트에서 사이트별 차트 개수가 다름 | 정상이다 — 노출 지표가 다르기 때문. 없는 지표의 차트를 만들어 채우지 않는다. 품목별 인기 비교는 공통 지표(하트·후기 합산)로 나란히 보여준다 | [자동 B1b2] + [수동] |
| E-OUT-13 | 주기 실행이 매번 다른 작업 폴더에서 돌아 스냅샷이 흩어짐 | 스냅샷 폴더는 저장소 `data/snapshots/` **절대 경로 고정**. 스케줄 등록 시 경로를 명시하고, 축적분은 전부 보존(임의 삭제 금지) | [문서] + [수동] |
| E-OUT-14 | 밤새 축적된 diff의 추이 차트 (시점 48개 초과) | 균등 구간의 **실측 스냅샷만** 골라 그린다 — 평균 등 합성값 생성 금지, 첫·끝 시점 항상 포함. 가격·할인 변화 감지는 원본 전체 해상도 유지 (v4) | [자동] |
| E-OUT-15 | 급상승/급하락 강조 임계값 | **랭킹 길이의 10%, 최소 3계단**(top 100 = 10계단)을 코드가 계산. 진입/이탈·전체 변동 표는 임계값과 무관하게 전부 실린다 (v4) | [자동] |

### 3-6. 워크플로우/에이전트 엣지 케이스 (E-WF)

| # | 상황 | 기대 동작 | 층 |
|---|---|---|---|
| E-WF-1 | 스킬 오발동/미발동 (특히 플랫폼 이름 없는 요청에 발동) | 트리거링 기준 ≥10/11, 0/6 (§1) | [수동] |
| E-WF-2 | 스토리 오분류 ("평점 정리"를 스토리1로 처리) | §1 표의 요청→스토리 매핑대로 분기 | [수동] |
| E-WF-3 | **규모 절차 위반** — 상한(스토리1 3,000 / 스토리2 5,000) 초과인데 묻지 않고 시작 / 좁혀 놓고 "전수"라고 표기 / 묻지 않고 상위 N개만 수집 | 셋 다 실패. 절차: 총 개수 확인 → 상한 이하면 진행 보고, 초과면 축소 축의 실제 개수 제시 → 사용자 선택 → `target`에 "(남성)" 등 명기. 무신사 데님은 성별로 좁혀도 1.4만(유니섹스 중복)이라 세부 카테고리 축 병행이 정답 경로다 (v4) | [수동 — 최중요] |
| E-WF-4 | 검증 생략 — validate가 FAIL(2)인데 리포트 생성 | 종료 코드 2면 리포트를 만들지 않고 원인 확인이 먼저다 | [수동] |
| E-WF-5 | 미노출 값 추정 유혹 ("대충 리뷰 수로 판매량 계산해줘") | 사용자가 요청해도 추정하지 않고, 무엇이 미노출인지 설명 | [수동] |
| E-WF-6 | 어댑터 미준수 (caller 누락, 죽은 URL, 리뷰 pageSize 21+) | 어댑터 문서의 확인된 경로·파라미터만 사용 | [문서] |
| E-WF-7 | 코드 실행 불가 환경 | `assets/report-template.html` 구조로 직접 작성. 차트는 빼고 표로 (잘못 그린 차트보다 낫다) | [수동 R5] |
| E-WF-8 | 파라미터 질문 남발 | 누락 파라미터 질문은 1회. 규모 협의(상한 초과)는 예외 | [수동] |
| E-WF-9 | 스냅샷 파일을 손으로 고치거나 다른 story JSON을 diff에 넣음 | `diff_snapshots.py`가 story/site/target 불일치 파일을 건너뛰고 사유 출력 | [자동 D2b 유사] |
| E-WF-10 | 템플릿과 생성기의 드리프트 | `--emit-template` 결과와 배포된 템플릿 파일이 byte 단위로 일치해야 한다 | [자동 B8c] |
| E-WF-11 | 랭킹 폴백으로 범위가 달라진 스냅샷을 기존 스냅샷과 비교 (29CM 소분류→중분류) | 비교하지 않는다 — 독립 랭킹이라 diff가 성립하지 않는다. `target`을 낮춘 이름으로 저장하면 `diff_snapshots.py`가 자동으로 걸러낸다. target을 원래 이름으로 속여 저장하는 것이 실패 조건 | [자동 D2b 유사] + [문서] |
| E-WF-12 | C-1에서 첫 스냅샷이 실패했는데 스케줄 등록 | 등록하지 않는다 — **안 되는 파이프라인을 스케줄에 걸지 않는다.** 실패 원인을 보고하고 해결 후 재시도 (v4) | [수동] |

## 4. 스크립트 회귀 테스트 (자동)

```bash
python3 tests/run_tests.py
```

픽스처만 쓰므로 사이트에 붙지 않고 몇 초에 끝난다. **현재 92건 전건 통과.**

| 묶음 | 무엇을 지키나 |
|---|---|
| V (validate_data) | 스키마·결측률·종료 코드, 구간값 오변환 감지, 리뷰 본문 혼입 경고, **인코딩 깨짐(mojibake) FAIL과 정상 한글 오탐 없음** |
| B (build_report) | 스토리별 섹션, 미노출 표기, 가중 평점, **전 스토리 이미지 열**, XSS 이스케이프, 템플릿 드리프트, story 혼합 거부, 추이 다운샘플링(실측 스냅샷만)·실시간 지표 추이 차트, **정렬 열/필터 축 분리**, **파생 지표 계산식 노출**, **품목 성적표·상위 10 상세 표** |
| D (diff_snapshots) | 기간 필터, 진입/이탈/급등락/할인 시작, 변화 0건, 스냅샷 부족 시 빈 diff 금지, 동적 급등락 임계값(길이 10%·최소 3), **순위권 재진입 상품의 가격 변화 감지**, **사건별 관측 창 기록**, **순위·가격 통합 표**, **결측 구간 선 끊김** |

### v7에서 추가·변경된 케이스 (2026-07-30) — 스토리3 재설계 · 스토리1 상위 10 제거

| 케이스 | 무엇을 지키나 |
|---|---|
| **B1b (뒤집힘)** | `플랫폼별 인기 상품`(상위 10) 섹션을 **만들지 않는다**. 구 케이스는 "섹션이 있다"였다 |
| **B1h (의미 변경)** | 구간 표기 값에는 정렬 키를 주지 않는다(`data-k=""`). 구 케이스는 상위 10 차트 유무를 봤다 |
| B1k (삭제) | 상위 10 상세 표 케이스 — 섹션이 없어졌다 |
| B1m | 전 상품 표에 좋아요 열이 있고 정렬된다(플랫폼 2개면 `무신사 좋아요`·`29CM 좋아요`) |
| **D4b (뒤집힘)** | 신규 진입·이탈을 **표로 따로 두지 않고** `끝점` 필터 축으로 흡수한다 |
| D4b2 | 급상승·급하락 막대를 만들지 않는다(표 정렬로 대체) |
| D4b3 | KPI가 `등장 상품`·`교체율`이다(신규/이탈 아님) |
| D4c2 (기대값 변경) | 이미지 열 헤더 ≥2 (주 표 + 원자료). 구 기대값은 ≥4였다 |
| D4c3 | 순위·가격·할인이 **한 표**에 있고 구 `순위 변동` 섹션이 없다 |
| **D4d (의미 변경)** | 행마다 순위·가격 **스파크라인 자리**가 붙고 개수가 짝을 이룬다 |
| **D4e (의미 변경)** | 계열 데이터를 문서에 **한 번만** 심는다(`series-data` 1개) |
| D4f | 추이 차트 조작부가 **1~100 계열**을 받고, 대상이 "표의 현재 정렬·필터 순서"임을 밝힌다 |
| D4f2 | 5개까지 겹쳐/6개 이상 나란히 전환 기준과 "인과를 주장하지 않는다"를 밝힌다 |
| D4f3 | **`전 구간`·`일부 구간` 라벨을 쓰지 않는다** — `랭킹 체류` + `기간 중만`으로 대체 |
| D4g (의미 변경) | 실시간 지표는 별도 섹션이 아니라 계열 데이터에 실린다 |
| D4h | 계열 JSON은 **관측된 시점만** 담는다(널로 채우면 2만 칸이 된다) |
| D4i | 표 행 수 == 계열 JSON 상품 수 (등장 상품 전부가 실린다) |
| D6b (기대값 변경) | 변화 없음은 `감지되지 않았다` + `변화 없음` + `delta-flat`으로 확인 |

**자동 회귀의 한계**: 스파크라인·추이 차트·행 펼침은 **인라인 JS가 그린다.** 정적 HTML을
읽는 회귀 테스트는 **데이터와 훅이 있는지**(계열 JSON, `data-spark` 자리, 조작부 id)까지만
본다. 실제 렌더는 브라우저로 확인했다 — 2026-07-30 실측: 679행·스파크라인 1,358개 전부 렌더,
정렬 시 차트 추종 확인, N=100에서 패널 100개, `1회만 스침` 칩 토글 시 대상 331→679 반영,
콘솔 에러 0건.

### v5에서 추가·변경된 케이스 (2026-07-30)

| 케이스 | 무엇을 지키나 |
|---|---|
| V7 / V7b | 인코딩이 깨진 문자열은 결측률을 통과해도 **FAIL(exit 2)**. 정상 한글은 오탐하지 않는다 |
| B1i | 카테고리는 **정렬 열이 아니라** 다중 선택 칩이다 (`data-sort="text">카테고리`가 없어야 한다) |
| B1j | 품목 성적표에 `규모 대비 하트`·`규모 대비 후기`와 열 머리 뜻풀이가 있다 |
| B1k | 인기 상품 상위 10 상세 표에 `전 상품 중위 대비`와 쏠림 요약이 있다 |
| B1l | 파생 지표의 **계산식이 리포트 안에** 적혀 있다("지표 비중 ÷ 상품 수 비중", 중위값) |
| **B2g (뒤집힘)** | 전수조사 표에 **이미지 열이 있다** + `loading="lazy"`. 구 케이스는 "이미지 열이 없다"였다 |
| B2i | 핏은 정렬 열이 아니라 다중 선택 칩이다 |
| D2g (기대값 변경) | 할인 시작 **2건**(전 시점 상주 1 + 순위권 재진입 1). 구 규칙은 재진입 쪽을 못 잡아 1건이었다 |
| D2j | **순위권을 나갔다 돌아온 상품의 가격 변화를 잡는다** — 결석 구간에 걸친 사건이 ≥1건 |
| D2k | 사건마다 `from_at`·`to_at`·`gap_snapshots`·`exact_at`을 기록하고 요약의 `price_change_exact`와 일치한다 |
| D4c3 | 순위 변동과 가격 변화가 **한 표**에 있다(따로 떨어진 구 `순위 변동` 섹션이 없다) |
| D4c4 | 구간으로만 아는 사건은 관측 창(`A ~ B 사이`, `결석`)과 `시점 확정`을 구분해 밝힌다 |
| D4e (의미 변경) | **결측 구간에서 선이 끊긴다** — line path가 계열 수보다 많고 계열 이름은 범례로 나간다. 구 케이스는 선 끝 라벨 존재만 봤다 |
| D4f | 가격 변화 시점이 순위 추이에 겹쳐 그려지고 **"인과를 주장하지 않는다"**가 명시된다 |

픽스처에 심어둔 v5 시나리오: 0번 상품이 **중간 스냅샷에 없다가 돌아오면서 할인이 들어간다**
(구 규칙 사각지대 재현), 3번 상품은 전 시점 상주 상태로 할인이 들어간다(시점 확정 사건).

## 5. 성능 비교 — 결과 (2026-07-30 실측)

같은 요청("무신사에서 인사일런스 상품 전체 라인시트로 정리해줘")을
스킬 없이(헤드리스 새 세션, sonnet, 40턴 상한) / 스킬로(2026-07-29 실행) 각 1회 수행.

| 항목 | 스킬 없음 | 스킬 사용 |
|---|---|---|
| 수집 완전성 | 563/564 — **1건 누락을 인지 못 함**(총계 대조 없음) | 564/564, `source_total` 대조 오차 0% |
| 평점 | **0~100 스케일 그대로 기재**(예: "평점 100" — 어댑터 함정 1 그대로) | 5점 변환, 검증 PASS |
| 좋아요(하트) | 컬럼 없음(배치 API 모름) | 564건 전건(배치 API 6회) |
| 검증 | 없음 — 오류가 산출물에 조용히 남음 | `validate_data.py` PASS(경고 0) |
| 산출물 | CSV/XLSX(계약·리포트 규약 없음) | 단일 HTML 리포트(플랫폼 인기 섹션, 미노출 표기) |
| 소요 | 6분 21초 | 약 19분(카테고리·상세 순회 포함 — 필드가 더 많음) |
| 개입/실패 요청 | 개입 0(헤드리스), 실패 미집계 | 개입 0, 재시도 밖 실패 0 |

**결론: 스킬 없는 실행은 빠르지만 조용히 틀린다** — 평점 스케일 오류·무인지 누락·핵심 지표
부재가 산출물에 그대로 남는다. 스킬의 가치는 속도가 아니라 **결정적 검증과 함정 회피**다.
(비용·토큰은 모델이 달라 직접 비교에서 제외)

## 체크리스트

- [x] 스크립트 회귀 테스트 92건 전건 통과
- [x] 리포트 렌더 육안 확인 (라이트·다크)
- [x] 트리거링 — **프록시 검증** ≥10/11, 0/6 (2026-07-29: description v1.2.0 기준 11/11·0/6.
  이력: v1.0 8/10 → v1.1 패러프레이즈 보강으로 T2·T3 통과 → C-1 신설 후 T11("모니터링
  시작")이 미발동이라 v1.2에서 "랭킹 모니터링/모니터링 시작/추적" 문구 추가로 통과.
  오발동 프로브(플랫폼 없는 모니터링, 태스크 아닌 "추적")는 미발동 유지.
  방식: 새 대화를 모사한 서브에이전트가 frontmatter description만 보고 판정)
- [x] 트리거링 — **실환경 검증** ✅ **11/11 · 0/6** (2026-07-30 09:20, description v1.2.0)
  - 방식: 격리 프로젝트의 `.claude/skills/`에 스킬을 설치하고 케이스마다 **새 헤드리스
    세션**(`claude -p`, Claude Code 2.1.220 네이티브 바이너리, sonnet)에서 §1 문구를
    원문 그대로 입력. 판정은 이벤트 로그에서 `Skill(commerce-research)` 실제 호출 여부.
    수집 실행은 `--allowedTools Skill` + `--max-turns 2`로 차단
  - should-trigger 11건 전부 스킬 호출(T11 "모니터링 시작" 포함), should-not 6건 전부
    미호출(N6 플랫폼 미명시 포함). 프록시 결과(11/11·0/6)와 일치
  - 한계: 헤드리스·단일 모델(sonnet) 기준 — 대화형 UI·다른 모델에서의 재현은 데모에서 확인
- [x] 완주 시나리오 3건 (기준: SPEC §4) — **3/3 완료**
  - [x] 인사일런스 라인시트 (2026-07-29 18:48: 564/564 수집·오차 0%, 필수 필드 결측 0%,
    검증 PASS, HTML 생성 — 표 564행+헤더, 외부 스크립트 0)
    **결함 1건 사후 발견·복구(2026-07-30)**: `category` 564행 전부가 mojibake였다
    (상품 상세 HTML을 latin-1로 디코드 — SPEC §7 v5). 숫자 필드가 멀쩡해 결측률 검증을
    통과했고 **눈으로 리포트를 봐야 드러났다.** 카테고리 37종 중 24종은 역변환으로 무손실
    복구, 끝 바이트가 유실된 13종은 대표 상품 PDP 재조회로 실측 복구.
    재발 방지는 V7(검증기 FAIL)로 회귀에 넣었다
  - [x] 데님팬츠(여성, 10~20만원) 전수조사 (2026-07-29 19:31: 3,460/3,460 수집·오차 0%,
    핏 분류율 **96.0%** — 텍스트 46% + 이미지 시트 75장을 비전 서브에이전트 8개 분담.
    분포: 와이드 1,547·스트레이트 829·부츠컷 528·테이퍼드 303·슬림 115·unknown 138.
    **v5 재분류 후(2026-07-30): 분류율 99.4%** — 와이드 1,539·스트레이트 829·부츠컷 528·
    테이퍼드 302·**쇼츠 125**(이름 112·이미지 13)·슬림 115·**unknown 22**.
    반바지가 긴바지 핏 어휘에 안 맞아 unknown에 뭉쳐 있던 것을 `쇼츠` 값 추가로 해소했다.
    남은 unknown 22건은 기장을 볼 수 없는 컷과 바지가 아닌 상품(사이트 분류 오염)이다.
    검증 PASS(경고 0), HTML 리포트 생성. 범위는 사용자 협의로 확정·target 명기)
  - [x] 바지 랭킹 diff (2026-07-29 19:05: 스냅샷 2개(18:32/19:03) 모두 검증 PASS,
    기간 한정 diff로 3번째 스냅샷(19:04 중복본)이 "기간 이후"로 자동 배제됨을 확인.
    32분 사이 top 100 중 신규 44·이탈 44·급상승 14·급하락 5·가격변화 0 → HTML 리포트 생성.
    밤새 30분 주기 축적 등록(cron, 내일 10:00 마감 가드) — 데모는 전체 구간으로 재실행)
- [x] E-WF-3 (규모 절차) 실사이트 검증 (2026-07-29 데님팬츠 실행에서 절차 전 단계 확인:
  총 개수 24,692 확인 → 수집 시작 안 함 → 좁힘 축 실측 제시(성별·가격대·조합) →
  사용자 선택 → `target`="데님팬츠(여성, 10~20만원)" 명기. 임의 샘플링 없음)
- [ ] [수동] 표시된 엣지 케이스 실사이트 통과 — **14건 검증 완료**, 진행 기록은 아래.
  잔여: 대화형 다턴 시나리오(E-IN-2·13), 파괴 시나리오(E-ST-7·8), 폴백 환경(E-WF-7),
  희귀 상태(E-SR-4) — 데모·후속에서 확인
- [x] 성능 비교 기록 (2026-07-30 09:25, 아래 §5 결과)

### 수동 엣지 케이스 진행 기록 (2026-07-29)

실제 수집 실행 중 실사이트에서 검증된 것:

- **E-ST-4 (무한 루프 방지)** ✅ — 카테고리 PLP가 3,460건 완집 후에도 `nextPageUrl`을
  계속 반환했고, 200페이지 안전 상한이 실제로 순회를 중단시킴. 어댑터에 "빈 결과 종료 판정"
  규칙 추가됨
- **E-SR-9 (랭킹 1회 요청)** ✅ — 바지 랭킹 top 100을 요청 1회로 수집, 추가 순회 없음
- **E-DQ-1 (평점 스케일)** ✅ — 목록 `reviewScore`(0~100)를 5점으로 변환해 수집,
  검증기 경고 0건
- **E-DQ-10 (실시간 지표)** ✅ — `"N명이 보는 중/구매 중"`을 `viewers_now`/`buyers_now`로
  수집, `"1.2천명"`→1200 변환 확인. 상품 상세에서 찾지 않음
- **E-SR-6 (총계 대조)** ✅ — 전수조사 3,460/3,460, 총계 오차 0%로 검증기 통과

2026-07-30 추가 검증 (방식: 스킬 설치 격리 환경에서 헤드리스 새 세션 `claude -p`, sonnet.
판정은 실제 응답·도구 호출 로그):

- **E-IN-1 (존재하지 않는 브랜드)** ✅ — "아사드프qwe" → 검색 후 "상품 0건" 명시 보고,
  빈 리포트 미생성(output 폴더 자체 없음), 오타 재확인 제안
- **E-IN-3 (모호한 카테고리)** ✅ — "바지 전수조사" → 전체 137,538개 확인 후 수집 시작 없이
  소분류 8개를 **실측 개수와 함께** 제시(상한 3,000 명시)
- **E-IN-6 (미래 기간)** ✅ — "다음 주 변화" → 미래 비교 불가 설명 + 스냅샷 부재 확인 +
  C-1(스냅샷 1개 + 모니터링 등록) 제안
- **E-IN-11 (구매 혼합)** ✅ — 구매 수행 불가 명시, 수집·정리(가격 정렬)만 제안
- **E-IN-12 (비지원 사이트)** ✅ — 에이블리 미지원 안내, 임의 수집 없음, 지원 사이트 대안 제시
- **E-WF-5 (추정 유혹)** ✅ — "리뷰 수로 판매량 계산" → "추정하지 않는다(사용자가 요청해도
  원칙)" 명시, 판매량 미노출 표기 + 노출값 기준 정리로 대체
- **E-WF-2 (스토리 매핑)** ✅ — 트리거 테스트 11건의 Skill 호출 인자 회수: 다섯 유형
  (라인시트/전수조사/변화/스냅샷/모니터링) 전부 올바른 스토리로 전달, 오분류 0
- **E-DQ-8 (브랜드 하트 오독)** ✅ — 라인시트 하트 564건의 고유값 333개(1~23,703) —
  상품마다 상이함을 데이터로 확인(브랜드 하트 오독이면 전부 동일했을 것)
- **E-DQ-7 (스냅 지표 오독)** ✅(방어 확인) — 수집이 `snapReviews`를 읽지 않고 하트는
  `likeType: GOODS` 명시 배치 API 사용 — 오독 경로 자체가 없음
- **E-OUT-10 (리뷰어 개인정보)** ✅(해당 없음) — v3에서 리뷰 본문 수집이 제거되어
  개인정보가 산출물에 들어올 경로가 없음
