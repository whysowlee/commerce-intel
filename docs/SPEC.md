# commerce-research 스킬 스펙

- 상태: **v16 (2026-07-30)** — **수집 경로 재반전: 경로 A(API) 우선 · 경로 B(브라우저) 백업** ·
  확정 스킴(API·URL)이 바뀌면 어댑터를 개정하는 유지보수 모델 명문화
- **최근 수정: 2026-07-30 18:55**
- 작성 기준: The Complete Guide to Building Skills for Claude
- 컨펌: 선빈 (미완)

> **v16에서 바뀐 것 (사용자 결정, 2026-07-30):**
> - **수집 경로를 경로 A(API) 우선으로 되돌렸다.** v6의 "경로 B 우선"을 재반전한다.
>   경로 B(브라우저)는 **백업 경로**다 — 경로 A가 막히거나(스킴 변경·차단), 경로 A가
>   주지 않는 값(화면 전용 값·화면 총계)이 필요할 때 쓴다
> - **유지보수 모델: 확정적인 스킴(API 엔드포인트·URL·파라미터)이 바뀌면 스킬을
>   업데이트한다.** 어댑터는 실측 일자와 함께 검증된 스킴을 기록하고, 런타임에서 스킴
>   변화가 감지되면(400/404 · 필드 소실 · 결측률 급증) **경로 B로 폴백해 작업을 완주한 뒤
>   스킴 변화를 보고**한다. 어댑터 개정은 사용자 확인 후 수행한다
> - **v6의 실측 사실은 전부 유효하다 — 결론만 바뀐다.** 경로 A의 구조적 누락
>   (29CM BEST API 435 중 422), 화면 총계 대조 규칙, 순환 검증 금지는 그대로다.
>   따라서 **경로 A로 모을 때도 완전성 검증은 독립 총계 대조가 필수**이고,
>   총계를 주지 않는 API(29CM BEST)로 "전량"을 모을 때는 **화면 총계(경로 B)와 대조하고
>   어긋나면 경로 B로 보완**한다 (§2)
> - 무신사는 2026-07-30에 실시간 지표 원시값 API(§4-1, 어댑터)가 확인되어 화면 전용
>   값의 대부분(보는 중·조회수·누적판매·브랜드 하트)이 경로 A로 조회된다 — A 우선의
>   실효성이 커졌다
>
> **v15에서 바뀐 것 (사용자 결정, 2026-07-30):**
> - **축약 표기(`1.2천`·`판매 9만개`)를 정수로 파싱하는 것을 허용한다** — v6의 금지를
>   뒤집는다. 근거는 오차 성질이다: 축약은 반올림이라 **±4% 이내로 유계**이고 그 정도로
>   뒤집히는 판정이 이 스킬에 없다. **구간 표기(`300회 이상`)는 참값 상한이 없으므로
>   금지 규칙을 그대로 유지한다** (§6)
>   - 원문은 가능하면 `*_count_display`에 함께 담는다. **축약 원문과 정수가 같이 있어도
>     검증기가 경고하지 않는다** — 구간 원문과 정수가 같이 있을 때만 경고다
>   - 반올림 노출값이라는 사실은 리포트 각주로 밝힌다(정밀도가 생긴 것이 아니다)
>   - **기존 축적분이 소급 정합된다** — 랭킹 스냅샷 44개의 `purchase_count` 2,880건과
>     `viewers_now` 138건이 이미 파싱된 값이었다. 마이그레이션 대상 없음
>   - v6과 **E-DQ-10(“1.2천명”→1200)의 오래된 모순도 이 결정으로 해소된다**
>
> **v14에서 바뀐 것 (사용자 결정, 2026-07-30):**
> - **자사몰이 비범위 → 범위로 옮겨졌다.** 스토리1의 비교 플랫폼에 브랜드 자체 운영
>   쇼핑몰이 들어온다. **엔진은 Cafe24만** 지원하고 고도몰·임웹·Shopify 등은 비범위로
>   남는다(§3). 새 어댑터는 `references/cafe24.md`다
>   - 실측 스토어: 인사일런스(`insilence.co.kr`)·2000아카이브스(`2000archives.com`).
>     둘 다 Cafe24 확인(`cfa-js.cafe24.com` 등)
>   - **사이트가 하나가 아니라 브랜드마다 도메인이 다르다.** 사용자가 브랜드명만 말하면
>     **검색해서 도메인을 찾고, 브랜드가 맞는지 확인한 뒤, Cafe24인지 판별**한다.
>     `meta.site`에는 도메인 문자열이 들어간다(`insilence.co.kr`)
> - **자사몰은 후기 수·평점·좋아요·조회수·누적판매를 수집하지 않는다 — 전부 `null`이다.**
>   근거는 두 가지이고 둘 다 실측이다:
>   - **robots.txt가 막는다.** 두 스토어 모두 `User-agent: *`에 `Disallow: /*page_4=`
>     (상세 안 후기 게시판 페이지 넘김)와 `/exec/front/`(리뷰 더보기 경로)가 있다.
>     §1의 "우회 없이 사이트가 밝힌 정책 안에서 동작한다"에 따라 우회하지 않는다
>   - **화면에 개수 자체가 없는 스토어가 있다.** 인사일런스는 외부 위젯(크리마)을 쓰는데
>     상세에 위젯이 마운트조차 되지 않고, Cafe24 기본 후기 게시판은 비활성이다
>   - **영향: 자사몰은 스토리1 비교 축 ②(같은 상품 반응 강도)에서 빠진다.**
>     축 ①(품목별 입점 차이)·축 ③(가격·할인 포지셔닝)은 그대로 성립한다.
>     빠진 사실을 리포트 각주로 밝힌다
> - **자사몰 전량은 카테고리를 전부 돌아 상품번호로 중복 제거해 얻는다.**
>   `ALL` 카테고리 하나로는 부족하다 — 인사일런스 실측: 남성 `ALL` 222 + 여성 `ALL` 206 =
>   합집합 **428**인데 `SEASON OFF` 349 중 **127건(23%)이 `ALL`에 없다.** 전체 합집합은 **555**.
>   Cafe24에는 스토어 전체를 주는 루트 카테고리가 없다(`cate_no=1`·`/product/list.html` 404)
> - **자사몰은 `source_total`이 `null`이다 — 화면 총계가 없다.** 완전성 근거는
>   **"네비게이션의 모든 상품 카테고리 소진 + 각 카테고리가 `btnNext=#none` 도달"**이다.
>   29CM 브랜드 목록의 `hasNext=false` 근거와 같은 성격이다. ±5% 판정은 적용하지 않는다
> - **목록의 품절 처리가 세 번째 패턴이다** — 무신사는 기본 제외, 29CM는 포함+배지,
>   **자사몰은 포함+배지 없음**이다(상세 JSON-LD `offers`로만 판정). 실측: 목록 192개에
>   품절 문자열 0건인데 그중 `product_no=7489`는 상세에서 「품절」이다.
>   **비교 시 무신사를 `&isSoldOut=true`로 맞춰야 한다**
>
> **v13에서 바뀐 것 (사용자 확인, 2026-07-30):**
> - **스토리1 상한은 3,000이 맞다.** 본문은 v6에서 이미 3,000으로 올라갔는데 **근거 문장만
>   "2,000개 ≈ 40분"으로 남아 서로 모순**이었다. 3,000개 ≈ 60분으로 맞췄다
> - 근거에 **카테고리도 상세에만 있다**는 사실을 추가했다 — 목록만 쓰면 `category` 결측
>   100%로 검증이 FAIL한다(2026-07-30 실측). 상세 방문이 지배 비용인 이유가 하나 더 늘었다
>
> **v12에서 바뀐 것 (사용자 제안, 2026-07-30):**
> - **색상 변형을 묶어 속성 분류 횟수를 줄인다.** 같은 상품의 색상 전개가 각각 별개
>   상품으로 올라오는데 색상은 핏을 바꾸지 않는다 — 그룹당 한 번만 판단하면 된다
>   - 새 스크립트 **`group_variants.py`** (`--plan` / `--apply`). scripts가 3종 → 4종 (§2)
>   - 실측: 슬랙스 3,004개에서 미분류 1,492건 → **판단 1,071회(28% 감소)**.
>     색상 전개가 많은 카테고리(티셔츠·데님)일수록 절감이 커진다
>   - **안전성 검증**: 서로 다른 핏이 섞인 그룹 **0개**. 섞이면 `conflict`로 표시하고
>     전파하지 않는다
>   - 묶는 규칙은 결정적이다 — **같은 브랜드 + 색상·품번·괄호를 뗀 이름 완전일치**뿐.
>     유사도 매칭은 쓰지 않는다(v6에서 실측으로 기각한 방법)
>   - 데이터 계약에 `attributes_basis: group` 추가 — 전파받은 값과 직접 본 값을 구분한다
>
> **v11에서 바뀐 것 (사용자 지적, 2026-07-30):**
> - **`단독 입점 상품 성격`을 `품목별 입점 차이`에 합쳤다.** 그 섹션의 품목 분포 막대가
>   커버리지 표의 `단독` 열과 **같은 값을 같은 순서로** 그리고 있었다(실측: 동점
>   tie-break만 달랐다). 표를 그 열로 정렬하면 같은 답이다
> - 단독 입점의 고유 정보는 **가격대**뿐이라 사이트별 `단독 중위가` KPI와
>   **단독 판매가 분포** 차트로 남겼다(분포는 신설 — 중위값 하나로는 고가 라인 여부를 못 본다)
> - 비교 레이어가 **4블록 → 3블록**, 스토리1 섹션이 **7 → 6**개가 됐다
>
> **v10에서 바뀐 것 (사용자 지적, 2026-07-30):**
> - **`가격·할인 포지셔닝 차이`의 상품 표를 `전 상품` 표에 합쳤다.** 구 구조는 매칭 상품
>   전 건을 두 곳에 실어서 **같은 상품이 리포트에 두 번** 나왔다(로우클래식 합집합 741행,
>   매칭 283건 중복). 포지셔닝 섹션은 **품목 단위 집계만** 남는다 (§4 스토리1)
> - 합집합 표에 **`가격 차이` 열**(사이트A 판매가 − 사이트B 판매가)과 **`가격` 필터 축**
>   (`판매가 다름` / `판매가 동일` / `비교 불가`)이 붙는다. 한쪽만 입점한 상품은 미노출이
>   아니라 **비교 대상이 아니다** — 그렇게 구분해 표기한다
> - 리포트에서 **상품 목록이 나오는 표는 `전 상품` 하나뿐**이 됐다
>
> **v9에서 바뀐 것 (사용자 지적·결정, 2026-07-30):**
> - **지금까지 무신사에서 품절 상품을 못 받고 있었다.** 목록은 `isSoldOut=true`를 줘야
>   품절을 준다 — 화면·API 모두 그렇다. 파라미터 후보 10개를 스윕해 이 하나만 유효함을
>   확인했고, 응답이 `false`/`true` 혼합이라 "품절만"이 아니라 **"품절 포함"**이다 (§7)
>   - 실측 영향: 로우클래식 **261 → 440**(+179, 41%),
>     슬랙스 여성 10~20만원 **1,903 → 3,004**(+1,101, 37%)
>   - **랭킹(스토리3)은 해당 없다** — `soldOut`/`isSoldOut`을 줘도 101건 중 품절 0건
> - **스토리2 규모 상한 3,000 → 5,000** (사용자 결정). 품절을 포함하면 같은 축의 실제
>   규모가 1.5배가 되므로 구 상한으로는 협의가 과도하게 자주 걸린다
> - **리포트 표에 `판매` 필터 축 추가 — 기본은 판매 중만.** 품절을 함께 수집하면 표가 두 배로
>   길어져 훑기 어렵다는 지적. 칩으로 품절을 켜고 건수는 칩에 찍힌다(조용히 숨기지 않는다).
>   스토리1(단일·합집합)·스토리2에 적용 (§4 리포트 공통)
> - **§7 함정 추가 — 무신사 카테고리 화면은 스크롤하면 성별 필터를 잃는다.** 성별로 좁힌
>   전수조사는 경로 A만 재현할 수 있다
>
> **v8에서 바뀐 것 (사용자 확인, 2026-07-30):**
> - **판매가 정의 개정 — `price_sale`은 전 회원 공통 쿠폰적용가다.** 무신사 상세의 안내
>   문구가 "모든 회원이 사용 가능한 상품 쿠폰 중 최대 할인 쿠폰을 기준으로 산정"이라고
>   밝힌다(사용자 화면 확인). 개인화 가격이 아니므로 재현 가능하고 상품 간 비교도 성립한다
>   - 무신사: `price_sale` = **`finalPrice`**, `discount_rate` = **`finalDiscount`**
>     (구 `price`/`saleRate`는 쿠폰 전 값). 로우클래식 261개 실측에서 할인 중이
>     **4개 → 115개**로 바뀐다 — 5% 쿠폰 102개를 "할인 없음"으로 기록하고 있었다
>   - 29CM: `price_sale` = **`displayPrice`**(상세 `totalDiscountedItemPrice`),
>     `discount_rate` = **`saleRate`**(상세 `totalDiscountedRate`). 구 규칙(`sellPrice`만)은 폐기.
>     **⚠️ 단 상세의 `totalDiscountedItemPrice`는 화면 메인 판매가가 아니다** — 「나의 구매
>     가능 가격 / 첫 구매가」로 **앱전용·신규 쿠폰이 얹힌 개인화 가격**이고, 2026-07-30
>     브라우저 실측에서 58건 중 **32건(55%)이 `displayPrice`와 달랐다.** 판매가는 목록
>     경로에서 담는다(어댑터 §4)
>   - **랭킹 API는 원래부터 쿠폰적용가였다** — 무신사 `info.finalPrice`·`discountRatio`를
>     PDP와 대조해 확인(6007700: 134,400/20% = `couponPrice`/`totalDiscount`).
>     기존 스냅샷은 정상이다
>   - **미검증 해소 — 문구는 없고 동작으로 확인했다 (2026-07-30 브라우저 실측)**:
>     29CM 상세에는 판매가 산정 근거를 밝히는 문구가 **아예 없다**(비로그인 4개 상품에서
>     `모든 회원`·`전 회원`·`누구나`·`기준으로 산정` 0건, 가격 영역 툴팁도 없음).
>     대신 ① 화면 판매가가 `displayPrice`와 **4/4 일치** ② 가격을 정하는 상품쿠폰 43건이
>     **전부 무조건부**(첫구매·등급·신규·앱전용 0건)이고 43/43 선착순 총량 제한 없음
>     ③ 최대 할인 쿠폰 자동 선택이 무신사와 같은 동작 — 이 세 가지로 확인했다.
>     따라서 판매가 정의(전 회원 공통 쿠폰적용가)는 유지한다
>
> **v7에서 바뀐 것 (사용자 결정, 2026-07-30):**
> - **스토리3 레이아웃 재설계.** 섹션 8개 → 4개. 구 구조는 표 4개(신규 진입·이탈·순위 변동·
>   가격 변화)가 열 구성이 거의 같았는데 **등장 상품 679개 중 158개(23%)만** 실렸다 —
>   중복이면서 동시에 누락이었다 (§4 스토리3)
> - **한 표에 등장 상품 전부.** 신규 진입·이탈 표를 없애고 `끝점` 필터 축으로 흡수.
>   급상승·급하락 막대도 없앴다(표 정렬로 같은 답이 나온다)
> - **`전 구간`/`일부 구간` 폐기 → `랭킹 체류 28/33회` 숫자 + `끝점` 배지 4종.**
>   그 라벨은 "첫·마지막 스냅샷 둘 다에 있었나"라는 구현 사정을 말하는데 읽는 사람은
>   "관측이 온전한가"로 읽었다
> - **처음·마지막 두 점 대신 추이를 싣는다.** 행마다 순위·가격 스파크라인, 그리고
>   **순위(좌축)·가격(우축)·할인 구간(배경 띠)을 한 좌표계**에 놓는 패널
> - **추이 차트 계열 수를 사용자가 정한다(1~100).** 대상은 **표의 현재 정렬·필터 순서**라
>   차트가 따로 선택 규칙을 갖지 않는다. 5개까지 겹쳐 보기, 6개 이상 나란히 보기
> - **KPI에서 신규 진입·이탈을 빼고 `등장 상품 수`·`교체율`로 대체** (§4 스토리3)
> - **계열 데이터를 문서에 한 번만 심고 인라인 JS가 그린다** — 스파크라인·차트·행 펼침이
>   같은 데이터 하나를 쓴다. 외부 리소스는 여전히 0이다 (§4 스토리3)
> - **스토리1 `플랫폼별 인기 상품`(좋아요 상위 10) 제거** — 전 상품 표를 좋아요로 정렬하면
>   같은 답이 나온다. 같은 답을 두 번 그리지 않는다 (§4 스토리1)
>
> **v6에서 바뀐 것 (사용자 결정, 2026-07-30):**
> - **스토리1의 핵심이 "한 브랜드를 여러 플랫폼에서 비교"로 재정의됐다.** 입력은
>   브랜드 + **플랫폼 1개 이상**이다. 1개면 기존 단일 라인시트와 같고, 2개 이상이면
>   합집합 라인시트가 된다. 플랫폼은 확장 가능한 축으로 다룬다 (§4 스토리1)
> - **`플랫폼 간 동일 상품 매칭`이 비범위 → 범위로 이동했다** (§3). 단 매칭 규칙은
>   **정규화 상품명 완전일치뿐**이고 **퍼지·가격 보조 매칭은 금지**다. 근거(인사일런스
>   실측 2026-07-30): 유사도 0.82↑ 후보 11건을 열어보니 `헨리넥 숏슬리브`↔`와플 헨리넥
>   롱슬리브`, `프렌치 워크 자켓 FADED BLUE`↔`FADED BLACK`처럼 대부분 다른 상품이었고,
>   `레터링 그래픽 티셔츠`↔`스탠실 그래픽 티셔츠`는 **정가까지 66,000원으로 같아
>   가격이 오탐을 확증**했다. 가격은 보조 신호로도 못 쓴다
> - **라인시트는 플랫폼 합집합 + `입점` 칼럼**이다. 매칭된 상품은 1행이고 어느 플랫폼에
>   입점돼 있는지가 칼럼에 찍힌다. 미매칭은 오류가 아니라 **단독 입점 사실**이다 (§4 스토리1)
> - **품목 비교축을 매칭 상품에서 자동 추출한다** — 사람이 매핑 표를 쓰지 않는다.
>   근거: 인사일런스 기준 29CM 43종 / 무신사 37종 카테고리 중 **이름이 그대로 겹치는 건
>   8종뿐**이라(실측) 원본 이름으로는 축이 맞지 않는다 (§4 스토리1)
> - **플랫폼별 품목 인기 분석을 4개 축으로 고도화** — ① 품목별 입점 커버리지 갭
>   ② 같은 상품 반응 강도 비교 ③ 가격·할인 포지셔닝 차이 ④ 단독 입점 상품 성격 (§4 스토리1)
> - **29CM 브랜드 목록에 경로 A가 열렸다** (2026-07-30 실측) — BEST API의
>   `brandFacetInputs.frontBrandNo`. 29CM이 P1 제약에서 벗어났다 (§2·§3·§7).
>   **단 이 경로는 전량이 아니다 — 아래 항목 참조**
> - **수집 경로를 경로 B(브라우저) 우선으로 뒤집었다** (사용자 지시 "다 뜯어고쳐").
>   이 프로젝트의 목적 자체가 API가 없는 사이트를 브라우저로 다루는 예제이고,
>   **실측이 경로 B가 더 완전하다는 것을 보였다** (§2):
>   - **경로 A(29CM BEST API)는 전량이 아니다** — 인사일런스 카탈로그는 435개인데
>     경로 A는 **422개**만 준다. 수집분은 화면 집합의 부분집합이고 유령은 0건이다.
>     **누락 원인은 특정하지 못했다** — 품절 필터도 아니고(수집분에 품절이 이미 120건 있다)
>     저반응 때문도 아니다(누락분의 좋아요 4~39·후기 0~3은 수집분이 커버하는 범위다).
>     누락이 품절 쪽으로 3배 치우칠 뿐이다(누락분 85% vs 수집분 28%).
>     **어떤 상품이 빠질지 예측할 수 없으므로 화면 총계 대조 없이는 전량을 주장할 수 없다.**
>     브라우저 브랜드 페이지는 품절을 `품절` 배지와 함께 포함하며 435건 전부를 준다
>   - **화면은 총계를 노출한다** — 브랜드 페이지에 `435개`가 찍힌다. 경로 A는 총계가 없어
>     `source_total`에 수집 건수를 넣었고, 그래서 `총계 대비 오차 0.0%`가 **순환 검증**이었다.
>     경로 B에서는 이 검증이 실제로 성립한다
>   - 문서에 "껍데기만"으로 적혀 있던 **무신사 랭킹 · 29CM 카테고리·브랜드 목록이 브라우저에서는
>     전부 완전 렌더된다.** 구 표는 전부 서버 HTML 기준이었다
> - **스토리3 랭킹 코드가 경로별로 다르다** — 화면(경로 B)은 6자리만 받아 바지 전체가
>   `003000`이고, 대분류 `003`을 주면 **에러 없이 다른 랭킹**(`sectionId=200&categoryCode=000`)이
>   온다. top 100 고정은 유지한다 (§4 스토리3·§7)
> - **스토리 A·B의 조회수(`viewers_now`)는 구조적 `null`이다** — 카테고리 목록·브랜드 목록·
>   상품 상세 어디에도 `N명이 보는 중`이 없다(실측). 랭킹 화면에만 있다 (§6)
> - **스크롤 종료 판정 = 3연속 무증가**, 축약 표기는 원문 보존 + HTTP 보완 (§7)
>
> **v5에서 바뀐 것 (사용자 결정, 2026-07-30):**
> - **스토리2 표에 이미지 열을 넣는다** — v3의 "전수조사 표는 이미지 열 제외" 결정을
>   뒤집었다. 전 행 유지 원칙은 그대로이고 썸네일은 `loading="lazy"`다 (§4 스토리2)
> - **핏 값에 `쇼츠` 추가** — 기존 다섯 값은 긴바지 레그 실루엣 축이라 반바지에 정답이
>   없었다. 실측: 쇼츠 107개 중 96개가 `unknown`이고 11개는 `와이드`가 붙어 갈렸다.
>   축을 쪼개지 않고 한 값을 더하는 쪽을 택했다("와이드 쇼츠"는 표현되지 않는다) (§4 스토리2)
> - **카테고리·속성은 정렬 열이 아니라 다중 선택 필터 축이다** (§4 리포트 공통)
> - **스토리1 분석 심화** — 품목 성적표(규모 대비 반응 지수)와 인기 상품 상위 10 표
>   (전 상품 중위 대비)를 추가 (§4 스토리1)
> - **스토리3 순위 변동과 가격·할인 변화를 한 섹션으로 합친다** — 통합 표 + 순위 추이에
>   변화 시점 오버레이 (§4 스토리3)
> - **가격·할인 변화 감지 기준 개정: 연속 스냅샷 쌍 → 상품별 마지막 관측 상태 대비.**
>   근거(33개 축적분 실측): 등장 상품 679개 중 전 시점 상주는 7개(1%)뿐이고,
>   연속 관측 쌍 2,621개 중 구 규칙이 비교하는 것은 71%였다. 나머지 29%가 사각지대라
>   실제 변화 2건을 **0건으로 보고**했다. 시점 정밀도는 관측 창으로 밝힌다 (§4 스토리3)
> - **§7 인코딩 함정 + 검증기 mojibake FAIL** — 무신사 상품 상세에 charset 선언이 없어
>   latin-1로 읽히면 한글이 조용히 깨진다. 실제로 `category` 564행이 깨진 채 리포트까지 나갔다
>
> **v4에서 바뀐 것 (사용자 결정, 2026-07-29):**
> - **수집 규모 상한을 스토리별로 분리** — 구 "1만 개" 단일 기준 폐기. 상한의 근거를
>   목록 수집량이 아니라 **총 소요 시간을 지배하는 비용**(상세 방문·이미지 분류)으로 교정.
>   스토리1 **2,000개**(~40분) / 스토리2 **5,000개**(~3시간) / 스토리3 top 100 고정 (§4)
>   — **스토리1 상한은 v6에서 3,000으로 올렸다** (2026-07-30 사용자 결정, 아래 참조)
> - §4 부록 성별 규모 실측 완료 — 무신사는 남+여 > 전체(유니섹스 중복 집계)라
>   성별 축만으로는 데님팬츠가 상한 아래로 내려오지 않는다
> - §7 함정 추가 — 무신사 카테고리 PLP API는 page≥2가 403 (카테고리 전량은 경로 B 기본)
> - **스토리3 동작 2개로 분리** — C-1 모니터링 시작(스냅샷 1개 + 스케줄 등록, 스케줄링이
>   스펙 안으로) / C-2 변화 조회(기간 필수). 축적 주기는 **사이트별 원본 갱신 주기**
>   (무신사 30분 / 29CM 1시간 — 구 "30분 일괄" 대체)
> - 스토리3 판정 기준 확정 — 급등락 강조 **랭킹 길이의 10%(최소 3계단)**, 추이는
>   48개 초과 시 **균등 구간 대표 스냅샷**으로 요약(평균 금지), 할인은 **감지**(예측 아님),
>   실시간 지표(`viewers_now`/`buyers_now`) **추이 차트 포함**
>
> **v3에서 바뀐 것 (사용자 결정, 2026-07-29):**
> - **사이트 기본값 제거** — 전 스토리에서 사이트는 필수 입력이다. 요청에 플랫폼
>   이름이 없으면 트리거되지 않고, 실행 중 미지정이면 무신사로 가정하지 않고 묻는다
> - 스토리2에서 **리뷰 본문 수집을 제거**했다. 만족/불만족 판단 재료는 사이트가 노출한
>   **후기 수·평균 평점**뿐이다 (두 사이트 모두 목록/상세에서 바로 보인다)
> - 수집 규모 기준: 상품 1만 개까지는 묻지 않고 진행, 초과 시 범위 협의 (**v4에서 폐기**)
> - 대형 리포트: **표 전 행 유지 + 전수조사 표는 이미지 열 제외**
> - 전수조사 조사 칼럼 확정 — 분류 속성은 핏만 P0 (§4 스토리2)
> - 스토리3 축적 확정 — **30분에 1번**, 스냅샷 공간은 저장소 `data/snapshots/` 고정
>   (git 미추적), 데모 단계 전부 보존, 실행 주체는 Claude Code 스케줄 기능 (§4·§5)
>
> v2에서 바뀐 것 (실사이트 조사): 노출 지표 목록(§6), 수집 경로 2단 폴백(§2),
> 강건성 함정 목록(§7), 실시간 지표·구간 표기 필드(§6).

## 1. 배경

무신사·29CM는 공개 API를 제공하지 않는다. 이 프로젝트는 두 사이트에서 자연어 태스크를
수행하는 **강건한 스킬의 예제**를 만든다. 스킬은 Agent Skills 개방 표준을 따르며 특정
실행 환경에 국한되지 않는다.

### 접근 정책 (조사로 확인)

- 무신사 `robots.txt`는 일반 봇을 `Disallow: /`로 막지만 **`Claude-User`·`Claude-SearchBot`은
  `Allow: /`로 전면 허용**한다.
- 29CM `robots.txt`는 `*`에 `Allow: /`이고 로그인 영역만 막는다.
- 두 사이트 모두 403·캡차·봇 챌린지 없이 전 페이지 200이 확인됐다.

즉 이 스킬은 **우회 없이 사이트가 밝힌 정책 안에서** 동작한다.
User-Agent 위장은 금지한다(§7).

## 2. 실행 환경과 아키텍처

### 실행 환경 (둘 다 지원)

| 프로필 | 페이지 접근 | 후처리·리포트 생성 |
|---|---|---|
| Aside (주 환경) | 내장 에이전트가 스킬의 사이트 플레이북을 따라 수행 | 코드 실행 가능 시 `scripts/`, 불가 시 `assets/report-template.html` 구조를 따라 직접 생성 |
| Claude Code | 환경이 제공하는 브라우저 제어 도구로 플레이북 수행 | `scripts/` (deterministic) |

**전제 조건 (v16 개정): 기본 수집은 웹 요청(JSON API)으로 한다** — 웹 페이지·API를
가져올 수 있는 도구면 된다. **브라우저를 제어할 수 있는 도구**(페이지 열기·스크롤·
페이지 안에서 JS 실행)는 **백업 경로(경로 B)용**이다 — 경로 A가 막혔거나 화면 전용
값·화면 총계가 필요할 때 쓴다. 특정 도구 이름에 의존하지 않는다(설계 원칙 1).
브라우저 도구가 없는 환경에서는 백업 경로가 없다는 것만 인지하고 경로 A로 진행한다.

### 설계 원칙

1. **플랫폼 중립** — SKILL.md는 어떤 에이전트든 따라할 수 있는 절차서로 쓴다.
   특정 도구 이름에 의존하는 지시를 SKILL.md에 두지 않는다.
2. **deterministic 후처리** — 수집 결과는 §6 데이터 계약대로 JSON에 담고,
   검증·집계·diff·HTML 생성은 `scripts/`가 한다.
   **데이터 계약이 수집(에이전트)과 처리(코드) 사이의 이식성 경계다.**
3. **단일 스킬 + 사이트 어댑터** — 스킬은 하나, 사이트별 상세는 `references/<site>.md`로
   분리한다(progressive disclosure). 신규 사이트는 references 추가로 확장한다.
4. **판단은 최대한 코드로 내린다** — 사람의 해석이 개입할수록 결과가 흔들린다.
   만족/불만족 판정(§4-B), 결측 판정, 변화 감지는 전부 코드가 한다.

### 수집 경로 — 경로 A(API) 우선, 경로 B(브라우저) 백업 (v16 재개정)

**v2~v5 "경로 A 우선 → B 폴백" → v6 "경로 B 우선" → v16에서 다시 경로 A 우선으로
되돌렸다** (2026-07-30 사용자 결정). 근거는 셋이다.

1. **경로 A가 빠르고 파싱이 안정적이다** — 목록 한 페이지가 요청 한 번이고, 필드가
   구조화돼 있어 화면 파싱 함정(가상화·카드 경계·축약 표기)이 없다
2. **화면 전용이던 값들이 경로 A로 열렸다** — 무신사 실시간 지표 원시값 API
   (어댑터 §4-1: 보는 중·조회수·누적판매·브랜드 하트), 29CM PLP API의 `totalCount`
3. **유지보수 모델을 바꿨다** — 확정 스킴이 바뀌면 어댑터를 개정한다(아래).
   스킴 변화에 대한 강건성은 브라우저가 아니라 **문서 갱신 절차**가 담당한다

**단 v6의 실측은 그대로 유효하다** — 경로 A에는 구조적 누락이 실측된 케이스가 있고
(아래), 완전성 검증(화면 총계 대조·순환 검증 금지)은 경로와 무관하게 필수다.

#### 스킴 변경 시 스킬 업데이트 (v16 신설)

- 어댑터(`references/<site>.md`)는 **검증된 스킴을 실측 일자와 함께** 기록한다.
  미검증 항목을 사실처럼 쓰지 않는 기존 원칙은 그대로다
- 런타임에 스킴 변화가 감지되면 — 확인된 엔드포인트의 400/404, 필드 소실,
  필수 필드 결측률 급증(§7의 30% 기준) — **경로 B로 폴백해 작업을 완주**하고
  `meta.notes`에 기록한 뒤, **스킴이 바뀐 사실을 사용자에게 보고한다**
- 어댑터 개정(새 스킴 실측·문서 반영)은 보고 후 지시에 따라 수행한다 —
  폴백은 임시 조치이고 **정착지는 갱신된 경로 A**다

#### 페이지별 수집 가능 여부 (2026-07-30 실측)

구 표는 전부 **서버 HTML 기준**이어서, 브라우저로 열면 렌더되는 페이지가 "껍데기만"으로
적혀 있었다. 두 열로 나눈다.

| 페이지 | 서버 HTML | **브라우저 렌더 후** | 실측 |
|---|---|---|---|
| 무신사 상품 상세 | 있음 | 있음 | |
| 무신사 카테고리 목록 | 있음 | **완전 렌더** | 60개/초, 3,606개/60초 |
| 무신사 브랜드 목록 | 껍데기만 | **완전 렌더** | `/brand/{slug}/products` 563개 = 화면 총계 |
| 무신사 랭킹 | 껍데기만 | **완전 렌더** | 1,019위까지, 결번 0 |
| 29CM 상품 상세 | 있음 (JSON-LD) | 있음 | |
| 29CM 카테고리 목록 | 껍데기만 | **완전 렌더** | 50개/페이지, 7,567개 = 화면 총계 |
| 29CM 브랜드 목록 | 껍데기만 | **완전 렌더** | 435개 = 화면 총계 |

**브라우저로는 일곱 화면 전부에서 목록을 얻는다.** "29CM는 목록을 못 준다",
"랭킹은 화면으로 안 잡힌다"는 서술은 폐기된다.

#### 두 경로의 역할 (v16)

- **경로 A (기본)** — 사이트가 화면을 그릴 때 쓰는 JSON API. 목록 순회·정확값
  (하트 배치·실시간 지표 원시값)·상세 값(JSON-LD 등)이 여기서 나온다.
  어댑터에 검증된 엔드포인트·파라미터가 실측 일자와 함께 적혀 있다.
- **경로 B (백업)** — 화면을 열어 **눈에 보이는 값만** 읽는다. 쓰는 경우는 셋이다:
  ① 경로 A가 막히거나 스킴이 바뀌었을 때의 폴백 ② 경로 A가 주지 않는 값 —
  **화면 총계**(29CM BEST API에는 총계가 없다), 화면 전용 노출값 ③ 경로 A의 전량성
  검증·보완(아래 구조적 누락). 추출은 접근성 스냅샷이 아니라 **페이지 안에서 JS를
  실행**해 한다 (수천 개 규모에서 스냅샷은 컨텍스트가 터진다).

#### 경로 A의 구조적 누락 (2026-07-30 실측, v6에서 확인)

**29CM BEST API(`brandFacetInputs.frontBrandNo`)는 브랜드 카탈로그 전량이 아니다.**

| | 건수 |
|---|---|
| 브랜드 페이지 화면 총계 = 브라우저 순회 | **435** |
| 경로 A(BEST API, 정렬 4종 합집합) | **422** |
| 누락 | **13건 (3.0%)** — 이 중 **11건이 품절** |
| 유령(수집엔 있고 화면엔 없음) | 0건 (수집분 ⊂ 화면집합) |

**누락의 원인은 특정하지 못했다.** 처음에 "랭킹이라 품절을 뺀다"고 적었으나
**같은 수집분 안에 품절이 이미 120건 들어 있어 성립하지 않는다.** 저조한 반응 때문도
아니다 — 누락 13건은 좋아요 4~39·후기 0~3인데, 경로 A 수집분에는 좋아요 5 이하가 35건,
후기 0건이 172건 들어 있다. 즉 **누락된 상품을 다른 상품과 구별해 주는 관측 가능한 기준이
없다.**

확립된 것은 두 가지다.

- **경로 A는 전량이 아니다** (435 중 422, 재현됨). 화면 집합의 부분집합이며 유령은 0건이다
- **누락이 품절 쪽으로 치우친다** — 누락분 품절률 **85%**(11/13) vs 수집분 품절률
  **28%**(120/422). 약 3배 편향이지만 필터가 아니라 편향일 뿐이다

**원인을 모르는 것이 규칙을 더 강하게 만든다.** 어떤 상품이 빠질지 예측할 수 없으므로
**화면 총계 대조 없이는 전량을 주장할 수 없다.** 브라우저 브랜드 페이지는 품절 상품을
`품절` 배지와 함께 포함하며 435건 전부를 준다.

**v16의 처리 — 경로 A로 모으되 전량 검증·보완에 경로 B를 쓴다.**

- **29CM 카테고리 전수(스토리2)**: PLP API(§2-2, 어댑터)가 `totalCount`를 주므로
  **경로 A 안에서 완전성 검증이 끝난다** — B가 필요 없다
- **29CM 브랜드 카탈로그(스토리1)**: BEST API 합집합으로 모은 뒤 **화면 총계(경로 B)와
  대조한다.** 어긋나면(실측 435 vs 422) **경로 B 순회로 보완**하고 그 사실을
  `meta.notes`에 남긴다. 대조 없이 "전량"을 주장하지 않는다
- **무신사**: 목록 API가 `totalCount`를 주고 `isSoldOut=true`도 받으므로 경로 A로 완결된다

#### 독립 총계 대조가 완전성 검증의 근거다 (경로 무관)

- `source_total`에는 **수집 절차와 독립인 총계**를 담는다 — 응답의 `totalCount`
  (무신사 목록 API·29CM PLP API) 또는 화면에 찍힌 총계 (`435개`, `7,567개`, `24,686개`)
- 총계를 주지 않는 API(29CM BEST)로 모으면서 `source_total`에 수집 건수를 넣으면
  `총계 대비 오차 0.0% — 기준 통과`가 **자기 자신과 비교하는 순환 검증**이 된다(실제 사고)
- **`source_total`에 수집 건수를 넣지 않는다.** 독립 총계를 못 얻었으면 `null`
- 랭킹은 총계 대신 순위 번호로 검증한다 — `max(rank) == 수집 건수 == distinct(rank)`이고
  결번이 있으면 불완전이다 (실측: 1,019위까지 결번 0·중복 0)

어느 경로를 썼는지 `meta.notes`에 기록한다. 경로가 바뀌어도 §6 계약은 같으므로
리포트 코드는 영향받지 않는다.

### 스킬 폴더 구조

```
commerce-research/
├── SKILL.md                  # 트리거, 공통 워크플로우, 스토리별 절차
├── references/
│   ├── musinsa.md            # URL 패턴, 순회 방법, 필드 매핑, 함정
│   ├── 29cm.md               # 〃
│   └── report-spec.md        # 리포트 섹션 구성 상세
├── scripts/
│   ├── validate_data.py      # 수집 JSON 스키마 검증·결측 리포트
│   ├── build_report.py       # 수집/diff JSON → HTML 리포트
│   ├── diff_snapshots.py     # 스토리3 스냅샷 기간 비교
│   └── group_variants.py     # 색상 변형 묶기 — 스토리2 속성 분류 횟수 절감
└── assets/
    └── report-template.html  # 리포트 구조 템플릿(코드 실행 불가 환경 폴백)
```

명명 규칙: kebab-case, 스킬 폴더 안에 README.md 금지, frontmatter에 `< >` 금지.

## 3. 범위 / 비범위

### 범위

- P0: 무신사에서 스토리 1·2·3 각각 자연어 요청 1개로 end-to-end 수행
- P0: 결과가 HTML 페이지로 저장됨 (이미지는 원본 URL 핫링크)
- P0 (v6): **스토리1 멀티 플랫폼 비교** — 한 브랜드를 무신사·29CM 합집합으로 정리하고
  입점 차이를 드러낸다. 플랫폼 1개만 입력하면 단일 라인시트로 동작한다
- P1 (v16 개정): 29CM 어댑터 — 카테고리는 PLP API(`totalCount` 제공), 브랜드는
  BEST API 합집합이 **기본 경로(경로 A)**다. **단 BEST API는 전량이 아님이 실측됐으므로
  (435 중 422, §2) 브랜드 카탈로그는 화면 총계와 대조하고 어긋나면 경로 B로 보완한다.**
  조회수·누적판매를 노출하지 않는 지표 비대칭은 남는다
- P1 (v14 신설): **자사몰 어댑터 — Cafe24 기반만** (`references/cafe24.md`).
  스토리1의 비교 플랫폼으로 들어온다. 사이트가 하나가 아니라 **브랜드마다 도메인이 다르므로**
  브랜드명에서 도메인을 찾아 확인하는 절차가 앞에 붙는다(§4 스토리1).
  **후기·평점·좋아요·조회수·누적판매는 수집하지 않는다** — robots.txt가 후기 게시판
  페이지 넘김을 막고, 스토어에 따라 화면에 개수 자체가 없다(v14 이력·어댑터 §0).
  지표 비대칭이 29CM보다 크다는 것을 리포트에 명시한다
- **플랫폼 간 동일 상품 매칭** (v6에서 비범위→범위) — 규칙은 §4 스토리1에 있다.
  **정규화 상품명 완전일치만** 허용하고 추정 매칭은 금지다
- 비로그인 상태에서 접근 가능한 데이터만 수집
- 수치는 **사이트 노출값만**. 추정·보간하지 않는다. 미노출은 `null`("미노출"로 표기)

### 비범위

- 로그인 전용 데이터, 구매·결제 등 상태를 바꾸는 모든 행위
- 에이블리/지그재그/W컨셉 (구조상 확장 가능하게만 설계)
- **Cafe24가 아닌 엔진의 자사몰** (v14) — 고도몰·임웹·Shopify·메이크샵 등. 엔진 판별은
  `references/cafe24.md` §1에 있고, Cafe24가 아니면 **수집하지 않고 비범위임을 알린다.**
  실제 요청이 오면 그때 실측해서 어댑터를 추가한다(자사몰 자체는 v14에서 범위로 들어왔다)
- **자사몰의 후기·평점·좋아요·조회수·누적판매** (v14) — robots.txt가 후기 게시판 순회를
  막고 외부 리뷰 위젯은 화면에 개수를 내지 않는다. 위젯 업체 API를 뜯는 방법도 쓰지 않는다
- 판매량 추정 모델 (리뷰 수 기반 역산 등 일체의 추정)
- **추정 매칭** (v6) — 상품명 유사도·가격 일치·이미지 대조로 동일 상품을 판정하는 일체의
  방법. 실측에서 오탐이 확인됐다(§4 스토리1). 완전일치가 안 되면 단독 입점으로 둔다
- 스킬에 내장된 스케줄러 (주기 실행 자체는 v3에서 스펙에 들어왔다 — §4 스토리3.
  단 실행 주체는 스킬 밖의 스케줄러이고, 데모 기준은 Claude Code 스케줄 기능이다)
- 이미지 다운로드·재배포 (속성 분류용 임시 다운로드는 예외)
- **리뷰 본문 수집·해석** (v3) — 만족/불만족 판단은 노출된 후기 수·평균 평점만 쓴다(§4 스토리2)

## 4. 유저 스토리별 스펙

### 리포트 공통 — 표 조작과 파생 지표 (v5, 2026-07-30 확정)

리포트 구조 자체는 `references/report-spec.md`가 정본이다. 여기서는 **판정·정의**만 둔다.

- **정렬 대상 = 순서에 뜻이 있는 열**: 상품명 · 가격 · 후기 · 평점 · 좋아요 · 조회수 ·
  누적판매 · 순위. 열 머리를 눌러 오름차순↔내림차순으로 뒤집고, 눌리는 열임을 알 수 있게
  **중립 화살표를 항상 표시한다.** 값이 없는 행은 방향과 무관하게 뒤로 간다
- **필터 축 = 값의 종류가 정해진 열**: 카테고리 · 속성(핏 등) · 변화 종류. 이 열들은
  **정렬하지 않는다** — 가나다순은 열람에 쓸모가 없고, 정렬 화살표가 필터를 가린다.
  대신 값마다 건수를 붙인 **다중 선택 칩**을 낸다. 한 축 안에서 여러 개를 켜면 OR,
  서로 다른 축끼리는 AND다
  - **예외 — `입점` 축은 한 축 안에서도 AND다** (v14). 칩은 **플랫폼 하나당 하나**이고,
    두 개를 켜면 "그 플랫폼들에 **모두** 있는 상품"만 남는다. 이 축을 쓰는 목적 자체가
    "무신사와 29CM 둘 다에 있는 상품"을 골라보는 것이라 OR로 두면 거의 전 행이 남아
    쓸모가 없다. 구 구조는 `양쪽 입점`·`무신사 단독`처럼 **조합을 미리 나열한 값 하나**여서
    플랫폼이 늘면 조합이 폭발하고 "정확히 2곳"을 고를 방법이 없었다.
    **동작이 다르므로 화면에 `모두 만족` 배지를 붙여 밝힌다** — 안 밝히면 OR로 읽는다
- **파생 지표는 계산식을 리포트 안에 싣는다** — 이름만으로 뜻이 통하지 않는 열은
  열 머리에 hover 뜻풀이를, 섹션 각주에 계산식과 예시를 넣는다. 정의를 모르는 숫자는
  읽는 사람이 지어내서 해석한다
  - **규모 대비 하트·후기** = 그 품목의 지표 비중 ÷ 상품 수 비중. 1.00이면 상품 수만큼
    반응한 것이고 1.30 이상 ▲, 0.70 이하 ▼. 상품 수가 1~2개인 품목은 크게 튀므로
    **상품 수 열과 함께 읽어야 한다**고 각주에 밝힌다
  - **전 상품 중위 대비** = 이 상품의 지표값 ÷ 그 지표가 노출된 전 상품의 **중위값**.
    평균이 아니라 중위값으로 나눈다 — 상위 몇 개가 평균을 끌어올려 기준이 흔들린다
  - **상품당 평균** = 합 ÷ **그 지표가 노출된 상품 수**. 미노출을 0으로 세지 않는다
- 전부 노출값의 합과 비율이다. 추정 금지 원칙(§3 비범위)은 파생 지표에도 그대로 적용된다

### 스토리 1 — 브랜드 라인시트 (멀티 플랫폼 비교)

**이 스토리의 핵심은 한 브랜드를 여러 플랫폼에서 비교하는 것이다** (v6 재정의).

- **트리거 예**: "무신사랑 29cm에서 인사일런스 라인시트로 정리해줘",
  "무신사에서 인사일런스 상품 전체 라인시트로 정리해줘"(플랫폼 1개),
  **"인사일런스를 무신사랑 자사몰에서 비교해줘"**(v14 — 자사몰)
- **입력**: 브랜드명(필수), 플랫폼 **1개 이상**(필수 — 기본값 없음, 요청에 명시된 것만).
  현재 지원은 무신사·29CM·**자사몰(Cafe24)**이고 **플랫폼은 확장 축이다** — 새 사이트는
  `references/` 추가로 들어오며 아래 처리·리포트 규칙은 사이트 수와 무관하게 성립해야 한다
- **처리**: 플랫폼별로 브랜드 상품 목록 전체 순회 → 사이트별 JSON(§6) → 검증 →
  **매칭·합집합** → HTML
- **규모 상한 (v6 — 구 2,000)**: **3,000개까지 묻지 않고 진행** (초과 시 규모를 알리고 좁힐 축 협의 —
  §4 스토리2와 같은 절차). 근거: 스토리1의 지배 비용은 목록이 아니라 **상품당 상세 방문
  1회**다 — 조회수 구간·누적판매뿐 아니라 **카테고리(`baseCategoryFullPath`)도 상세에만
  있다**(2026-07-30 실측: 목록만 쓰면 `category` 결측 100%로 검증이 FAIL한다).
  예의 대기 포함 상품당 ~1.2초 → **3,000개 ≈ 60분**이 단일 요청 완주 UX의 한계선이다.
  실측: 로우클래식 440개(품절 포함) 상세 순회 약 4분, 인사일런스 무신사 2,022개(품절 포함).
  **멀티 플랫폼일 때 상한은 플랫폼별로 적용한다** (합집합 크기가 아니라 각 사이트 수집량).
  **자사몰도 상품당 상세 1회가 필요하다**(v14) — 품절과 판매가를 상세 JSON-LD에서
  확정하므로 비용이 같은 수준이고 상한 근거가 그대로 적용된다

#### 자사몰(Cafe24)일 때 앞에 붙는 절차 (v14)

무신사·29CM는 사이트가 하나씩이지만 **자사몰은 브랜드마다 도메인이 다르다.**
수집을 시작하기 전에 대상을 확정하는 단계가 하나 더 있다. 상세는 `references/cafe24.md` §1.

1. **도메인을 찾는다** — 사용자가 URL을 줬으면 그대로 쓰고, 브랜드명만 말했으면 검색한다.
   **도메인을 지어내지 않는다**
2. **브랜드가 맞는지 확인한다** — 타이틀·로고·푸터 상호를 대조한다. 후보가 여럿이거나
   애매하면 **제시하고 사용자가 고른다**(E-IN-2와 같은 처리)
3. **Cafe24인지 판별한다** — 아니면 **수집하지 않고 비범위임을 알린다**(§3)
4. `meta.site`에 **도메인 문자열**을 담는다(`insilence.co.kr`). 어떻게 찾았는지를
   `meta.notes`에 남긴다

**전량 확보 방식이 다르다** — 브랜드 페이지 하나가 아니라 **네비게이션의 모든 상품
카테고리를 순회하고 상품번호로 중복을 제거한다.** `ALL` 카테고리 하나로는 부족하다
(인사일런스 실측: `ALL` 합집합 428, 전체 합집합 555, 누락 127건/23%).
**`source_total`은 `null`이고**(화면 총계 없음) 완전성 근거는
**"모든 상품 카테고리 소진 + 각 카테고리 `btnNext=#none` 도달"**이다 — ±5% 판정을
적용하지 않는다(29CM 브랜드 목록과 같은 처리, 아래 성공 기준 참조).

#### 동일 상품 매칭 — 완전일치만 (v6 확정)

두 플랫폼의 상품을 이어붙이려면 "같은 상품"을 판정해야 한다. **규칙은 하나뿐이다.**

1. 상품명을 정규화한다 — 소문자화 · `[...]`·`(...)` 제거 · 영숫자와 한글만 남기고 나머지
   (공백·`_`·`/`·하이픈 등) 제거
2. 정규화 결과가 **완전히 같을 때만** 동일 상품이다
3. 같지 않으면 **단독 입점**으로 둔다. 이것은 매칭 실패가 아니라 유통 사실이다

**추정 매칭은 금지다** (§3 비범위). 유사도 매칭·가격 일치 보조·이미지 대조를 쓰지 않는다.
근거는 인사일런스 실측(2026-07-30)이다 — 유사도 0.82↑ 후보 11건 중 대부분이 다른 상품:

| 29CM | 무신사 | 실제 |
|---|---|---|
| 헨리넥 숏슬리브 BLACK (69,000) | 와플 헨리넥 롱슬리브 BLACK (77,000) | 소매 길이가 다른 별개 |
| 프렌치 워크 자켓 FADED BLUE (177,000) | 프렌치 워크 자켓 FADED BLACK (177,000) | 컬러웨이가 다른 별개 |
| 레터링 그래픽 티셔츠 CHARCOAL (66,000) | 스탠실 그래픽 티셔츠 CHARCOAL (66,000) | 프린트가 다른 별개 — **정가까지 같다** |

마지막 행이 결정적이다. **가격 일치가 오탐을 확증해주기 때문에 가격은 보조 신호로도
쓸 수 없다.** 완전일치의 실측 커버리지는 29CM 421개 중 338개(80.3%)이고 오탐 0이다.

매칭 결과는 리포트가 계산하며 근거를 남긴다 — 각 행에 어느 플랫폼에서 왔는지와
정규화 키를 보존해 사후 확인이 가능해야 한다.

#### 품목 비교축 — 매칭 상품에서 자동 추출 (v6 확정)

사이트 카테고리 이름은 서로 다르다. 인사일런스 실측에서 **29CM 43종 / 무신사 37종 중
이름이 그대로 겹치는 건 8종뿐**이다(반소매 티셔츠, 긴소매 티셔츠, 카디건, 코튼 팬츠,
베스트, 플리스, 피케/카라 티셔츠, 후드 집업). 무신사 `청/데님 팬츠` ↔ 29CM `데님 팬츠`,
무신사 `항공 점퍼/블루종/MA-1` ↔ 29CM `블루종`처럼 같은 품목이 다른 이름이라
원본 이름으로 나란히 놓으면 축이 어긋난다.

**사람이 매핑 표를 관리하지 않는다.** 매칭된 동일 상품이 각 플랫폼에서 어느 카테고리에
들어가 있는지를 세어 **코드가 대응 관계를 만든다**:

1. 매칭된 상품마다 `(무신사 카테고리, 29CM 카테고리)` 쌍을 센다
2. 한 카테고리의 대응 후보 중 **건수가 가장 많은 것**을 대응으로 정하고, 근거 건수를 남긴다
3. 대응이 생기지 않은 카테고리(단독 입점 품목)는 **통합하지 않고 그대로 둔다** —
   억지로 붙이지 않는다
4. 리포트는 통합축과 **각 사이트 원본 카테고리를 모두 싣는다.** 통합축은 근거 건수와 함께
   표시해 독자가 검증할 수 있어야 한다 (근거 1~2건인 대응은 약한 대응임을 표시)

#### 출력 리포트

① 헤더(브랜드, 수집 시각, **플랫폼 목록**, 플랫폼별 상품 수와 합집합 크기, 매칭 건수,
결측·노출 요약)
② **플랫폼 비교 요약** — 아래 고도화 4개 축
③ 플랫폼별 인기 상품
④ **합집합 전 상품 표** — 칼럼: 이미지 · 상품명 · **입점**(어느 플랫폼에 있는지) ·
품목(통합축) · 플랫폼별 정가/판매가/할인율 · 후기 수 · 평점 · 좋아요 · 조회수 ·
누적판매 · 링크. 매칭된 상품은 **1행**이고 플랫폼별 값이 나란히 들어간다

`입점` 칼럼은 필터 축이다(§4 리포트 공통) — "양쪽 입점 / 무신사 단독 / 29CM 단독"으로
걸러 볼 수 있어야 한다.

#### 플랫폼별 품목 인기 분석 — 고도화 4개 축 (v6 확정)

"플랫폼별로 어떤 품목이 인기인가"에 대한 답이다. 전부 `build_report.py`가 계산하고
**노출값의 합과 비율일 뿐 추정이 아니다.**

1. **품목별 입점 차이** (구 `품목별 입점 커버리지 갭` + `단독 입점 상품 성격`) — 품목마다
   **플랫폼별 `입점` 열과 플랫폼별 `단독` 열**을 낸다 (v14 개정 — 구 구조는
   `양쪽 입점`(=모든 플랫폼) + 플랫폼별 `단독`이었다).
   **구 구조는 플랫폼이 3개 이상이면 상품을 잃었다** — "정확히 2곳에만 입점"이 어느 열에도
   들어가지 않아 실측에서 합집합 28인데 열 합계가 26이었다. 게다가 헤더의 `양쪽 입점`은
   `2곳 이상`을 세고 있어 **같은 이름이 다른 값**을 말했다.
   - `입점` 열은 그 플랫폼에 있으면 센다 — 여러 곳에 있는 상품은 여러 열에 잡히므로
     **열 합계가 합집합보다 클 수 있다**(정상). 열 수는 플랫폼 수에 선형으로만 는다
   - **검산: `단독` 열들의 합 + `2곳 이상 입점` = 합집합.** 코드가 이 등식을 지킨다
   - `겹침 비율`(구 `양쪽 비율`) = 2곳 이상 입점 ÷ 그 품목 합집합
   - KPI는 플랫폼이 2개면 `양쪽 입점`, 3개 이상이면 `2곳 이상 입점`과 `전 플랫폼 입점`을
     **따로** 낸다 — 둘은 다른 값이다
   브랜드가 어느 품목을 어느 플랫폼에 몰아줬는지가 보인다. 실측(인사일런스, 2026-07-30):
   `니트/스웨터`는 양쪽 입점이 **26%**(42건 중 11건)로 무신사에 쏠려 있고,
   `반소매 티셔츠`는 **71%**(77건 중 55건)로 양쪽에 고르다 — 품목별로 편차가 크다.
   무신사 단독 225건에 `[2 PACK]`·`[SET]`·`[KKST 콜라보]` 같은 번들·콜라보 표기가 있지만
   **15건뿐이다** — 단독 입점의 주된 사유는 특정 상품 유형이 아니라 **카탈로그 폭 차이**
   (무신사 564 vs 29CM 422)로 보이며, 유형 쏠림을 단정하지 않는다
2. **같은 상품 반응 강도 비교** — **매칭된 상품만** 대상으로 플랫폼별 하트·후기를 품목
   단위로 대조한다. 동일 상품이므로 **상품 구성 차이가 통제된 비교**가 되고, 두 사이트가
   모두 노출하는 지표(하트·후기)만 쓰므로 추정이 없다. 매칭 건수를 함께 싣고,
   **단독 입점은 이 비교에서 제외**한다(제외 사실을 각주로 밝힌다)
   - **자사몰은 이 축에서 빠진다** (v14). 하트·후기를 수집하지 않으므로 비교할 값이 없다.
     **빠진 사실을 각주로 밝힌다** — 빈 열이나 0으로 그리지 않는다. 비교 대상 플랫폼이
     자사몰뿐이면(무신사·29CM 없이 자사몰 1개) 이 축 자체를 만들지 않는다
3. **가격·할인 포지셔닝 차이** — **집계만 내는 섹션이다** (v10 개정, 2026-07-30).
   품목별 평균 판매가 차이와 "판매가 같은 것 / 다른 것" 요약까지다.
   **상품별 대조 표는 전 상품 표의 `가격 차이` 열로 합쳤다** — 구 구조는 매칭 상품 전 건을
   여기서 또 실어서 **같은 상품이 리포트에 두 번** 나왔다(로우클래식 실측: 합집합 741행인데
   매칭 283건이 별도 표로 중복).
   **판매가는 두 사이트 모두 쿠폰적용가다**(v8 — 무신사 `finalPrice`, 29CM `displayPrice`).
   정가가 같은데 판매가만 다르면 그것이 플랫폼별 할인 정책 차이다
4. (구 **단독 입점 상품 성격 분석** — v11에서 ①에 합쳤다. 품목 분포 막대가 ① 표의
   `단독` 열과 같은 값이었고, 고유 정보인 단독 상품의 **가격대**만 KPI·분포로 남겼다)

v5의 품목 성적표(상품 수 · 하트 합 · 상품당 하트 · 규모 대비 하트 · 후기 합 · 상품당 후기 ·
규모 대비 후기 · 중위 가격 · 평균 할인율 · 후기가중 평점)는 **사이트별로 그대로 유지한다.**
고도화 4개 축은 그 위에 얹히는 비교 레이어다. 지표 정의는 §4 리포트 공통.

**플랫폼이 1개면** 고도화 4개 축은 만들지 않는다(비교 대상이 없다). 빈 섹션을 만들지 말고
기존 단일 라인시트 구성으로 낸다.
- **`플랫폼별 인기 상품`(상위 10) 섹션은 만들지 않는다 (v7, 2026-07-30 사용자 결정)** —
  전 상품 표를 좋아요로 정렬하면 같은 답이 나온다. 전 상품 표에 좋아요 열이 있고
  (플랫폼이 2개 이상이면 사이트별로) 정렬 가능하다. 구간 표기 값(§6)은 여전히
  정렬 키를 갖지 않으므로 순위 비교에 끼지 않는다
- **성공 기준**: 상품 수가 사이트 노출 총계의 ±5% 이내, 필수 필드 결측 <5%.
  **멀티 플랫폼일 때는 플랫폼별로 각각 판정한다** — 한쪽이 미달이면 그 사이트만 경고를
  달고, 합집합 리포트는 만든다(부분 실패를 전체 실패로 처리하지 않는다).
  추가: 매칭 건수와 단독 입점 건수의 합이 합집합 크기와 일치해야 한다(코드가 검산)
  - **29CM 브랜드 목록은 `source_total`이 응답에 없다** — `pagination`이 `hasNext`만 준다.
    끝까지 순회해 센 값을 `item_count`에 담고 `source_total`은 그 값으로 두되,
    **사이트가 총계를 주지 않았다는 사실을 `meta.notes`에 남긴다.** ±5% 기준은 이 경우
    자기 자신과의 비교가 되어 무의미하므로, 완전성 근거는 **`hasNext=false` 도달**이다
  - **자사몰도 `source_total`이 없다** (v14) — 화면 어디에도 총계가 없다.
    **`source_total`은 `null`로 두고 ±5% 판정을 건너뛴다.** 완전성 근거는
    **"네비게이션의 모든 상품 카테고리 소진 + 각 카테고리 `btnNext=#none` 도달"**이다.
    **어느 카테고리에도 걸리지 않은 상품은 구조적으로 놓치며 검출할 방법이 없다** —
    이 한계를 `meta.notes`에 남긴다
  - **자사몰은 필수 필드 결측 <5% 판정에서 반응 지표를 뺀다** (v14) — 후기·평점·좋아요·
    조회수·누적판매는 수집 대상이 아니므로 결측이 아니라 **구조적 미노출**이다.
    §6의 "노출 시 필수"가 그대로 적용된다(노출되지 않았으므로 `null`이 정상)

### 스토리 2 — 상품군 시장 전수조사

- **트리거 예**: "무신사 데님팬츠 시장 전수조사 해줘"
- **입력**: 카테고리명(필수), 사이트(**필수 — 기본값 없음**), 성별(선택)

#### 리뷰 본문은 수집하지 않는다 (v3 — 사용자 결정)

만족/불만족 판단 재료는 사이트가 각 상품에 노출한 **후기 수와 평균 평점**뿐이다.
두 사이트 모두 이 값이 목록/상세에서 바로 보이므로 별도 수집 비용이 없다.
리뷰를 한 건씩 읽고 해석·요약하는 일은 **비범위**다.

리포트가 계산하는 것(전부 deterministic — `build_report.py`):
- 후기 수 **가중** 평균 평점 (후기 많은 상품이 더 무겁게)
- 상품 평점 분포 (~2.9 / 3.0~3.4 / 3.5~3.9 / 4.0~4.4 / 4.5~, 평점 미노출은 제외하고 건수 명시)
- 실수로 `reviews[]`가 담겨 오면 `validate_data.py`가 경고한다

#### 범위 정책 (v9 — 임계값 5,000)

조사 결과 무신사 데님팬츠(`003002`)는 **24,673개 / 412페이지**다.
목록 수집 자체는 1만 개도 감당되지만(60개/페이지 × 167회 ≈ 4~7분), **목록은 지배 비용이
아니다.** 스토리2의 실제 비용은 ① 상품당 상세 방문 1회(조회수 구간·누적판매) ~1.2초
② 핏 분류 중 **이미지 확인이 필요한 ~26%**(상품명 텍스트 분류 74% — 175개 실측) 장당
~4초로, 합쳐 상품당 ~2.3초다. 3,000개 ≈ 2시간이 단일 실행의 현실적 한계이고,
1만 개는 ~7시간이라 성립하지 않는다 (v3의 1만 기준은 목록 비용만 본 오류였다).

절차 — SKILL.md에 알고리즘으로 명시돼 있다:

1. 목록 첫 페이지에서 **사이트가 말하는 총 개수**를 읽는다 (`source_total`)
2. **0건** → 중단, "검색 0건" 보고
3. **5,000건 이하** → 묻지 않고 진행. 시작 시 개수·예상 소요 한 줄 보고
4. **5,000건 초과** → 수집을 시작하지 않는다. 좁힐 축의 **실제 개수를 조회해서**
   제시하고 사용자가 고른다. "전체로"라고 하면 전체 진행(예상 소요 먼저 보고)
5. 최종 범위를 `meta.target`에 쓴다 — "데님팬츠(남성)" 형식. 리포트 헤더에 그대로 실린다

**임의 샘플링 금지** — 묻지 않고 상위 N개만 수집하는 것이 최악의 실패다.

좁힐 수 있는 축(우선순위 순):

| 축 | 무신사 | 29CM |
|---|---|---|
| **성별** | `gf` 파라미터 (`A`/`M`/`F`) | 대분류 코드에 이미 포함 (여성의류 `268100100` / 남성의류 `272100100`) |
| 세부 카테고리 | 6자리 소분류 | 9자리 소분류 (예: 남성 데님 팬츠 `272104104`) |
| **가격대** | `minPrice`/`maxPrice` **확인** (10~20만원 5,464 실측) | 미검증 |
| 브랜드 | PLP 필터 파라미터 미검증 | 미검증 |

성별 필터의 실제 축소 효과는 §4 부록에 기록한다.

#### 처리

1. 카테고리 특정 → 규모 확인 → 범위 결정 (위 절차)
2. 상품 목록 전체 수집 — 후기 수·평점·좋아요는 목록 노출값 그대로
3. **속성 분류** — 근거 순서: ① 상품명·옵션·상세 텍스트 → ② **색상 변형 그룹 전파** →
   ③ 대표 이미지 확인 → ④ `unknown`. 찍지 않는다.
   판단 근거를 `attributes_basis`에 `name`/`detail`/`image`/`group`/`unknown`으로 남긴다
   - **② 그룹 전파 (v12 신설)**: 같은 상품의 색상 전개가 각각 별개 상품으로 올라온다.
     색상은 핏을 바꾸지 않으므로 **그룹당 한 번만 판단**한다. `group_variants.py`가
     `--plan`으로 그룹·대표를 뽑고 `--apply`로 구성원에 전파한다
   - 묶는 규칙은 **같은 브랜드 + 색상·품번·괄호를 뗀 이름 완전일치**뿐이다.
     **유사도로 묶지 않는다** — 이 프로젝트는 상품명 유사도 매칭을 실측으로 기각했다
   - 실측 절감: 슬랙스(여성 10~20만원) 3,004개에서 미분류 1,492건 →
     **판단 1,071회(28% 감소)**. 검증: 서로 다른 핏이 섞인 그룹 **0개**
   - 한 그룹에 값이 섞이면(`conflict`) 전파하지 않고 직접 정한다
4. 검증 → 리포트

#### 조사 칼럼 구성 (2026-07-29 확정)

- **표 칼럼**: **이미지** · 상품명(링크) · 브랜드 · 카테고리 · 정가/판매가/할인율 ·
  후기 수 · 평점 · 조회수(구간 문구) · 누적판매 · 좋아요 · 품절 + 분류 속성
  - **이미지 열을 넣는다 (v5, 2026-07-30 사용자 결정)** — v3의 "전수조사 표는 이미지 열
    제외"를 뒤집었다. 전 행 유지 원칙은 그대로이고, 썸네일은 `loading="lazy"`라 화면에
    들어올 때만 받는다(3,460행 실측 4.1MB, 단일 파일 유지)
- **분류 속성은 핏만 P0** — 부츠컷/와이드/스트레이트/슬림/테이퍼드/**쇼츠**/unknown.
  상품명 텍스트로 74%가 해결되고(175개 실측) 나머지 ~26%만 이미지 확인이라
  상한 5,000 규모에서 감당된다
- **`쇼츠`를 값으로 추가 (v5, 2026-07-30 사용자 결정 — B안)**: 앞의 다섯 값은 긴바지의
  레그 실루엣 축이라 반바지에는 정답이 없다. 그대로 두면 `unknown`에 뭉친다
  (실측: 쇼츠 107개 중 96개가 unknown, 11개는 `와이드`가 붙어 같은 종류가 갈렸다).
  **축을 쪼개지 않고 한 값을 더하기로 했다** — 대신 "와이드 쇼츠"는 표현되지 않는다
  - 이름 신호(근거 `name`): `쇼츠` `숏츠` `숏팬츠` `반바지` `버뮤다` `BURMUDA`(오타가
    실제로 쓰인다) `shorts` `하프` `N부`(2~5부)
  - 이름에 단서가 없어도 **대표 이미지에 무릎 위 기장이 보이면 `쇼츠`**(근거 `image`)
  - **함정: `하프 밴딩`·`Half-band`는 기장이 아니라 허리 밴딩이다** — 긴바지로 둔다
  - **함정: `shorts` 뒤에 밑줄이 오는 이름이 많다**(`A-line shorts_light blue`).
    밑줄은 단어 문자라서 단어 경계로만 찾으면 놓친다
  - 기장을 볼 수 없는 컷(허리 클로즈업)과 바지가 아닌 상품은 `unknown`으로 둔다
- **속성은 정렬 열이 아니라 다중 선택 필터 축이다** (§4 리포트 공통)
- 워싱 톤·기장·소재 등 추가 속성은 **사용자가 명시적으로 요청할 때만** 수행한다.
  이미지 전수 확인이 필요한 속성은 예상 소요를 먼저 알린다

- **출력 리포트**: ① 시장 요약(상품 수, 후기 합계, 중위 가격, 가중 평균 평점, 가격 분포,
  상품 평점 분포, 속성 분포) ② 전 상품 표(**이미지 열 포함**·전 행, 속성 컬럼 + 속성 칩 필터)
- **성공 기준**: 합의된 범위 완주(중단 시 부분 리포트에 명시), 속성 분류율 ≥80%,
  후기 수·평점 결측이 실제 미노출과 일치(추정·보간 0건)

### 스토리 3 — 카테고리 랭킹 모니터링

#### 동작 2개로 분리 (2026-07-29 확정)

| 동작 | 트리거 예 | 입력 | 하는 일 |
|---|---|---|---|
| **C-1 모니터링 시작** | "무신사 바지 랭킹 모니터링 시작해줘" | 카테고리·사이트(필수) | 스냅샷 1개 수집 → **스케줄 등록**(사이트별 주기) → 등록 내용 보고 |
| **C-2 변화 조회** | "무신사 바지 랭킹, 지난 일주일 변화 보여줘" | 카테고리·사이트·**기간**(필수) | 축적분에서 diff → 변화 리포트 |

- **스케줄링이 스펙 안에 들어왔다**: C-1은 실행 환경의 스케줄 기능(데모 기준
  Claude Code)에 주기 실행을 등록하는 것까지가 동작이다. 등록 전에 첫 스냅샷이
  정상 수집되는 것을 확인한다. 스킬이 자체 타이머를 돌리는 것은 여전히 비범위다
- **축적 주기 = 사이트의 원본 갱신 주기** (2026-07-29 확정): 무신사 실시간 **30분**,
  29CM `HOURLY` **1시간**. 원본 주기보다 촘촘히 찍으면 같은 랭킹을 중복 저장할 뿐이다
- **기간 없는 변화 요청**("변화 알려줘"): 변화 분석을 하지 않는다. 스냅샷 1개만 찍고
  C-1(모니터링 시작)을 제안한다
- **처리(C-2)**: 기간 내 스냅샷 diff → 변화 리포트. 스냅샷 1개 이하면 "비교 대상 부족"
- **수집 규모 (조사 확인)**: 무신사 랭킹 API는 한 번에 102개를 준다.
  **top 100은 1페이지로 끝난다** — 순회 부담이 없다.
  29CM BEST API도 100개/페이지라 top 100은 요청 1번이다
- **랭킹 코드 체계가 경로별로 다르다 (v6 실측, 2026-07-30 / v16: 경로 A가 기본)**:
  - **경로 A(기본)는 대분류 3자리를 받는다** — 바지 전체 = `003`, 정상 200.
    기존 축적 스냅샷도 이 코드로 쌓였다
  - **경로 B(백업)로 내려가면 화면은 6자리만 받는다. 바지 전체 = `003000`이다.**
    대분류 `003`을 주면 에러도 리다이렉트도 없이 **다른 랭킹**
    (`sectionId=200&categoryCode=000` = NEW·전체)이 온다. 상태코드로는 알 수 없고
    1위 상품을 봐야 안다(재현 2/2). 폴백 시 반드시 코드를 바꾼다
  - **화면 랭킹은 1,019위까지 나온다** (실측: 순위 1~1,019, 결번 0·중복 0, 37스텝/25초).
    **그래도 top 100 고정을 유지한다** (2026-07-30 사용자 확정) — 기존 축적 스냅샷
    (경로 A `003`, 102건)과 모양이 같아야 diff 시계열이 이어진다. 100위 아래는 버린다
  - **완전성 검증은 순위 번호로 한다** — `max(rank) == 수집 건수 == distinct(rank)`.
    결번이나 중복이 있으면 불완전으로 처리한다. 화면 총계가 없어도 성립하는 검증이다
  - **광고 카드를 순위에서 제외한다** — 순위 번호가 붙지 않은 카드는 랭킹 항목이 아니다
- **29CM 랭킹 기준 (v3 확정, 2026-07-29 검증)**: 소분류 랭킹은 중분류를 필터한 것이
  아니라 **독립 랭킹**이다(데님 top 100의 84%가 바지 top 100에 없음을 대조로 확인).
  기본 기준은 **실시간(HOURLY) + 인기순(POPULARITY) + 세그먼트 전체**.
  **폴백**: BEST API 불가 시 중분류('바지')로 낮춰 수집하되 리포트에 명시하고,
  `target`을 중분류 이름으로 저장해 **기존 소분류 스냅샷과는 비교하지 않는다**
  (다른 랭킹이라 diff가 성립하지 않는다)
- **실시간 지표 (v2 신규)**: 무신사 랭킹 목록에는 `"1.2천명이 보는 중"`,
  `"48명이 구매 중"`이 붙는다(바지 랭킹 102건 중 98건에 존재).
  이것이 유저 스토리가 요구한 "몇 명이 보고 있음, 구매함"이다.
  `viewers_now`/`buyers_now`에 담는다. **랭킹 목록에만 있고 상품 상세에는 없다.**
  사이트가 반올림해 보여준 값이므로 정밀한 수치로 다루지 않는다
- **출력 리포트 (v7 재설계, 2026-07-30 사용자 결정)** — 섹션 8개 → 4개.
  구 구조는 표 4개(신규 진입·이탈·순위 변동·가격 변화)의 열 구성이 거의 같았고
  그러면서도 **등장 상품 679개 중 158개(23%)만** 실렸다(33개 축적분 실측).
  ① 헤더 ② KPI ③ 기간 개요 ④ **상품별 추이**(주 표) ⑤ **추이 차트**.
  구조 상세는 `references/report-spec.md`가 정본이다
  - **KPI** — 스냅샷 수 / **등장 상품 수** / **교체율** / 가격·할인 변화(시점 확정 수).
    신규 진입·이탈은 첫·마지막 두 시점만 비교한 값이라 기간이 길어지면 실제 교체를
    과소 표기한다 → **교체율 = 인접 스냅샷 평균 신규 유입률**로 대체
  - **상품별 추이 (한 행 = 한 상품)** — 기간에 순위권에 오른 상품 **전부**를 담는다.
    신규 진입·이탈은 `끝점` 축(`유지`/`신규 진입`/`이탈`/`기간 중만`)으로 흡수하고
    표를 따로 두지 않는다. 급상승·급하락 막대도 없앤다(표 정렬로 같은 답)
  - **`전 구간`/`일부 구간` 라벨 폐기** → **`랭킹 체류 28/33회 · 85%`** 숫자 + `끝점` 배지.
    구 라벨은 "첫·마지막 스냅샷 둘 다에 있었나"라는 구현 사정을 말하는데 읽는 사람은
    "관측이 온전한가"로 읽었다(2026-07-30 사용자 지적)
  - **처음·마지막 두 점이 아니라 추이를 싣는다** — 행마다 순위·가격 스파크라인.
    순위 축은 **전 행 공통 범위**(모양 비교 가능), 가격은 **상품별 범위**(높이 비교 금지),
    할인 구간은 가격 스파크라인 뒤 배경 띠
  - **1회만 스친 상품은 기본 필터에서 뺀다**(추이가 점 하나다). 건수는 칩에 표시해
    **조용히 숨기지 않는다**(33스냅샷 실측: 679개 중 348개가 1회)
  - **추이 차트** — **표의 현재 정렬·필터 순서대로 상위 N개**를 그린다. 차트가 따로 선택
    규칙을 갖지 않으므로 "왜 그 5개인지"를 설명할 필요가 없다
    - 계열 수 **1~100**을 사용자가 입력한다
    - **5개까지 겹쳐 보기**(계열 색 5개 — 상품끼리 비교), **6개 이상 나란히 보기**
      (작은 배수 격자). 순위선 100개를 겹치면 읽을 수 없다 — 색을 늘리지 않고
      **형태를 바꾼다**. 구 규칙("계열 5개까지")을 깨는 게 아니라 5를 넘을 때의 형태를 정한 것
    - 각 칸 = **순위(좌축) · 가격(우축) · 할인 구간(배경 띠)** 한 좌표계.
      **시간·가격·할인율·순위를 한 번에** 보려는 요청의 답이다
    - 가격이 안 변한 상품은 가격 선을 그리지 않고 우축 라벨만 둔다 —
      가운데 점선이 순위 그리드선과 혼동된다
    - 표 행을 누르면 그 상품의 큰 패널이 펼쳐진다
  - **계열 데이터는 문서에 한 번만 심는다**(`<script type="application/json">`) —
    스파크라인·차트·행 펼침이 같은 데이터 하나를 쓴다. 모든 시점을 `null`까지 채우면
    2만 칸이 되므로 **관측된 시점만** 담는다(실측 3,300건 ≈ 190KB).
    그리기는 인라인 JS가 한다 — **외부 리소스는 여전히 0**이다.
    코드 실행이 안 되는 폴백 환경에서는 스파크라인·차트 없이 표만 만든다
  - **모든 상품 표에 이미지 열 포함** — 이를 위해 `diff_snapshots.py`의 `trends`가
    `image_url`을 들고 있어야 한다(구 버전은 movers/entered/exited에만 있어서
    순위권을 드나든 상품이 빈칸이 됐다)
- **성공 기준**: 스냅샷 2개 이상이면 diff 리포트 생성. 1개 이하면 "비교 대상 부족"을
  명시하고 현재 스냅샷 리포트만 (빈 diff를 만들지 않는다)
- **리포트 판정 기준 (2026-07-29 확정 — 전부 코드가 계산)**:
  - 급상승/급하락 강조 = **랭킹 길이의 10%, 최소 3계단** (top100이면 10계단).
    강조에만 쓰이고 진입/이탈·전체 변동 표는 임계값과 무관하게 전부 실린다
  - 추이 요약 = 시점이 48개를 넘으면 **균등 구간의 대표 스냅샷만** 골라 그린다
    (첫·끝 포함, 평균 금지 — 실측 스냅샷을 고른다. 48 = 무신사 하루치).
    **가격·할인 변화 감지는 요약과 무관하게 원본 전체 해상도**로 한다
  - **가격·할인 변화 감지 = 상품별 마지막 관측 상태 대비 (v5 개정, 2026-07-30 사용자 결정)**.
    구 규칙은 "연속한 스냅샷 쌍에 둘 다 있는 상품"만 비교했다. 개정 근거(바지 33개
    축적분 실측):
    - 등장 상품 **679개 중 전 시점 상주는 7개(1%)** — top 100이 30분마다 크게 교체된다
    - 연속 관측 쌍 **2,621개 중 구 규칙이 비교하는 것은 1,868개(71%)**.
      나머지 **753개(29%)는 비교 자체가 안 됐다**
    - 결과: 축적분에 남아 있는 실제 변화 2건을 **0건으로 보고**했다. 값이 없다고 표시한
      게 아니라 없다고 단정한 것이라, "조용히 틀린다"에 해당한다
    - 두 끝값 모두 사이트가 노출한 값이라 **추정이 아니다.** 잃는 것은 "언제"의 해상도뿐
  - **관측 창을 사건마다 기록한다**: `from_at` · `to_at` · `gap_snapshots` ·
    `exact_at`(결석 0 = 스냅샷 1칸으로 시점 확정). 리포트는 확정된 것만 단일 시점으로
    찍고 나머지는 `A ~ B 사이 (N시간 · 스냅샷 M개 결석)`로 낸다.
    **상한(cutoff)을 두지 않는다** — 긴 창의 사건도 사실이고, 잘라내면 다시 조용한 누락이 된다
    (실측 결석 구간: 중위 2스냅샷(60분), 72%가 90분 이내, 최대 23스냅샷(690분))
  - **밝혀야 하는 한계 2개**: 결석 구간 안에서 여러 번 변했으면 **순변화 1건으로 뭉쳐지고**,
    변했다 원래대로 돌아왔으면 **아예 잡히지 않는다**(순위권 밖은 관측 자체가 없다)
  - **할인 시점 = 감지이지 예측이 아니다**: 가격·할인율 필드가 바뀐 것을 기록한다.
    구매량 추이로 할인을 추론하지 않는다(추정 금지)
  - **순위 추이 선은 순위권 밖 구간에서 끊는다** — 이어 그으면 그 구간에도 순위가
    있었다고 주장하는 것이다. 시점 축 라벨은 날짜가 바뀔 때만 날짜를 붙이고 솎아낸다
  - **실시간 지표 추이 차트 포함**: 변동 큰 상품 5개의 `viewers_now`/`buyers_now`
    추이를 그린다. 반올림 노출값임을 각주로 명시
- **과거 소급 불가 — 확정 (2026-07-29 조사 완료)**: 서버 측 검증 결과
  **과거 데이터의 출처는 축적분뿐이다.**
  - 무신사 현행 랭킹 API: 날짜 파라미터가 전부 무시됨을 응답 대조로 확인.
    `period`는 트레일링 윈도우라 기준일 지정 불가
  - 29CM BEST API: 날짜형 필드 후보 17개가 **DTO에 존재하지 않음**을 타입 프로브로 확인
  - Wayback 아카이브: 두 사이트 랭킹 페이지 모두 CSR 껍데기라 실질 0건
  - 무신사 월간 랭킹 아카이브 API(TOP30·월 단위)가 존재하지만 **쓰지 않는다**
    (2026-07-29 사용자 결정) — 변화 분석의 재료는 축적 스냅샷으로 일원화한다
  - 부수 확인: 무신사 REALTIME 랭킹의 원본 갱신 주기가 **약 30분**이다 —
    30분 축적 주기가 원본 갱신 주기와 정확히 맞는다(더 자주 찍어도 중복일 뿐)

### §4 부록 — 성별 필터 축소 효과 (2026-07-29 실측 완료)

| 대상 | 전체 | 남성 | 여성 |
|---|---|---|---|
| 무신사 데님팬츠 `003002` | 24,673~24,692* | **14,275** (`gf=M`) | **14,767** (`gf=F`) |
| 29CM 데님 팬츠 | — | `272104104` **694** (7페이지 실측) | `268106104` **2,580** (26페이지 실측) |

\* 실시간 변동 — 같은 날 두 번 조회한 값의 범위다.

**발견 — 무신사는 성별 축의 축소 효과가 제한적이다**: 남(14,275)+여(14,767)=29,042 >
전체(24,692). **유니섹스 상품이 양쪽에 중복 집계**되기 때문에 성별로 좁혀도 절반이 아니라
~40%만 줄어든다. 무신사 데님팬츠는 성별로 좁혀도 1.4만이라 상한(5,000)을 넘으므로,
**세부 카테고리 축과 병행**해야 상한 아래로 내려온다.

29CM는 성별이 카테고리 트리에 내장돼 있어 소분류를 고르면 규모가 바로 작아진다 —
여성 데님 2,580개·남성 694개는 협의 없이 진행 가능한 규모다.

## 5. 입출력 파일 규약

```
<작업 폴더>/
├── data/
│   ├── raw/<site>-<story>-<대상>-<YYYYMMDD-HHmm>.json   # 수집 원본(체크포인트 겸용)
│   ├── snapshots/<site>-ranking-<카테고리>-<YYYYMMDD-HHmm>.json
│   ├── images/                                          # 속성 분류용 임시 이미지
│   ├── validation.json / diff.json
└── output/<story>-<대상>-<YYYYMMDD-HHmm>.html
```

### 스냅샷 축적 공간 (v3 확정, 2026-07-29)

- **위치 고정**: 이 저장소 루트의 `data/snapshots/` — **git에는 올리지 않는다**(.gitignore).
  주기 실행이 매번 같은 곳에 쌓여야 diff가 성립하므로, 스케줄을 등록할 때는
  이 폴더의 **절대 경로**로 고정한다
- **보존**: 데모 단계에서는 전부 보존한다. 정리·압축 정책 없음.
  (30분 주기 기준 하루 48개 ≈ 2~3MB/카테고리 — 당분간 문제없는 규모)
- 폴더의 무결성은 읽기 시점에 코드가 보장한다 — `diff_snapshots.py`가 meta 기준으로
  site/target/기간을 걸러내므로, 폴더에 다른 스냅샷이 섞여 있어도 안전하다

## 6. 데이터 계약 (수집 JSON 스키마)

공통 메타: `{site, story, target, collected_at, item_count, source_total, incomplete, notes[]}`

`source_total`은 **사이트가 말한 총 개수**다. 수집 완전성 판정(±5%)의 기준이 된다.
사이트가 총계를 주지 않으면(29CM 브랜드 목록 — §7) 순회해 센 값을 담고 그 사실을 `notes`에 남긴다.
**자사몰은 화면 총계가 아예 없어 `source_total`을 `null`로 둔다** (v14 — §4 스토리1).

`site`는 **사이트 식별자**다. 무신사는 `musinsa`, 29CM은 `29cm`, **자사몰은 도메인 문자열**
(`insilence.co.kr`)이다 (v14). 값의 종류를 코드가 제한하지 않는다 — 새 플랫폼은 새 값으로 들어온다.

### 멀티 플랫폼은 계약을 바꾸지 않는다 (v6)

**수집 JSON은 언제나 사이트 하나짜리다.** 스토리1을 여러 플랫폼으로 돌려도 사이트마다
JSON을 따로 만들고, **합집합·매칭·통합축은 `build_report.py`가 여러 JSON을 받아 계산한다.**
수집 단계에 매칭 결과를 섞어 넣지 않는다.

이유는 §2 설계 원칙 2와 같다 — 계약이 수집(에이전트)과 처리(코드) 사이의 이식성 경계다.
매칭을 수집 쪽에 두면 플랫폼을 추가할 때마다 수집 로직이 서로를 알아야 하고, 매칭 규칙을
고칠 때 재수집이 필요해진다. 처리 쪽에 두면 **같은 원본으로 매칭 규칙만 바꿔 다시 그릴 수
있다.** 새 플랫폼은 JSON 하나를 더 넘기는 것으로 들어온다.

### 상품 레코드

| 필드 | 필수 | 설명 |
|---|---|---|
| `product_id` `name` `url` `image_url` `brand` `category` | 항상 | 결측률 <5% 기준 대상 |
| `price_original` `price_sale` `discount_rate` | 항상 | 할인 없으면 sale=original. **판매가 = 전 회원 공통 쿠폰적용가**(v8, §7) |
| `sold_out` | 항상 | 품절도 수집하되 표시한다 |
| `review_count` `rating` | 노출 시 | 미노출 `null` |
| `view_count` `purchase_count` `like_count` | 노출 시 | 미노출 `null` |
| `view_count_display` `purchase_count_display` | 구간 표기 시 | **v2 신규** — 아래 참조 |
| `viewers_now` `buyers_now` | 랭킹 목록만 | **v2 신규** — "N명이 보는 중/구매 중" |
| `rank` | 스토리3 | 랭킹 순위 |
| `attributes` `attributes_basis` | 스토리2 | 분류 결과와 그 근거(`name`/`detail`/`image`/`unknown`) |

`reviews[]`(리뷰 본문)는 **v3에서 계약에서 제외됐다.** 담겨 오면 `validate_data.py`가 경고한다.

### 구간 표기 값을 정수로 바꾸지 않는다 (v2 신규)

무신사 상품 상세의 조회수는 **`"300회 이상 (최근 1개월)"`처럼 구간으로 표기된다.**
이걸 `view_count: 300`으로 담으면 없는 정밀도를 만들어내는 것이고, 그 값으로 순위를
매기면 리포트가 조용히 틀린다.

규칙: **정확한 정수가 노출될 때만 `view_count`에 담는다. 구간 표기면 `view_count`는
`null`로 두고 원문을 `view_count_display`에 그대로 담는다.**
리포트는 구간 표기를 문구 그대로 싣고, 그 값으로는 순위 차트를 만들지 않는다.
둘 다 채워져 있으면 `validate_data.py`가 경고한다.

### 사이트별 노출 지표 (v2 — v1 가정 정정)

| 지표 | 무신사 | 29CM |
|---|---|---|
| 후기 수 / 평점 | 노출 | 노출 |
| **좋아요(하트)** | **노출** (상품 하트 수) | **노출** (`heartCount`) |
| **조회수** | **구간 표기로 노출** (`300회 이상 (최근 1개월)`) | **미노출** (필드 자체 없음) |
| 누적 판매량 | 랭킹 목록 배지 (`판매 5.5천개`) | 기본 꺼짐 (`isDisplaySellQty: false`) |
| 실시간 "보는 중/구매 중" | **노출** (랭킹 목록만) | 없음 |

> v1은 "29cm는 조회수 미노출"만 가정했고 무신사는 조회수·좋아요가 정수로 다 있다고 봤다.
> 실제로는 무신사 조회수가 구간 표기이고, 좋아요는 양쪽 다 노출한다.
> 사이트별 상세는 `references/<site>.md`의 매핑 표에서 관리한다.

#### 실시간 지표는 화면(페이지)별로 갈린다 (v6 실측, 2026-07-30)

유저 스토리의 "조회수 몇 개인지"는 `view_count`(구간 표기)가 아니라
**`viewers_now`(`N명이 보는 중`)** 다. 그런데 이 값이 붙는 화면이 랭킹뿐이다.

| 화면 | `viewers_now` | `buyers_now` | `purchase_count` 배지 |
|---|---|---|---|
| 무신사 랭킹 | **노출** (`003000` 46% · `003002` 29%) | 1,019건 중 0~3건 | 노출 |
| 무신사 카테고리 목록 | **없음** (0건) | 없음 | 없음 |
| 무신사 브랜드 목록 | **없음** | 없음 | 없음 |
| 무신사 상품 상세 | 없음 | 없음 | — |

**따라서 스토리1(라인시트)·스토리2(전수조사)의 `viewers_now`/`buyers_now`는 구조적 `null`이고
리포트에 "미노출"로 찍힌다** (2026-07-30 사용자 확정). 수집 경로가 없으므로 요구에서 뺀다.
랭킹에서 교차 조인해 채우는 것도 하지 않는다 — 출처가 섞이고 랭킹 안에 든 상품만
채워져 일관성이 깨진다. `buyers_now`는 랭킹에서도 사실상 미노출이다.

#### 축약 표기는 정수로 파싱한다 (v15, 2026-07-30 사용자 결정 — v6 개정)

화면은 좋아요를 `1.2천`, 누적판매를 `판매 9만개`로 **축약**해 보여준다.
v6은 구간 표기 규칙을 여기에도 적용해 파싱을 금지했으나, **v15에서 축약만 허용으로 뒤집는다.**

**근거 — 두 표기의 오차 성질이 다르다.**

| 표기 | 예 | 참값 범위 | 상대 오차 |
|---|---|---|---|
| **축약** | `1.2천` / `판매 64만개` | 1,150~1,249 / 635,000~644,999 | **≤ ±4%** (자릿수가 클수록 작아진다) |
| **구간** | `300회 이상 (최근 1개월)` | 300~∞ | **무한** |

축약은 반올림이라 오차가 유계이고, 그 정도 오차로 뒤집히는 판정이 이 스킬에 없다
(정렬·분포·합계 전부 자릿수 규모의 비교다). 구간은 상한이 없어 같은 논리가 성립하지 않는다.

- **축약값은 정수로 파싱해 정수 칸(`like_count`·`purchase_count`·`viewers_now` 등)에 담는다.**
  `천`=×1,000 · `만`=×10,000. **가능하면 원문도 `*_count_display`에 함께 담는다**(출처 보존) —
  둘이 같이 있어도 검증기가 경고하지 않는다(축약 패턴일 때만)
- **구간 표기는 v2 규칙 그대로다** — `view_count`는 `null`, 원문만 `view_count_display`에.
  정수와 함께 있으면 **검증기가 경고한다**. 이 규칙은 v15에서 바뀌지 않았다
- 정확값을 주는 경로가 있으면 여전히 그쪽이 낫다 — 좋아요는 무신사 하트 배치 API가
  정수를 준다. **경로 A(기본)로 모으면 축약 파싱 자체가 거의 필요 없다** — 이 규칙은
  주로 경로 B로 내려갔을 때 쓰인다. 어느 경로로 채웠는지 `meta.notes`에 남긴다
- **반올림 노출값임을 리포트 각주로 밝힌다.** 파싱을 허용한 것이지 정밀도가 생긴 것이 아니다
- **소급 영향**: 기존 축적 스냅샷 44개(`purchase_count` 2,880건 · `viewers_now` 138건)가
  이미 이 방식이었다 — v15로 **소급 정합**이 된다. 마이그레이션 대상이 없다

### 평점 스케일 통일

`rating`은 **5점 만점**으로 통일한다. 무신사는 목록 응답의 `reviewScore`가 **0~100**,
상세의 `satisfactionScore`가 0~5로 **한 사이트 안에서 스케일이 두 개다.**
변환을 빠뜨리면 `validate_data.py`가 "평점이 0~5 범위를 벗어난다"고 경고한다.

## 7. 강건성 정책

- **속도**: 페이지 요청 간 0.5–1.5초 대기. 페이지네이션 안전 상한 200페이지
- **재시도**: 실패 시 2회 재시도(간격 증가). 3연속 실패 시 건너뛰고 `notes`에 기록
- **차단**(403/429/캡차): 즉시 중단 → 수집분까지 **부분 리포트**, `incomplete: true`와
  중단 지점 명시. **우회 금지, User-Agent 위장 금지**
- **레이아웃 변경 감지**: 필수 필드 결측률 >30%면 "사이트 구조 변경 의심" 경고를
  리포트와 대화 양쪽에 표시하고 수집을 계속하지 않는다
- **체크포인트**: 페이지 단위로 `data/raw/`에 저장 → 중단 시 이어서 재개
- **입력 검증**: 존재하지 않는 브랜드/카테고리는 0건임을 확인하고 명시적으로 보고
  (빈 리포트를 만들지 않는다). 모호한 카테고리는 세부 후보를 제시

### v2 신규 — 조사에서 나온 함정들

- **평점 스케일이 사이트 안에서 두 개** — 목록 0~100 / 상세 0~5 (무신사). 5점으로 통일
- **`totalPages`를 믿지 않는다** — 무신사 리뷰 212건이 `totalPages=22`로 오지만 실제로는
  21페이지에서 끝난다. **빈 결과가 나올 때까지** 돌아 종료를 판정한다
- **리뷰 페이지 시작 번호가 사이트마다 다르다** — 무신사 1부터, 29CM **0부터**
- **`pageSize` 상한** — 무신사 리뷰는 20이 상한, 21 이상은 400
- **잘못된 파라미터가 조용히 무시된다** — 무신사 `sortCode`는 모르는 값을 넣어도 400 없이
  기본 정렬로 폴백한다. 정렬을 바꿨으면 결과가 실제로 달라졌는지 확인한다.
  반대로 `caller`는 **없으면 400**이다(브랜드 `BRAND` / 카테고리 `CATEGORY`)
- **스냅 지표를 상품 지표로 읽지 않는다** — 무신사 상품 상세의 `snapReviews.aggregations`에
  `viewCount`/`likeCount`가 있으나 **스냅 게시물 지표**다. 상품 조회수가 아니다
- **마크다운 변환 결과로 "데이터 없음"을 판정하지 않는다** — 29CM 상품 상세는 변환기를
  거치면 빈 껍데기로 보이지만 원본 HTML에는 JSON-LD가 들어 있다. 원본을 확인한다
- **죽은 URL** — `store.musinsa.com/app/contents/bestranking`,
  `search.musinsa.com/ranking/best`, `/brands/`(복수형)는 전부 실패한다

### v4 신규 — 조사에서 나온 함정

- **무신사 카테고리 PLP API의 page≥2는 서명이 필요하다** — page 번호를 직접 조립해
  요청하면 403 "잘못된 접근입니다"이지만, **page=1 응답의 `nextPageUrl`(hmacId 서명 포함)을
  그대로 따라가면 200이다** (2026-07-29 실측). 브랜드 PLP와 같은 규칙이 카테고리에도
  적용된다: **페이지 URL을 조립하지 말고 항상 `nextPageUrl` 체인을 따른다.**
  부가 확인: 화면 URL(`/category/{code}/goods`)은 `page` 파라미터를 무시하고 항상
  1페이지를 렌더한다(무한 스크롤) — **카테고리 전량 수집은 경로 A 체인이 기본이다**(v16).
  경로 B로 폴백할 때는 화면 스크롤 순회로 한다(60개/초 실측).
  가격 필터 `minPrice`/`maxPrice`는 API·화면 모두 동작 확인(총계가 실제로 줄어든다)

### v9 신규 — 품절 누락과 성별 필터 유실 (2026-07-30 실측)

- **무신사 목록은 `isSoldOut=true` 없이는 품절을 주지 않는다.**
  경로 A 응답 180건이 전부 `isSoldOut: false`, 브라우저 DOM 3,006건에 품절 배지 0개였다.
  `soldOut`·`includeSoldOut`·`excludeSoldOut`·`saleStatus`·`soldOutYn`·`onSale`·`status`·
  `displayStatus`는 **전부 무효**고 `isSoldOut=true`만 총계를 바꾼다(261→440).
  응답이 `false` 171 + `true` 9 혼합이므로 의미는 **"품절 포함"**이다.
  화면도 같은 파라미터를 받는다(`?isSoldOut=true` → 총계 440, 품절 배지 146개)
- **카테고리 화면은 스크롤하면 성별 필터를 잃는다.** 칩(`여성`)과 총계(3,004개)는 유지되는데
  `page=2` 이후 목록 요청이 `gf=A`로 나간다. 끝까지 스크롤하면 **5,010건**이 잡히고 그 안에
  **남성이 2,082건**이다. UI의 `여성` 버튼을 눌러도 URL이 `gf=A`로 되돌아간다.
  → **성별로 좁힌 범위는 경로 A(PLP API)만 끝까지 지킨다**(`gf=F` totalCount 3,004 = 수집 3,004)
- 부수 교훈 — **카드 경계를 링크 수로 세지 마라.** 카드에는 이미지·상품명 두 개의
  `/products/` 링크가 있어서 "링크 1개인 조상"으로 찾으면 실패하고, 가격 텍스트만 기준으로
  올라가면 **6개 카드를 담은 행 컨테이너**까지 올라가 이웃 카드 텍스트가 섞인다.
  **고유 product_id가 1개인 가장 가까운 조상**이 카드다(이 실수로 한 번 오진했다)

### v5 신규 — 인코딩 (2026-07-30 실측, 실제 사고)

- **무신사 상품 상세 HTML에 `<meta charset>` 선언이 없다.** 본문은 UTF-8인데 선언이
  없으니 도구·로케일에 따라 latin-1로 디코드되고, 그러면 한글이 `ë ë/ë¼ì´ëì¤`처럼
  깨진 채로 수집된다. **숫자 필드는 멀쩡하므로 결측률 검증을 그대로 통과한다** —
  눈으로 리포트를 봐야만 드러나는 실패다.
  - **응답 바이트를 UTF-8로 명시해 디코드한다.** 도구 기본값에 맡기지 않는다
  - **깨진 문자열에 `strip()`을 걸면 안 된다** — mojibake의 끝 바이트가 `\xa0`(NBSP)나
    `\x85`(NEL)일 때가 있고 파이썬은 이걸 공백으로 보고 지운다. 그러면 역변환으로도
    복구가 불가능해진다
- **검증기가 이걸 잡는다 (v5 신규)**: `validate_data.py`가 `name`·`brand`·`category`·
  `*_display`에서 "UTF-8 선두 바이트 + 연속 바이트" 패턴을 찾아 **FAIL(exit 2)**을 낸다.
  제대로 디코드된 한글은 latin-1로 인코딩이 아예 안 되므로 오탐이 없다.
  결측률이 아니라 **문자 자체의 온전함**을 보는 첫 검사다
- 사고 기록(2026-07-29 → 07-30 복구): 인사일런스 라인시트의 `category` **564행 전부**가
  깨진 채 리포트까지 나갔다. 카테고리 37종 중 24종은 latin-1→UTF-8 역변환으로 무손실
  복구했고, 끝 바이트가 유실된 13종은 **대표 상품 PDP를 다시 받아 실측값으로** 복구했다
  (추측으로 채우지 않았다)

### v6 신규 — 경로 B(브라우저) 함정 (2026-07-30 실측)

전부 **조용히 틀린 데이터를 만드는** 종류다. 예외도 에러도 나지 않는다.
v16에서 경로 B는 백업이 됐지만 **폴백으로 내려간 순간 이 함정들이 그대로 적용된다.**
첫 항목(기본 필터)은 화면·API가 같은 파라미터를 쓰므로 **경로와 무관하게** 적용된다.

- **⚠️ 목록의 기본 필터가 사이트마다 다르고, 화면 총계도 그 필터의 함수다 (2026-07-30 실측).**
  이번에 발견한 것 중 가장 큰 오차원이다.

  | 사이트 | 브랜드 목록 기본값 | 인사일런스 실측 |
  |---|---|---|
  | 무신사 | **품절 제외** | 563 → `&isSoldOut=true`면 **2,022** |
  | 29CM | **품절 포함** (필터 이름이 `품절상품 제외`다) | 435 (그중 품절 131) |

  - **화면 총계 대조를 통과해도 전량이 아닐 수 있다.** 563은 "품절 제외 총계"였고
    대조는 정상 통과했다. `source_total`을 읽을 때 **어떤 필터가 걸린 상태인지 함께
    기록해라.** 필터를 명시하지 않은 총계는 검증 근거로 불완전하다
  - **플랫폼 비교에서는 필터를 반드시 맞춰라.** 맞추지 않으면 결론이 뒤집힌다 —
    실측: 무신사(품절 제외 563) vs 29CM(품절 포함 435)으로 비교했을 때 **"29CM 단독 입점
    83건"**이 나왔는데, 무신사를 품절 포함(2,022)으로 맞추자 **90건이 무신사 품절 상품과
    매칭돼 29CM 단독이 0건**이 됐다. 단독 입점 분석이 통째로 필터 산물이었다
  - **함정: `soldOut=true`를 보내면 조용히 무시되고 563이 온다.** 먹는 이름은 `isSoldOut=true`다
  - 스토리1은 **품절 포함이 기본이다** (2026-07-30 사용자 결정). 입점 여부를 보는 분석이라
    품절이어도 "낸 것"은 낸 것이다. 이 결정으로 인사일런스가 2,022개가 되어
    **스토리1 상한을 3,000으로 올렸다**(구 2,000)
  - 품절 이력을 넣으면 **같은 정규화 이름이 여러 번 나온다** — 실측 3건(재등록으로 보이며
    양쪽 다 품절). 합집합 행 수가 수집 건수보다 적어지는 정상 현상이다
- **목록 DOM이 가상화돼 있다 — 끝까지 스크롤한 뒤 한 번 읽으면 마지막 66개만 잡힌다.**
  무신사 PLP 실측: 페이지 높이가 4,518px → 260,538px(57배)로 늘어도 DOM 안의 상품 링크는
  **132~144개로 고정**(상품 66~72개 창)이고 노드 4,680개·heap 132MB로 평탄하다.
  → **매 스크롤 스텝마다 수집해 `product_id`로 누적한다.** 실측에서 실제로 당한 함정이다.
  (가상화 덕분에 메모리는 3,600개 구간에서도 평탄하므로 규모 상한의 제약은 브라우저가 아니다)
- **스크롤 종료 판정 = 3연속 무증가** (2026-07-30 사용자 확정).
  근거: 랭킹은 끝에 닿으면 증가가 **완전히 0**이 된다(8스텝 연속 0 확인). 36개/스텝을
  유지하다 24개 → 이후 0. 그래서 3회면 충분하다.
  - **무신사 카테고리 PLP는 자연 종료가 없다** — 24,686개까지 계속 나온다(60개/초, 감속 없음).
    스토리별 상한(스토리1 3,000 / 스토리2 5,000)에 닿으면 **사용자에게 알리고 마감**한다.
    상한에 걸려 끊은 것과 목록이 끝난 것을 리포트에서 구분한다
  - **29CM는 스크롤이 아니라 `&page=` 페이지네이션이다** — 빈 페이지 +
    `"선택한 필터의 검색 결과가 없습니다."`로 **결정적으로** 끝난다. 무증가 추정이 필요 없다
- **무신사 브랜드는 URL이 두 개고 하나는 전 상품이 아니다.**
  `/brand/{slug}`는 브랜드 **홈**(추천·기획전)이라 인사일런스에서 **128개**에서 끝나고,
  전 상품은 **`/brand/{slug}/products`**로 **563개 = 화면 총계**다.
  둘 다 200이고 둘 다 상품이 보이므로 **건수를 화면 총계와 대조하지 않으면 128/563만
  모은 것을 알 수 없다**
- **평점 스케일이 경로별로 다르다** — 화면(경로 B)은 `4.9`(0~5), 무신사 목록 API는 0~100.
  §6 평점 스케일 통일 규칙을 적용할 때 어느 경로로 읽었는지에 따라 변환이 달라진다
- **29CM 카드에는 가격에 `원`이 없다** (`57,960`). `원`을 기준으로 카드 컨테이너를 찾으면
  카드(`li`)를 지나쳐 `ul`까지 올라가고 **카드 텍스트가 이웃 카드로 번진다**(실측에서 발생).
  29CM 카드는 `li`이고 카드마다 `/catalog/` 링크가 4개다
- **랭킹 대분류 코드가 조용히 무시된다** — §4 스토리3 참조. `003` → `200/000`

### v6 신규 — 29CM 브랜드 목록 (2026-07-30 실측)

- **BEST API에 브랜드 facet이 있다** — `facets.brandFacetInputs[].frontBrandNo`.
  DTO에 없는 필드는 잘못된 값을 보내도 200 + 무시로 넘어가지만, **실존 필드는 400과
  함께 필수 하위 필드명을 알려준다.** 이 성질로 필드명을 찾아냈다.
  `frontBrandNo`는 `/store/brand/{brandId}` URL의 ID와 같은 값이다
- **브랜드 ID 확증 경로가 열렸다** — 브랜드 페이지의 `__NEXT_DATA__`에
  `{"brandId":1642,"nameKor":"인사일런스","nameEng":"INSILENCE"}`가 **서버 렌더**된다.
  상품 목록은 없지만 브랜드 메타는 있으므로 ID→이름 확인이 된다.
  기존 "brandId=1642는 검색 결과 제목으로 추정"이라는 미검증 항목이 해소됐다
- **가격이 세 개다 — 판매가는 쿠폰적용가로 담는다 (v8 개정, 2026-07-30)**.
  실측: `originalPrice` 54,000 / `sellPrice` 43,200 / `displayPrice` 37,160.
  `displayPrice`가 쿠폰적용가고 **이것이 계약의 `price_sale`이다.**
  `saleRate`도 `displayPrice` 기준이라 짝이 맞으므로 재계산이 필요 없다.
  구 규칙(`sellPrice`만 쓰고 쿠폰가는 버림)은 폐기됐다 — 로우클래식 100개 중 22개가
  `displayPrice != sellPrice`이고, 그중 정가=판매가인데 쿠폰만 걸린 상품은
  `sellPrice`로 담으면 **"할인 없음"으로 기록된다**
- **이 API는 랭킹이라 창을 좁게 잡으면 카탈로그가 조용히 잘린다** — 인사일런스 유니크
  상품 수: `MONTHLY`+`CLICK` **422** / `MONTHLY`+`POPULARITY` 421 / `DAILY` 409 /
  `WEEKLY` **365** / `HOURLY` **26**. 전 조합 합집합도 422다
  (**그리고 그 422가 전량이 아니다 — 실제 카탈로그는 435개다.** 위 총계 항목 참조).
  **스토리 C 기본값인 `HOURLY`를 스토리 A에 쓰면 카탈로그의 94%가 사라진다.**
  스토리 A는 `MONTHLY`로 **정렬 종류(`POPULARITY`/`CLICK`/`ORDER`/`LIKE`)를 훑어 합집합**을
  만든다. 누락된 상품은 **"상대 플랫폼 단독 입점"으로 잘못 집계**되어 §4 스토리1의
  단독 입점 분석을 직접 오염시킨다. `MONTHS_3`·`MONTHS_6`은 HTTP 500이라 못 쓴다
- **총계를 주지 않는다** — `pagination`에 `page`/`size`/`hasNext`만 있고 총 개수가 없다.
  브랜드 페이지 `__NEXT_DATA__`에도 상품 수가 없다(`likeCount`만 있다).
- **⚠️ 그래서 쓰던 완전성 근거 두 개(`hasNext=false` 도달 · 정렬 조합 간 합집합이 더 늘지
  않음)는 완전성을 보장하지 않는다 — 브라우저 대조로 반박됐다 (2026-07-30).**
  두 근거를 **모두 만족한 422건**이 실제로는 **435건 중 422건**이었다. 누락 13건 중
  **11건이 품절**이다. 합집합이 422에서 포화되는 것은 **완전성의 근거가 아니라
  정렬들이 같은 집합을 반복해서 준다는 뜻**이었다.
  **누락 원인은 특정하지 못했다** — 품절 필터가 아니고(수집분에 품절 120건 존재)
  저반응 때문도 아니다(누락분 좋아요 4~39·후기 0~3은 수집분이 커버하는 구간).
  누락이 품절 쪽으로 3배 치우칠 뿐이다(85% vs 28%). **예측이 불가능하다는 것이
  화면 총계 대조를 유일한 근거로 만든다.**
  - **완전성 근거는 화면 총계 대조뿐이다** — 브랜드 페이지가 `435개`를 화면에 노출한다.
    `source_total`에는 **화면 총계**를 담고, 수집 건수를 넣지 않는다(넣으면 순환 검증이 된다)
  - **v16 처리: 브랜드 카탈로그는 경로 A(BEST API 합집합)로 모으되 화면 총계와 대조하고,
    어긋나면 경로 B 순회로 보완한다** (§2). 카테고리 전수는 PLP API(`totalCount` 제공)로
    경로 A 안에서 완결된다
  - 품절 누락은 §4 스토리1의 **단독 입점 분석을 직접 오염시킨다** — 상대 플랫폼에만 있는
    것처럼 집계된다. 아래 `HOURLY` 함정과 같은 종류의 사고이며 원인만 다르다
- **중복 상품이 온다** — 인사일런스 5페이지에서 422건을 받았으나 유니크 `itemId`는 421개다.
  **`product_id` 기준 dedup이 필수다** (랭킹순 페이지네이션 중 순위가 흔들리면 생기는
  현상으로 보인다). 스토리1은 중복이 경고 대상이므로 dedup 없이 넘기면 검증기가 짚는다
- **카테고리는 코드로만 온다** — `extraInfo.categories[]`에 `largeCategoryCode` /
  `mediumCategoryCode` / `smallCategoryCode`가 있고 이름이 없다. 이름은 카테고리 트리 API
  (`recommend-api.29cm.co.kr/api/v4/best/categories?categoryList=<코드,..>`)로 해석한다.
  `smallCategoryCode`가 `null`인 상품이 있어 **중분류로 폴백**해야 한다

## 8. 성공 기준 (스킬 전체)

정량:
- 트리거 테스트: should-trigger 10건 중 ≥9 로드, should-NOT-trigger 6건 중 0 로드.
  **트리거 필요조건: 요청에 플랫폼 이름(무신사/29cm) 명시** — 브랜드/카테고리만으로는
  트리거하지 않는다
- 스토리별 자연어 요청 1개로 완주 (파라미터 누락 시 질문 1회 허용,
  스토리2 범위 협의는 예외적으로 허용)
- 필수 필드 결측 <5%, 재시도로 해소되지 않은 실패 요청 0
- **스크립트 회귀 테스트 전건 통과** (`python3 tests/run_tests.py`)

정성 (DoD):
- 3개 스토리 데모: 인사일런스 라인시트 / 데님팬츠 전수조사 / 무신사 바지 랭킹 top 100
- 결과물이 HTML 페이지로 저장되고 브라우저에서 열림
- 스킬이 폴더/zip으로 배포 가능, 사용법 문서화
- 선빈 컨펌

## 9. SKILL.md frontmatter

```yaml
---
name: commerce-research
description: 무신사, 29CM에서 상품·랭킹 데이터를 수집해 HTML 리포트로 만든다. 브랜드
  전 상품 라인시트, 카테고리 시장 전수조사(노출된 후기 수·평점 통계, 핏 등 속성 분류),
  랭킹 top 100 스냅샷과 기간별 변화·할인 시점 추적에 쓴다. 요청에 무신사/musinsa/29cm/29CM
  같은 플랫폼 이름이 명시되고 "라인시트", "전수조사", "리뷰 분석", "랭킹 변화",
  "랭킹 모니터링", "모니터링 시작", "추적", "시장 조사"
  같은 수집·분석 태스크가 함께 있을 때 쓴다. "상품들 표로 만들어줘", "상품 다 모아서
  보여줘", "상품 목록 정리해줘"처럼 일상 표현으로 상품 수집·정리를 요청할 때도 쓴다.
  플랫폼 이름 없이 브랜드나 카테고리만 말하면 쓰지 않는다. 상품을 대신 구매하거나,
  개인 쇼핑 추천을 하거나, 지원하지 않는 쇼핑몰(쿠팡·에이블리 등)을 다룰 때도 쓰지 않는다.
compatibility: 웹 페이지를 가져올 수 있는 도구가 필요하다. 코드 실행이 되면 scripts/로
  검증과 리포트를 만들고, 안 되면 assets/report-template.html 구조를 따라 직접 작성한다.
metadata:
  version: 1.2.0
---
```
