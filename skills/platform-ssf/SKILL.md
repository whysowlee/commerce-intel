---
name: platform-ssf
description: SSF샵(ssfshop.com — 삼성물산 패션부문)에서 상품·카테고리 데이터를 수집해
  commerce-intel 데이터 계약 JSON을 만든다. 요청에 SSF샵/SSF/ssfshop이 명시되고 수집·정리
  태스크가 함께 있을 때 쓴다. 예: "SSF샵 여성 데님 정리해줘", "SSF샵 팬츠 카테고리
  전수조사", "SSF샵 데님 판매량순 상위 뽑아줘", "SSF 랭킹 계속 모아줘". **랭킹 크론 축적이
  허용된다(D41, 2026-08-04 사용자 승인 — 구 D30의 건별 제한을 개정)**. 다른 플랫폼
  요청이나 상품 구매 대행에는 쓰지 않는다.
compatibility: 웹 요청(HTML) 도구로 수집이 완결된다. 쿠키·referer·서명 불필요 —
  브라우저 제어 도구가 필요 없다.
metadata:
  version: 0.1.0
  status: draft            # draft → ready 승격은 사용자가 한다
  refresh-cycle: 랭킹 60분 · 그 외 1440분   # 사이트 명시 "매 시간마다 갱신" (2026-08-04)
  measured-at: "2026-08-04"   # 가장 최근 실측일
---

# platform-ssf

SSF샵에서 **화면에 노출된 값만** 모아 데이터 계약 JSON을 만든다. DB 적재·분석·리포트와
공통 규칙(속도·차단 중단·추정 금지·순회 상한)은 `commerce-intel` 오케스트레이터가 갖는다.

> ⚠️ **이 스킬은 초안(draft)이다.** `「미검증」` 표기 항목은 사실이 아니라 확인 과제다.
> 미검증 항목에 의존하는 수집을 하게 되면 그 사실을 사용자에게 먼저 알린다.

## 수집 신분 — 랭킹 크론 축적 허용 (D41, 2026-08-04 사용자 승인)

**랭킹 주기 축적을 등록해도 된다.** 구 D30은 이 사이트를 "사용자 지시 건별 수집만,
크론 금지"로 뒀는데 **2026-08-04 사용자 승인으로 개정됐다**(D41). robots.txt가 Claude UA
3종(ClaudeBot·Claude-User·Claude-SearchBot)을 명시 허용하는 것은 그대로이고(2026-08-03
실측), 바뀐 것은 우리 판단이다 — 이력은 SPEC-INTEL D30·D41에 있다.

**주기는 1시간이다** — 사이트가 `/ranking` 페이지에 "순위는 매 시간마다 갱신됩니다"라고
**직접 밝혔다**(2026-08-04 실측, §R). 4개 사이트 중 유일하게 확정된 값이다.

`/secured/`·`/raffle/`은 robots가 금지한 경로다 — 건드리지 않는다.

## 수집 절차

1. **`references/adapter.md`를 먼저 읽는다** — 검증된 요청 스킴·필드 매핑·함정이
   실측 일자와 함께 있다. 어댑터에 없는 스킴을 사실처럼 쓰지 않는다.
2. **카테고리 코드 확인** — `sitemap_category.xml`에 1,353개 카테고리 URL이 전부
   나열된다. 코드는 접두사+3자 세그먼트 계층(예: `SFMA41` 여성 → `SFMA41A04` 팬츠 →
   `SFMA41A04A07` 데님). 대분류 10종은 adapter.md §3. 검색 경로는 「미검증」이라
   카테고리 경로로 대상을 잡는다. 브랜드 단위 목록 경로도 「미검증」이다.
3. **총계 확보** — 목록 응답의 hidden input `ctgryGodsListTotalRow`가 독립 총계다.
   **총계는 필터의 함수다**(기본 4,233 / 품절제외 4,091 실측) — `source_total`에 담을 때
   읽은 시점의 필터 상태를 `meta.notes`에 병기한다. 수집 건수를 넣지 않는다(순환 검증 금지).
4. **목록 순회** — `selectProductList`를 `currentPage` 증가로 돈다. 페이지당 60개 고정.
   깊은 페이지 상한 없음(p2700 정상 실측). 종료 판정은 수집 건수의 총계 도달 + 빈 페이지.
5. **품절 처리** — **기본 목록이 품절 포함이다**(제외가 opt-in: `benefit=EXC_SLDOUT`).
   단 판매량순 정렬에서는 품절품이 꼬리로 밀린다 — 1페이지만 보고 "품절 미노출"로
   판정하지 마라(adapter.md 함정 1).
6. **검증 대조** — 수집 건수를 총계와 나란히 보고한다. 다르면 그 차이를 밝힌다.

## 이 플랫폼의 노출 지표

| 계약 필드 | 노출 | 주의 (상세는 adapter.md) |
|---|---|---|
| review_count / rating | 노출 | rating은 **5점 만점 그대로**(변환 불요). 리뷰 없는 상품은 결측 → `null` 정확 (실측 결측 28.3%) |
| like_count | 노출(하트) | 결측 0% 실측 |
| view_count | **미노출** | 항상 `null` |
| purchase_count | **미노출** | 항상 `null`. 판매량순 정렬(`SALE_QTY_SEQ`)은 있지만 수치는 안 준다 |
| viewers_now / buyers_now | **미노출** | 항상 `null` |
| category | 요청 dspCtgryNo + 상세 JSON-LD 경로("여성 » 팬츠 » 와이드") | 목록 카드에는 없다 |

미노출 필드는 항상 `null`이다 — 다른 출처에서 끌어와 채우지 않는다.

## SSF샵 고유 특성 (상세는 adapter.md)

- **랭킹 전용 페이지·엔드포인트가 있다**(2026-08-04 실측 — 이전 "별도 BEST 없음" 기록
  정정): `/ranking` 페이지 + `/rankingGodList` XHR. `rankSect`로 클릭랭킹·판매랭킹을
  가른다(§R). **갱신 주기는 1시간 — 사이트가 명시**한다.
  카테고리 정렬 `SALE_QTY_SEQ`(판매량순)도 여전히 쓸 수 있다. **다만 둘이 같은 집계인지는
  확인하지 않았다** — 「관계 재확인 필요」(§R).
- **연령대별 인기순**(`preferAge=1020AGE/30AGE/40AGE/50AGE_OVER`) — 다른 플랫폼에 없는
  축이다. 쓰게 되면 `raw_extras`에 담고 해석하지 않는다.
- 목록 카드에 색상 변형 목록이 노출된다 — `raw_extras` 후보.
- 상품 URL의 브랜드 슬러그는 장식이다(`/x/{godNo}/good`도 200) — URL 조립은 godNo만 있으면 된다.

## 참고

- `references/adapter.md` — 검증된 스킴·필드 매핑·함정 (수집 시작 전 필독)
- 스킴이 바뀐 것을 감지하면(400/404·필드 소실·결측 급증): 경로 B는 「미검증」이므로
  `platform-generic` 절차로 폴백해 완주하고 `meta.notes`에 기록한 뒤 사용자에게 보고한다.
  어댑터 개정은 보고 후의 일이다.

## 미검증 목록 (다음 실측의 할 일)

- [ ] 검색 경로 — `/search/result`는 CSR 껍데기, 상품 API 미추적. 카테고리 경로로 수집이
  완결되므로 차단 요인은 아니다
- [ ] 브랜드 단위 목록 경로 — 브랜드 전 상품을 어떻게 받는지 미실측
- [ ] 옵션/사이즈별 재고(`variants[]`) — 상세 HTML에 없고 AJAX, 엔드포인트 미특정
- [ ] 랭킹(판매량순) 갱신 주기 — 시차 2회 대조 필요
- [ ] 개인화 가격 — JSON-LD price와 목록가 일치는 1개 상품만 대조
- [ ] 레이트리밋 임계 — 2초 간격 16요청 무사고까지만 확인
